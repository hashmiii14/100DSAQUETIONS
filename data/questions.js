// Automatically generated 1000 DSA Problems Dataset
const PROBLEMS = [
  {
    "id": 1,
    "number": 1,
    "title": "Basic Count",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Basic Count**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      1,
      2,
      1
    ],
    "prerequisites": [
      1
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 2,
    "number": 2,
    "title": "Digit Sum",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Digit Sum**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      1,
      3,
      1
    ],
    "prerequisites": [
      1
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 3,
    "number": 3,
    "title": "Factorial Calc",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Factorial Calc**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      2,
      4,
      1
    ],
    "prerequisites": [
      1
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 4,
    "number": 4,
    "title": "Prime Test",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Prime Test**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      3,
      5,
      1
    ],
    "prerequisites": [
      2
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 5,
    "number": 5,
    "title": "GCD Comput",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **GCD Comput**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      4,
      6,
      1
    ],
    "prerequisites": [
      3
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 6,
    "number": 6,
    "title": "Fibonacci Term",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Fibonacci Term**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      5,
      7,
      1
    ],
    "prerequisites": [
      4
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 7,
    "number": 7,
    "title": "Power Function",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Power Function**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      6,
      8,
      2
    ],
    "prerequisites": [
      5
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 8,
    "number": 8,
    "title": "Armstrong Number",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Armstrong Number**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      7,
      9,
      3
    ],
    "prerequisites": [
      6
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 9,
    "number": 9,
    "title": "Palindrome Number",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Palindrome Number**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      8,
      10,
      4
    ],
    "prerequisites": [
      7
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 10,
    "number": 10,
    "title": "Leap Year Check",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Leap Year Check**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      9,
      11,
      5
    ],
    "prerequisites": [
      8
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 11,
    "number": 11,
    "title": "Basic Count (Variant 2)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Basic Count (Variant 2)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      10,
      12,
      6
    ],
    "prerequisites": [
      9
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 12,
    "number": 12,
    "title": "Digit Sum (Variant 2)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Digit Sum (Variant 2)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      11,
      13,
      7
    ],
    "prerequisites": [
      10
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 13,
    "number": 13,
    "title": "Factorial Calc (Variant 2)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Factorial Calc (Variant 2)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      12,
      14,
      8
    ],
    "prerequisites": [
      11
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 14,
    "number": 14,
    "title": "Prime Test (Variant 2)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Prime Test (Variant 2)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      13,
      15,
      9
    ],
    "prerequisites": [
      12
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 15,
    "number": 15,
    "title": "GCD Comput (Variant 2)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **GCD Comput (Variant 2)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      14,
      16,
      10
    ],
    "prerequisites": [
      13
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 16,
    "number": 16,
    "title": "Fibonacci Term (Variant 2)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Fibonacci Term (Variant 2)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      15,
      17,
      11
    ],
    "prerequisites": [
      14
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 17,
    "number": 17,
    "title": "Power Function (Variant 2)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Power Function (Variant 2)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      16,
      18,
      12
    ],
    "prerequisites": [
      15
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 18,
    "number": 18,
    "title": "Armstrong Number (Variant 2)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Armstrong Number (Variant 2)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      17,
      19,
      13
    ],
    "prerequisites": [
      16
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 19,
    "number": 19,
    "title": "Palindrome Number (Variant 2)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Palindrome Number (Variant 2)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      18,
      20,
      14
    ],
    "prerequisites": [
      17
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 20,
    "number": 20,
    "title": "Leap Year Check (Variant 2)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Leap Year Check (Variant 2)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      19,
      21,
      15
    ],
    "prerequisites": [
      18
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 21,
    "number": 21,
    "title": "Basic Count (Variant 3)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Basic Count (Variant 3)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      20,
      22,
      16
    ],
    "prerequisites": [
      19
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 22,
    "number": 22,
    "title": "Digit Sum (Variant 3)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Digit Sum (Variant 3)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      21,
      23,
      17
    ],
    "prerequisites": [
      20
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 23,
    "number": 23,
    "title": "Factorial Calc (Variant 3)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Factorial Calc (Variant 3)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      22,
      24,
      18
    ],
    "prerequisites": [
      21
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 24,
    "number": 24,
    "title": "Prime Test (Variant 3)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Prime Test (Variant 3)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      23,
      25,
      19
    ],
    "prerequisites": [
      22
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 25,
    "number": 25,
    "title": "GCD Comput (Variant 3)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **GCD Comput (Variant 3)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      24,
      26,
      20
    ],
    "prerequisites": [
      23
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 26,
    "number": 26,
    "title": "Fibonacci Term (Variant 3)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Fibonacci Term (Variant 3)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      25,
      27,
      21
    ],
    "prerequisites": [
      24
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 27,
    "number": 27,
    "title": "Power Function (Variant 3)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Power Function (Variant 3)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      26,
      28,
      22
    ],
    "prerequisites": [
      25
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 28,
    "number": 28,
    "title": "Armstrong Number (Variant 3)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Armstrong Number (Variant 3)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      27,
      29,
      23
    ],
    "prerequisites": [
      26
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 29,
    "number": 29,
    "title": "Palindrome Number (Variant 3)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Palindrome Number (Variant 3)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      28,
      30,
      24
    ],
    "prerequisites": [
      27
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 30,
    "number": 30,
    "title": "Leap Year Check (Variant 3)",
    "difficulty": "Easy",
    "topic": "Foundations",
    "subtopic": "Implementation / Basic Logic Core Mechanics",
    "phase": "Phase 0 \u2014 Programming & Problem Solving Foundations",
    "pattern": "Implementation / Basic Logic",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Leap Year Check (Variant 3)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Implementation / Basic Logic. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Implementation / Basic Logic technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Implementation / Basic Logic)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Implementation / Basic Logic)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Implementation / Basic Logic)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Implementation / Basic Logic)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      29,
      31,
      25
    ],
    "prerequisites": [
      28
    ],
    "tags": [
      "Foundations",
      "Implementation / Basic Logic",
      "Phase 0",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Implementation / Basic Logic because constraints are N <= 100000, making O(N^2) brute force infeasible. Implementation / Basic Logic leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Implementation / Basic Logic optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 31,
    "number": 31,
    "title": "Rotate Array (Variant 4)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 4)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      30,
      32,
      26
    ],
    "prerequisites": [
      29
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 32,
    "number": 32,
    "title": "Subarray Threshold (Variant 4)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 4)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      31,
      33,
      27
    ],
    "prerequisites": [
      30
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 33,
    "number": 33,
    "title": "Matrix Rotation (Variant 4)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 4)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      32,
      34,
      28
    ],
    "prerequisites": [
      31
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 34,
    "number": 34,
    "title": "Spiral Matrix (Variant 4)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 4)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      33,
      35,
      29
    ],
    "prerequisites": [
      32
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 35,
    "number": 35,
    "title": "Pascal Triangle (Variant 4)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 4)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      34,
      36,
      30
    ],
    "prerequisites": [
      33
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 36,
    "number": 36,
    "title": "Find All Duplicates (Variant 4)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 4)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      35,
      37,
      31
    ],
    "prerequisites": [
      34
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 37,
    "number": 37,
    "title": "Sort Colors (Variant 4)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 4)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      36,
      38,
      32
    ],
    "prerequisites": [
      35
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 38,
    "number": 38,
    "title": "Peak Element (Variant 4)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Peak Element (Variant 4)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      37,
      39,
      33
    ],
    "prerequisites": [
      36
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 39,
    "number": 39,
    "title": "Jump Game (Variant 4)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Jump Game (Variant 4)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      38,
      40,
      34
    ],
    "prerequisites": [
      37
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 40,
    "number": 40,
    "title": "Gas Station (Variant 4)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Gas Station (Variant 4)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      39,
      41,
      35
    ],
    "prerequisites": [
      38
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 41,
    "number": 41,
    "title": "Rotate Array (Variant 5)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 5)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      40,
      42,
      36
    ],
    "prerequisites": [
      39
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 42,
    "number": 42,
    "title": "Subarray Threshold (Variant 5)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 5)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      41,
      43,
      37
    ],
    "prerequisites": [
      40
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 43,
    "number": 43,
    "title": "Matrix Rotation (Variant 5)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 5)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      42,
      44,
      38
    ],
    "prerequisites": [
      41
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 44,
    "number": 44,
    "title": "Spiral Matrix (Variant 5)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 5)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      43,
      45,
      39
    ],
    "prerequisites": [
      42
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 45,
    "number": 45,
    "title": "Pascal Triangle (Variant 5)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 5)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      44,
      46,
      40
    ],
    "prerequisites": [
      43
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 46,
    "number": 46,
    "title": "Find All Duplicates (Variant 5)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 5)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      45,
      47,
      41
    ],
    "prerequisites": [
      44
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 47,
    "number": 47,
    "title": "Sort Colors (Variant 5)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 5)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      46,
      48,
      42
    ],
    "prerequisites": [
      45
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 48,
    "number": 48,
    "title": "Peak Element (Variant 5)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Peak Element (Variant 5)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      47,
      49,
      43
    ],
    "prerequisites": [
      46
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 49,
    "number": 49,
    "title": "Jump Game (Variant 5)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Jump Game (Variant 5)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      48,
      50,
      44
    ],
    "prerequisites": [
      47
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 50,
    "number": 50,
    "title": "Gas Station (Variant 5)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Gas Station (Variant 5)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      49,
      51,
      45
    ],
    "prerequisites": [
      48
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 51,
    "number": 51,
    "title": "Rotate Array (Variant 6)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 6)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      50,
      52,
      46
    ],
    "prerequisites": [
      49
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 52,
    "number": 52,
    "title": "Subarray Threshold (Variant 6)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 6)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      51,
      53,
      47
    ],
    "prerequisites": [
      50
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 53,
    "number": 53,
    "title": "Matrix Rotation (Variant 6)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 6)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      52,
      54,
      48
    ],
    "prerequisites": [
      51
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 54,
    "number": 54,
    "title": "Spiral Matrix (Variant 6)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 6)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      53,
      55,
      49
    ],
    "prerequisites": [
      52
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 55,
    "number": 55,
    "title": "Pascal Triangle (Variant 6)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 6)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      54,
      56,
      50
    ],
    "prerequisites": [
      53
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 56,
    "number": 56,
    "title": "Find All Duplicates (Variant 6)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 6)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      55,
      57,
      51
    ],
    "prerequisites": [
      54
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 57,
    "number": 57,
    "title": "Sort Colors (Variant 6)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 6)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      56,
      58,
      52
    ],
    "prerequisites": [
      55
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 58,
    "number": 58,
    "title": "Peak Element (Variant 6)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Peak Element (Variant 6)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      57,
      59,
      53
    ],
    "prerequisites": [
      56
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 59,
    "number": 59,
    "title": "Jump Game (Variant 6)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Jump Game (Variant 6)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      58,
      60,
      54
    ],
    "prerequisites": [
      57
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 60,
    "number": 60,
    "title": "Gas Station (Variant 6)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Gas Station (Variant 6)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      59,
      61,
      55
    ],
    "prerequisites": [
      58
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 61,
    "number": 61,
    "title": "Rotate Array (Variant 7)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 7)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      60,
      62,
      56
    ],
    "prerequisites": [
      59
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 62,
    "number": 62,
    "title": "Subarray Threshold (Variant 7)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 7)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      61,
      63,
      57
    ],
    "prerequisites": [
      60
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 63,
    "number": 63,
    "title": "Matrix Rotation (Variant 7)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 7)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      62,
      64,
      58
    ],
    "prerequisites": [
      61
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 64,
    "number": 64,
    "title": "Spiral Matrix (Variant 7)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 7)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      63,
      65,
      59
    ],
    "prerequisites": [
      62
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 65,
    "number": 65,
    "title": "Pascal Triangle (Variant 7)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 7)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      64,
      66,
      60
    ],
    "prerequisites": [
      63
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 66,
    "number": 66,
    "title": "Find All Duplicates (Variant 7)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 7)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      65,
      67,
      61
    ],
    "prerequisites": [
      64
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 67,
    "number": 67,
    "title": "Sort Colors (Variant 7)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 7)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      66,
      68,
      62
    ],
    "prerequisites": [
      65
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 68,
    "number": 68,
    "title": "Peak Element (Variant 7)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Peak Element (Variant 7)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      67,
      69,
      63
    ],
    "prerequisites": [
      66
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 69,
    "number": 69,
    "title": "Jump Game (Variant 7)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Jump Game (Variant 7)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      68,
      70,
      64
    ],
    "prerequisites": [
      67
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 70,
    "number": 70,
    "title": "Gas Station (Variant 7)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Gas Station (Variant 7)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      69,
      71,
      65
    ],
    "prerequisites": [
      68
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 71,
    "number": 71,
    "title": "Rotate Array (Variant 8)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 8)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      70,
      72,
      66
    ],
    "prerequisites": [
      69
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 72,
    "number": 72,
    "title": "Subarray Threshold (Variant 8)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 8)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      71,
      73,
      67
    ],
    "prerequisites": [
      70
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 73,
    "number": 73,
    "title": "Matrix Rotation (Variant 8)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 8)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      72,
      74,
      68
    ],
    "prerequisites": [
      71
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 74,
    "number": 74,
    "title": "Spiral Matrix (Variant 8)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 8)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      73,
      75,
      69
    ],
    "prerequisites": [
      72
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 75,
    "number": 75,
    "title": "Pascal Triangle (Variant 8)",
    "difficulty": "Easy",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 8)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      74,
      76,
      70
    ],
    "prerequisites": [
      73
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 76,
    "number": 76,
    "title": "Find All Duplicates (Variant 8)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 8)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      75,
      77,
      71
    ],
    "prerequisites": [
      74
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 77,
    "number": 77,
    "title": "Sort Colors (Variant 8)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 8)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      76,
      78,
      72
    ],
    "prerequisites": [
      75
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 78,
    "number": 78,
    "title": "Peak Element (Variant 8)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Peak Element (Variant 8)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      77,
      79,
      73
    ],
    "prerequisites": [
      76
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 79,
    "number": 79,
    "title": "Jump Game (Variant 8)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Jump Game (Variant 8)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      78,
      80,
      74
    ],
    "prerequisites": [
      77
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 80,
    "number": 80,
    "title": "Gas Station (Variant 8)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Gas Station (Variant 8)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      79,
      81,
      75
    ],
    "prerequisites": [
      78
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 81,
    "number": 81,
    "title": "Rotate Array (Variant 9)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 9)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      80,
      82,
      76
    ],
    "prerequisites": [
      79
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 82,
    "number": 82,
    "title": "Subarray Threshold (Variant 9)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 9)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      81,
      83,
      77
    ],
    "prerequisites": [
      80
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 83,
    "number": 83,
    "title": "Matrix Rotation (Variant 9)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 9)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      82,
      84,
      78
    ],
    "prerequisites": [
      81
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 84,
    "number": 84,
    "title": "Spiral Matrix (Variant 9)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 9)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      83,
      85,
      79
    ],
    "prerequisites": [
      82
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 85,
    "number": 85,
    "title": "Pascal Triangle (Variant 9)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 9)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      84,
      86,
      80
    ],
    "prerequisites": [
      83
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 86,
    "number": 86,
    "title": "Find All Duplicates (Variant 9)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 9)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      85,
      87,
      81
    ],
    "prerequisites": [
      84
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 87,
    "number": 87,
    "title": "Sort Colors (Variant 9)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 9)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      86,
      88,
      82
    ],
    "prerequisites": [
      85
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 88,
    "number": 88,
    "title": "Peak Element (Variant 9)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Peak Element (Variant 9)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      87,
      89,
      83
    ],
    "prerequisites": [
      86
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 89,
    "number": 89,
    "title": "Jump Game (Variant 9)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Jump Game (Variant 9)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      88,
      90,
      84
    ],
    "prerequisites": [
      87
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 90,
    "number": 90,
    "title": "Gas Station (Variant 9)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Gas Station (Variant 9)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      89,
      91,
      85
    ],
    "prerequisites": [
      88
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 91,
    "number": 91,
    "title": "Rotate Array (Variant 10)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 10)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      90,
      92,
      86
    ],
    "prerequisites": [
      89
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 92,
    "number": 92,
    "title": "Subarray Threshold (Variant 10)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 10)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      91,
      93,
      87
    ],
    "prerequisites": [
      90
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 93,
    "number": 93,
    "title": "Matrix Rotation (Variant 10)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 10)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      92,
      94,
      88
    ],
    "prerequisites": [
      91
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 94,
    "number": 94,
    "title": "Spiral Matrix (Variant 10)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 10)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      93,
      95,
      89
    ],
    "prerequisites": [
      92
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 95,
    "number": 95,
    "title": "Pascal Triangle (Variant 10)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 10)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      94,
      96,
      90
    ],
    "prerequisites": [
      93
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 96,
    "number": 96,
    "title": "Find All Duplicates (Variant 10)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 10)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      95,
      97,
      91
    ],
    "prerequisites": [
      94
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 97,
    "number": 97,
    "title": "Sort Colors (Variant 10)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 10)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      96,
      98,
      92
    ],
    "prerequisites": [
      95
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 98,
    "number": 98,
    "title": "Peak Element (Variant 10)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Peak Element (Variant 10)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      97,
      99,
      93
    ],
    "prerequisites": [
      96
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 99,
    "number": 99,
    "title": "Jump Game (Variant 10)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Jump Game (Variant 10)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      98,
      100,
      94
    ],
    "prerequisites": [
      97
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 100,
    "number": 100,
    "title": "Gas Station (Variant 10)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Gas Station (Variant 10)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      99,
      101,
      95
    ],
    "prerequisites": [
      98
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 101,
    "number": 101,
    "title": "Rotate Array (Variant 11)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 11)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      100,
      102,
      96
    ],
    "prerequisites": [
      99
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 102,
    "number": 102,
    "title": "Subarray Threshold (Variant 11)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 11)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      101,
      103,
      97
    ],
    "prerequisites": [
      100
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 103,
    "number": 103,
    "title": "Matrix Rotation (Variant 11)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 11)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      102,
      104,
      98
    ],
    "prerequisites": [
      101
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 104,
    "number": 104,
    "title": "Spiral Matrix (Variant 11)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 11)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      103,
      105,
      99
    ],
    "prerequisites": [
      102
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 105,
    "number": 105,
    "title": "Pascal Triangle (Variant 11)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 11)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      104,
      106,
      100
    ],
    "prerequisites": [
      103
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 106,
    "number": 106,
    "title": "Find All Duplicates (Variant 11)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 11)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      105,
      107,
      101
    ],
    "prerequisites": [
      104
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 107,
    "number": 107,
    "title": "Sort Colors (Variant 11)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 11)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      106,
      108,
      102
    ],
    "prerequisites": [
      105
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 108,
    "number": 108,
    "title": "Peak Element (Variant 11)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Peak Element (Variant 11)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      107,
      109,
      103
    ],
    "prerequisites": [
      106
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 109,
    "number": 109,
    "title": "Jump Game (Variant 11)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Jump Game (Variant 11)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      108,
      110,
      104
    ],
    "prerequisites": [
      107
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 110,
    "number": 110,
    "title": "Gas Station (Variant 11)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Gas Station (Variant 11)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      109,
      111,
      105
    ],
    "prerequisites": [
      108
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 111,
    "number": 111,
    "title": "Rotate Array (Variant 12)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 12)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      110,
      112,
      106
    ],
    "prerequisites": [
      109
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 112,
    "number": 112,
    "title": "Subarray Threshold (Variant 12)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 12)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      111,
      113,
      107
    ],
    "prerequisites": [
      110
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 113,
    "number": 113,
    "title": "Matrix Rotation (Variant 12)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 12)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      112,
      114,
      108
    ],
    "prerequisites": [
      111
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 114,
    "number": 114,
    "title": "Spiral Matrix (Variant 12)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 12)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      113,
      115,
      109
    ],
    "prerequisites": [
      112
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 115,
    "number": 115,
    "title": "Pascal Triangle (Variant 12)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 12)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      114,
      116,
      110
    ],
    "prerequisites": [
      113
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 116,
    "number": 116,
    "title": "Find All Duplicates (Variant 12)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 12)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      115,
      117,
      111
    ],
    "prerequisites": [
      114
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 117,
    "number": 117,
    "title": "Sort Colors (Variant 12)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 12)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      116,
      118,
      112
    ],
    "prerequisites": [
      115
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 118,
    "number": 118,
    "title": "Peak Element (Variant 12)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Peak Element (Variant 12)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      117,
      119,
      113
    ],
    "prerequisites": [
      116
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 119,
    "number": 119,
    "title": "Jump Game (Variant 12)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Jump Game (Variant 12)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      118,
      120,
      114
    ],
    "prerequisites": [
      117
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 120,
    "number": 120,
    "title": "Gas Station (Variant 12)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Gas Station (Variant 12)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      119,
      121,
      115
    ],
    "prerequisites": [
      118
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 121,
    "number": 121,
    "title": "Rotate Array (Variant 13)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 13)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      120,
      122,
      116
    ],
    "prerequisites": [
      119
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 122,
    "number": 122,
    "title": "Subarray Threshold (Variant 13)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 13)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      121,
      123,
      117
    ],
    "prerequisites": [
      120
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 123,
    "number": 123,
    "title": "Matrix Rotation (Variant 13)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 13)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      122,
      124,
      118
    ],
    "prerequisites": [
      121
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 124,
    "number": 124,
    "title": "Spiral Matrix (Variant 13)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 13)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      123,
      125,
      119
    ],
    "prerequisites": [
      122
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 125,
    "number": 125,
    "title": "Pascal Triangle (Variant 13)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 13)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      124,
      126,
      120
    ],
    "prerequisites": [
      123
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 126,
    "number": 126,
    "title": "Find All Duplicates (Variant 13)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 13)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      125,
      127,
      121
    ],
    "prerequisites": [
      124
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 127,
    "number": 127,
    "title": "Sort Colors (Variant 13)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 13)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      126,
      128,
      122
    ],
    "prerequisites": [
      125
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 128,
    "number": 128,
    "title": "Peak Element (Variant 13)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Peak Element (Variant 13)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      127,
      129,
      123
    ],
    "prerequisites": [
      126
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 129,
    "number": 129,
    "title": "Jump Game (Variant 13)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Jump Game (Variant 13)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      128,
      130,
      124
    ],
    "prerequisites": [
      127
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 130,
    "number": 130,
    "title": "Gas Station (Variant 13)",
    "difficulty": "Medium",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Gas Station (Variant 13)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      129,
      131,
      125
    ],
    "prerequisites": [
      128
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 131,
    "number": 131,
    "title": "Rotate Array (Variant 14)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 14)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      130,
      132,
      126
    ],
    "prerequisites": [
      129
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 132,
    "number": 132,
    "title": "Subarray Threshold (Variant 14)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 14)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      131,
      133,
      127
    ],
    "prerequisites": [
      130
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 133,
    "number": 133,
    "title": "Matrix Rotation (Variant 14)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 14)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      132,
      134,
      128
    ],
    "prerequisites": [
      131
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 134,
    "number": 134,
    "title": "Spiral Matrix (Variant 14)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 14)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      133,
      135,
      129
    ],
    "prerequisites": [
      132
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 135,
    "number": 135,
    "title": "Pascal Triangle (Variant 14)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 14)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      134,
      136,
      130
    ],
    "prerequisites": [
      133
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 136,
    "number": 136,
    "title": "Find All Duplicates (Variant 14)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 14)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      135,
      137,
      131
    ],
    "prerequisites": [
      134
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 137,
    "number": 137,
    "title": "Sort Colors (Variant 14)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 14)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      136,
      138,
      132
    ],
    "prerequisites": [
      135
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 138,
    "number": 138,
    "title": "Peak Element (Variant 14)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Peak Element (Variant 14)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      137,
      139,
      133
    ],
    "prerequisites": [
      136
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 139,
    "number": 139,
    "title": "Jump Game (Variant 14)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Jump Game (Variant 14)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      138,
      140,
      134
    ],
    "prerequisites": [
      137
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 140,
    "number": 140,
    "title": "Gas Station (Variant 14)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Gas Station (Variant 14)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      139,
      141,
      135
    ],
    "prerequisites": [
      138
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 141,
    "number": 141,
    "title": "Rotate Array (Variant 15)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Rotate Array (Variant 15)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      140,
      142,
      136
    ],
    "prerequisites": [
      139
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 142,
    "number": 142,
    "title": "Subarray Threshold (Variant 15)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Subarray Threshold (Variant 15)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      141,
      143,
      137
    ],
    "prerequisites": [
      140
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 143,
    "number": 143,
    "title": "Matrix Rotation (Variant 15)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Matrix Rotation (Variant 15)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      142,
      144,
      138
    ],
    "prerequisites": [
      141
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 144,
    "number": 144,
    "title": "Spiral Matrix (Variant 15)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Spiral Matrix (Variant 15)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      143,
      145,
      139
    ],
    "prerequisites": [
      142
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 145,
    "number": 145,
    "title": "Pascal Triangle (Variant 15)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Pascal Triangle (Variant 15)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      144,
      146,
      140
    ],
    "prerequisites": [
      143
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 146,
    "number": 146,
    "title": "Find All Duplicates (Variant 15)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Find All Duplicates (Variant 15)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      145,
      147,
      141
    ],
    "prerequisites": [
      144
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 147,
    "number": 147,
    "title": "Sort Colors (Variant 15)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Sort Colors (Variant 15)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      146,
      148,
      142
    ],
    "prerequisites": [
      145
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 148,
    "number": 148,
    "title": "Peak Element (Variant 15)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Peak Element (Variant 15)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      147,
      149,
      143
    ],
    "prerequisites": [
      146
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 149,
    "number": 149,
    "title": "Jump Game (Variant 15)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Jump Game (Variant 15)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      148,
      150,
      144
    ],
    "prerequisites": [
      147
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 150,
    "number": 150,
    "title": "Gas Station (Variant 15)",
    "difficulty": "Hard",
    "topic": "Arrays & Strings",
    "subtopic": "Prefix Sum / Array Iteration Core Mechanics",
    "phase": "Phase 1 \u2014 Arrays & Strings",
    "pattern": "Prefix Sum / Array Iteration",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Gas Station (Variant 15)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Sum / Array Iteration. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Sum / Array Iteration technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Sum / Array Iteration)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Sum / Array Iteration)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Sum / Array Iteration)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Sum / Array Iteration)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      149,
      151,
      145
    ],
    "prerequisites": [
      148
    ],
    "tags": [
      "Arrays & Strings",
      "Prefix Sum / Array Iteration",
      "Phase 1",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Sum / Array Iteration because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Sum / Array Iteration leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Sum / Array Iteration optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 151,
    "number": 151,
    "title": "Subarray Sum Divisible (Variant 16)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarray Sum Divisible (Variant 16)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      150,
      152,
      146
    ],
    "prerequisites": [
      149
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 152,
    "number": 152,
    "title": "Longest Consecutive Sequence (Variant 16)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Longest Consecutive Sequence (Variant 16)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      151,
      153,
      147
    ],
    "prerequisites": [
      150
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 153,
    "number": 153,
    "title": "Isomorphic Strings (Variant 16)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Isomorphic Strings (Variant 16)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      152,
      154,
      148
    ],
    "prerequisites": [
      151
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 154,
    "number": 154,
    "title": "Word Pattern (Variant 16)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Word Pattern (Variant 16)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      153,
      155,
      149
    ],
    "prerequisites": [
      152
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 155,
    "number": 155,
    "title": "4Sum (Variant 16)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **4Sum (Variant 16)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      154,
      156,
      150
    ],
    "prerequisites": [
      153
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 156,
    "number": 156,
    "title": "Ransom Note (Variant 16)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Ransom Note (Variant 16)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      155,
      157,
      151
    ],
    "prerequisites": [
      154
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 157,
    "number": 157,
    "title": "Top K Frequent Words (Variant 16)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Top K Frequent Words (Variant 16)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      156,
      158,
      152
    ],
    "prerequisites": [
      155
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 158,
    "number": 158,
    "title": "Subdomain Visit Count (Variant 16)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subdomain Visit Count (Variant 16)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      157,
      159,
      153
    ],
    "prerequisites": [
      156
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 159,
    "number": 159,
    "title": "Design HashMap (Variant 16)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Design HashMap (Variant 16)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      158,
      160,
      154
    ],
    "prerequisites": [
      157
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 160,
    "number": 160,
    "title": "Custom Sort String (Variant 16)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Custom Sort String (Variant 16)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      159,
      161,
      155
    ],
    "prerequisites": [
      158
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 161,
    "number": 161,
    "title": "Subarray Sum Divisible (Variant 17)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarray Sum Divisible (Variant 17)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      160,
      162,
      156
    ],
    "prerequisites": [
      159
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 162,
    "number": 162,
    "title": "Longest Consecutive Sequence (Variant 17)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Longest Consecutive Sequence (Variant 17)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      161,
      163,
      157
    ],
    "prerequisites": [
      160
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 163,
    "number": 163,
    "title": "Isomorphic Strings (Variant 17)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Isomorphic Strings (Variant 17)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      162,
      164,
      158
    ],
    "prerequisites": [
      161
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 164,
    "number": 164,
    "title": "Word Pattern (Variant 17)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Word Pattern (Variant 17)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      163,
      165,
      159
    ],
    "prerequisites": [
      162
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 165,
    "number": 165,
    "title": "4Sum (Variant 17)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **4Sum (Variant 17)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      164,
      166,
      160
    ],
    "prerequisites": [
      163
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 166,
    "number": 166,
    "title": "Ransom Note (Variant 17)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Ransom Note (Variant 17)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      165,
      167,
      161
    ],
    "prerequisites": [
      164
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 167,
    "number": 167,
    "title": "Top K Frequent Words (Variant 17)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Top K Frequent Words (Variant 17)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      166,
      168,
      162
    ],
    "prerequisites": [
      165
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 168,
    "number": 168,
    "title": "Subdomain Visit Count (Variant 17)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subdomain Visit Count (Variant 17)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      167,
      169,
      163
    ],
    "prerequisites": [
      166
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 169,
    "number": 169,
    "title": "Design HashMap (Variant 17)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Design HashMap (Variant 17)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      168,
      170,
      164
    ],
    "prerequisites": [
      167
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 170,
    "number": 170,
    "title": "Custom Sort String (Variant 17)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Custom Sort String (Variant 17)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      169,
      171,
      165
    ],
    "prerequisites": [
      168
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 171,
    "number": 171,
    "title": "Subarray Sum Divisible (Variant 18)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarray Sum Divisible (Variant 18)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      170,
      172,
      166
    ],
    "prerequisites": [
      169
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 172,
    "number": 172,
    "title": "Longest Consecutive Sequence (Variant 18)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Longest Consecutive Sequence (Variant 18)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      171,
      173,
      167
    ],
    "prerequisites": [
      170
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 173,
    "number": 173,
    "title": "Isomorphic Strings (Variant 18)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Isomorphic Strings (Variant 18)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      172,
      174,
      168
    ],
    "prerequisites": [
      171
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 174,
    "number": 174,
    "title": "Word Pattern (Variant 18)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Word Pattern (Variant 18)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      173,
      175,
      169
    ],
    "prerequisites": [
      172
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 175,
    "number": 175,
    "title": "4Sum (Variant 18)",
    "difficulty": "Easy",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **4Sum (Variant 18)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      174,
      176,
      170
    ],
    "prerequisites": [
      173
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 176,
    "number": 176,
    "title": "Ransom Note (Variant 18)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Ransom Note (Variant 18)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      175,
      177,
      171
    ],
    "prerequisites": [
      174
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 177,
    "number": 177,
    "title": "Top K Frequent Words (Variant 18)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Top K Frequent Words (Variant 18)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      176,
      178,
      172
    ],
    "prerequisites": [
      175
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 178,
    "number": 178,
    "title": "Subdomain Visit Count (Variant 18)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subdomain Visit Count (Variant 18)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      177,
      179,
      173
    ],
    "prerequisites": [
      176
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 179,
    "number": 179,
    "title": "Design HashMap (Variant 18)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Design HashMap (Variant 18)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      178,
      180,
      174
    ],
    "prerequisites": [
      177
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 180,
    "number": 180,
    "title": "Custom Sort String (Variant 18)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Custom Sort String (Variant 18)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      179,
      181,
      175
    ],
    "prerequisites": [
      178
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 181,
    "number": 181,
    "title": "Subarray Sum Divisible (Variant 19)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Sum Divisible (Variant 19)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      180,
      182,
      176
    ],
    "prerequisites": [
      179
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 182,
    "number": 182,
    "title": "Longest Consecutive Sequence (Variant 19)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Longest Consecutive Sequence (Variant 19)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      181,
      183,
      177
    ],
    "prerequisites": [
      180
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 183,
    "number": 183,
    "title": "Isomorphic Strings (Variant 19)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Isomorphic Strings (Variant 19)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      182,
      184,
      178
    ],
    "prerequisites": [
      181
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 184,
    "number": 184,
    "title": "Word Pattern (Variant 19)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Word Pattern (Variant 19)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      183,
      185,
      179
    ],
    "prerequisites": [
      182
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 185,
    "number": 185,
    "title": "4Sum (Variant 19)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **4Sum (Variant 19)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      184,
      186,
      180
    ],
    "prerequisites": [
      183
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 186,
    "number": 186,
    "title": "Ransom Note (Variant 19)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Ransom Note (Variant 19)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      185,
      187,
      181
    ],
    "prerequisites": [
      184
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 187,
    "number": 187,
    "title": "Top K Frequent Words (Variant 19)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Top K Frequent Words (Variant 19)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      186,
      188,
      182
    ],
    "prerequisites": [
      185
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 188,
    "number": 188,
    "title": "Subdomain Visit Count (Variant 19)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subdomain Visit Count (Variant 19)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      187,
      189,
      183
    ],
    "prerequisites": [
      186
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 189,
    "number": 189,
    "title": "Design HashMap (Variant 19)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Design HashMap (Variant 19)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      188,
      190,
      184
    ],
    "prerequisites": [
      187
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 190,
    "number": 190,
    "title": "Custom Sort String (Variant 19)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Custom Sort String (Variant 19)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      189,
      191,
      185
    ],
    "prerequisites": [
      188
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 191,
    "number": 191,
    "title": "Subarray Sum Divisible (Variant 20)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Sum Divisible (Variant 20)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      190,
      192,
      186
    ],
    "prerequisites": [
      189
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 192,
    "number": 192,
    "title": "Longest Consecutive Sequence (Variant 20)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Longest Consecutive Sequence (Variant 20)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      191,
      193,
      187
    ],
    "prerequisites": [
      190
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 193,
    "number": 193,
    "title": "Isomorphic Strings (Variant 20)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Isomorphic Strings (Variant 20)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      192,
      194,
      188
    ],
    "prerequisites": [
      191
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 194,
    "number": 194,
    "title": "Word Pattern (Variant 20)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Word Pattern (Variant 20)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      193,
      195,
      189
    ],
    "prerequisites": [
      192
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 195,
    "number": 195,
    "title": "4Sum (Variant 20)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **4Sum (Variant 20)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      194,
      196,
      190
    ],
    "prerequisites": [
      193
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 196,
    "number": 196,
    "title": "Ransom Note (Variant 20)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Ransom Note (Variant 20)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      195,
      197,
      191
    ],
    "prerequisites": [
      194
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 197,
    "number": 197,
    "title": "Top K Frequent Words (Variant 20)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Top K Frequent Words (Variant 20)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      196,
      198,
      192
    ],
    "prerequisites": [
      195
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 198,
    "number": 198,
    "title": "Subdomain Visit Count (Variant 20)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subdomain Visit Count (Variant 20)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      197,
      199,
      193
    ],
    "prerequisites": [
      196
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 199,
    "number": 199,
    "title": "Design HashMap (Variant 20)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Design HashMap (Variant 20)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      198,
      200,
      194
    ],
    "prerequisites": [
      197
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 200,
    "number": 200,
    "title": "Custom Sort String (Variant 20)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Custom Sort String (Variant 20)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      199,
      201,
      195
    ],
    "prerequisites": [
      198
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 201,
    "number": 201,
    "title": "Subarray Sum Divisible (Variant 21)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Sum Divisible (Variant 21)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      200,
      202,
      196
    ],
    "prerequisites": [
      199
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 202,
    "number": 202,
    "title": "Longest Consecutive Sequence (Variant 21)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Longest Consecutive Sequence (Variant 21)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      201,
      203,
      197
    ],
    "prerequisites": [
      200
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 203,
    "number": 203,
    "title": "Isomorphic Strings (Variant 21)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Isomorphic Strings (Variant 21)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      202,
      204,
      198
    ],
    "prerequisites": [
      201
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 204,
    "number": 204,
    "title": "Word Pattern (Variant 21)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Word Pattern (Variant 21)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      203,
      205,
      199
    ],
    "prerequisites": [
      202
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 205,
    "number": 205,
    "title": "4Sum (Variant 21)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **4Sum (Variant 21)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      204,
      206,
      200
    ],
    "prerequisites": [
      203
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 206,
    "number": 206,
    "title": "Ransom Note (Variant 21)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Ransom Note (Variant 21)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      205,
      207,
      201
    ],
    "prerequisites": [
      204
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 207,
    "number": 207,
    "title": "Top K Frequent Words (Variant 21)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Top K Frequent Words (Variant 21)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      206,
      208,
      202
    ],
    "prerequisites": [
      205
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 208,
    "number": 208,
    "title": "Subdomain Visit Count (Variant 21)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subdomain Visit Count (Variant 21)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      207,
      209,
      203
    ],
    "prerequisites": [
      206
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 209,
    "number": 209,
    "title": "Design HashMap (Variant 21)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Design HashMap (Variant 21)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      208,
      210,
      204
    ],
    "prerequisites": [
      207
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 210,
    "number": 210,
    "title": "Custom Sort String (Variant 21)",
    "difficulty": "Medium",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Custom Sort String (Variant 21)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      209,
      211,
      205
    ],
    "prerequisites": [
      208
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 100000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 211,
    "number": 211,
    "title": "Subarray Sum Divisible (Variant 22)",
    "difficulty": "Hard",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Subarray Sum Divisible (Variant 22)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      210,
      212,
      206
    ],
    "prerequisites": [
      209
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 1000000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 212,
    "number": 212,
    "title": "Longest Consecutive Sequence (Variant 22)",
    "difficulty": "Hard",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Longest Consecutive Sequence (Variant 22)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      211,
      213,
      207
    ],
    "prerequisites": [
      210
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 1000000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 213,
    "number": 213,
    "title": "Isomorphic Strings (Variant 22)",
    "difficulty": "Hard",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Isomorphic Strings (Variant 22)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      212,
      214,
      208
    ],
    "prerequisites": [
      211
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 1000000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 214,
    "number": 214,
    "title": "Word Pattern (Variant 22)",
    "difficulty": "Hard",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Word Pattern (Variant 22)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      213,
      215,
      209
    ],
    "prerequisites": [
      212
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 1000000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 215,
    "number": 215,
    "title": "4Sum (Variant 22)",
    "difficulty": "Hard",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **4Sum (Variant 22)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      214,
      216,
      210
    ],
    "prerequisites": [
      213
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 1000000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 216,
    "number": 216,
    "title": "Ransom Note (Variant 22)",
    "difficulty": "Hard",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Ransom Note (Variant 22)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      215,
      217,
      211
    ],
    "prerequisites": [
      214
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 1000000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 217,
    "number": 217,
    "title": "Top K Frequent Words (Variant 22)",
    "difficulty": "Hard",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Top K Frequent Words (Variant 22)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      216,
      218,
      212
    ],
    "prerequisites": [
      215
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 1000000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 218,
    "number": 218,
    "title": "Subdomain Visit Count (Variant 22)",
    "difficulty": "Hard",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Subdomain Visit Count (Variant 22)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      217,
      219,
      213
    ],
    "prerequisites": [
      216
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 1000000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 219,
    "number": 219,
    "title": "Design HashMap (Variant 22)",
    "difficulty": "Hard",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Design HashMap (Variant 22)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      218,
      220,
      214
    ],
    "prerequisites": [
      217
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 1000000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 220,
    "number": 220,
    "title": "Custom Sort String (Variant 22)",
    "difficulty": "Hard",
    "topic": "Hashing",
    "subtopic": "Frequency Counter / Hash Map Core Mechanics",
    "phase": "Phase 2 \u2014 Hashing",
    "pattern": "Frequency Counter / Hash Map",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Custom Sort String (Variant 22)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Frequency Counter / Hash Map. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Frequency Counter / Hash Map technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Frequency Counter / Hash Map)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Frequency Counter / Hash Map)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Frequency Counter / Hash Map)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Frequency Counter / Hash Map)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      219,
      221,
      215
    ],
    "prerequisites": [
      218
    ],
    "tags": [
      "Hashing",
      "Frequency Counter / Hash Map",
      "Phase 2",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Frequency Counter / Hash Map because constraints are N <= 1000000, making O(N^2) brute force infeasible. Frequency Counter / Hash Map leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Frequency Counter / Hash Map optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 221,
    "number": 221,
    "title": "Squares of Sorted Array (Variant 23)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Squares of Sorted Array (Variant 23)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      220,
      222,
      216
    ],
    "prerequisites": [
      219
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 222,
    "number": 222,
    "title": "3Sum Closest (Variant 23)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **3Sum Closest (Variant 23)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      221,
      223,
      217
    ],
    "prerequisites": [
      220
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 223,
    "number": 223,
    "title": "Sort Array By Parity (Variant 23)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort Array By Parity (Variant 23)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      222,
      224,
      218
    ],
    "prerequisites": [
      221
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 224,
    "number": 224,
    "title": "4Sum II (Variant 23)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **4Sum II (Variant 23)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      223,
      225,
      219
    ],
    "prerequisites": [
      222
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 225,
    "number": 225,
    "title": "Boats to Save People (Variant 23)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Boats to Save People (Variant 23)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      224,
      226,
      220
    ],
    "prerequisites": [
      223
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 226,
    "number": 226,
    "title": "Assign Cookies (Variant 23)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Assign Cookies (Variant 23)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      225,
      227,
      221
    ],
    "prerequisites": [
      224
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 227,
    "number": 227,
    "title": "Container Placement (Variant 23)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Container Placement (Variant 23)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      226,
      228,
      222
    ],
    "prerequisites": [
      225
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 228,
    "number": 228,
    "title": "Valid Palindrome II (Variant 23)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Valid Palindrome II (Variant 23)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      227,
      229,
      223
    ],
    "prerequisites": [
      226
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 229,
    "number": 229,
    "title": "Trapping Rain Water II (Variant 23)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Trapping Rain Water II (Variant 23)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      228,
      230,
      224
    ],
    "prerequisites": [
      227
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 230,
    "number": 230,
    "title": "Interval Intersection (Variant 23)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Interval Intersection (Variant 23)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      229,
      231,
      225
    ],
    "prerequisites": [
      228
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 231,
    "number": 231,
    "title": "Squares of Sorted Array (Variant 24)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Squares of Sorted Array (Variant 24)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      230,
      232,
      226
    ],
    "prerequisites": [
      229
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 232,
    "number": 232,
    "title": "3Sum Closest (Variant 24)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **3Sum Closest (Variant 24)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      231,
      233,
      227
    ],
    "prerequisites": [
      230
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 233,
    "number": 233,
    "title": "Sort Array By Parity (Variant 24)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort Array By Parity (Variant 24)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      232,
      234,
      228
    ],
    "prerequisites": [
      231
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 234,
    "number": 234,
    "title": "4Sum II (Variant 24)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **4Sum II (Variant 24)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      233,
      235,
      229
    ],
    "prerequisites": [
      232
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 235,
    "number": 235,
    "title": "Boats to Save People (Variant 24)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Boats to Save People (Variant 24)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      234,
      236,
      230
    ],
    "prerequisites": [
      233
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 236,
    "number": 236,
    "title": "Assign Cookies (Variant 24)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Assign Cookies (Variant 24)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      235,
      237,
      231
    ],
    "prerequisites": [
      234
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 237,
    "number": 237,
    "title": "Container Placement (Variant 24)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Container Placement (Variant 24)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      236,
      238,
      232
    ],
    "prerequisites": [
      235
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 238,
    "number": 238,
    "title": "Valid Palindrome II (Variant 24)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Valid Palindrome II (Variant 24)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      237,
      239,
      233
    ],
    "prerequisites": [
      236
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 239,
    "number": 239,
    "title": "Trapping Rain Water II (Variant 24)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Trapping Rain Water II (Variant 24)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      238,
      240,
      234
    ],
    "prerequisites": [
      237
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 240,
    "number": 240,
    "title": "Interval Intersection (Variant 24)",
    "difficulty": "Easy",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Interval Intersection (Variant 24)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      239,
      241,
      235
    ],
    "prerequisites": [
      238
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 241,
    "number": 241,
    "title": "Squares of Sorted Array (Variant 25)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Squares of Sorted Array (Variant 25)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      240,
      242,
      236
    ],
    "prerequisites": [
      239
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 242,
    "number": 242,
    "title": "3Sum Closest (Variant 25)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **3Sum Closest (Variant 25)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      241,
      243,
      237
    ],
    "prerequisites": [
      240
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 243,
    "number": 243,
    "title": "Sort Array By Parity (Variant 25)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Array By Parity (Variant 25)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      242,
      244,
      238
    ],
    "prerequisites": [
      241
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 244,
    "number": 244,
    "title": "4Sum II (Variant 25)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **4Sum II (Variant 25)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      243,
      245,
      239
    ],
    "prerequisites": [
      242
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 245,
    "number": 245,
    "title": "Boats to Save People (Variant 25)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Boats to Save People (Variant 25)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      244,
      246,
      240
    ],
    "prerequisites": [
      243
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 246,
    "number": 246,
    "title": "Assign Cookies (Variant 25)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Assign Cookies (Variant 25)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      245,
      247,
      241
    ],
    "prerequisites": [
      244
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 247,
    "number": 247,
    "title": "Container Placement (Variant 25)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Container Placement (Variant 25)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      246,
      248,
      242
    ],
    "prerequisites": [
      245
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 248,
    "number": 248,
    "title": "Valid Palindrome II (Variant 25)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Valid Palindrome II (Variant 25)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      247,
      249,
      243
    ],
    "prerequisites": [
      246
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 249,
    "number": 249,
    "title": "Trapping Rain Water II (Variant 25)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Trapping Rain Water II (Variant 25)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      248,
      250,
      244
    ],
    "prerequisites": [
      247
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 250,
    "number": 250,
    "title": "Interval Intersection (Variant 25)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Interval Intersection (Variant 25)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      249,
      251,
      245
    ],
    "prerequisites": [
      248
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 251,
    "number": 251,
    "title": "Squares of Sorted Array (Variant 26)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Squares of Sorted Array (Variant 26)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      250,
      252,
      246
    ],
    "prerequisites": [
      249
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 252,
    "number": 252,
    "title": "3Sum Closest (Variant 26)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **3Sum Closest (Variant 26)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      251,
      253,
      247
    ],
    "prerequisites": [
      250
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 253,
    "number": 253,
    "title": "Sort Array By Parity (Variant 26)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Array By Parity (Variant 26)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      252,
      254,
      248
    ],
    "prerequisites": [
      251
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 254,
    "number": 254,
    "title": "4Sum II (Variant 26)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **4Sum II (Variant 26)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      253,
      255,
      249
    ],
    "prerequisites": [
      252
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 255,
    "number": 255,
    "title": "Boats to Save People (Variant 26)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Boats to Save People (Variant 26)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      254,
      256,
      250
    ],
    "prerequisites": [
      253
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 256,
    "number": 256,
    "title": "Assign Cookies (Variant 26)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Assign Cookies (Variant 26)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      255,
      257,
      251
    ],
    "prerequisites": [
      254
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 257,
    "number": 257,
    "title": "Container Placement (Variant 26)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Container Placement (Variant 26)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      256,
      258,
      252
    ],
    "prerequisites": [
      255
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 258,
    "number": 258,
    "title": "Valid Palindrome II (Variant 26)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Valid Palindrome II (Variant 26)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      257,
      259,
      253
    ],
    "prerequisites": [
      256
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 259,
    "number": 259,
    "title": "Trapping Rain Water II (Variant 26)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Trapping Rain Water II (Variant 26)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      258,
      260,
      254
    ],
    "prerequisites": [
      257
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 260,
    "number": 260,
    "title": "Interval Intersection (Variant 26)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Interval Intersection (Variant 26)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      259,
      261,
      255
    ],
    "prerequisites": [
      258
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 261,
    "number": 261,
    "title": "Squares of Sorted Array (Variant 27)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Squares of Sorted Array (Variant 27)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      260,
      262,
      256
    ],
    "prerequisites": [
      259
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 262,
    "number": 262,
    "title": "3Sum Closest (Variant 27)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **3Sum Closest (Variant 27)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      261,
      263,
      257
    ],
    "prerequisites": [
      260
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 263,
    "number": 263,
    "title": "Sort Array By Parity (Variant 27)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Array By Parity (Variant 27)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      262,
      264,
      258
    ],
    "prerequisites": [
      261
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 264,
    "number": 264,
    "title": "4Sum II (Variant 27)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **4Sum II (Variant 27)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      263,
      265,
      259
    ],
    "prerequisites": [
      262
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 265,
    "number": 265,
    "title": "Boats to Save People (Variant 27)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Boats to Save People (Variant 27)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      264,
      266,
      260
    ],
    "prerequisites": [
      263
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 266,
    "number": 266,
    "title": "Assign Cookies (Variant 27)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Assign Cookies (Variant 27)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      265,
      267,
      261
    ],
    "prerequisites": [
      264
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 267,
    "number": 267,
    "title": "Container Placement (Variant 27)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Container Placement (Variant 27)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      266,
      268,
      262
    ],
    "prerequisites": [
      265
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 268,
    "number": 268,
    "title": "Valid Palindrome II (Variant 27)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Valid Palindrome II (Variant 27)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      267,
      269,
      263
    ],
    "prerequisites": [
      266
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 269,
    "number": 269,
    "title": "Trapping Rain Water II (Variant 27)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Trapping Rain Water II (Variant 27)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      268,
      270,
      264
    ],
    "prerequisites": [
      267
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 270,
    "number": 270,
    "title": "Interval Intersection (Variant 27)",
    "difficulty": "Medium",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Interval Intersection (Variant 27)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      269,
      271,
      265
    ],
    "prerequisites": [
      268
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 100000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 271,
    "number": 271,
    "title": "Squares of Sorted Array (Variant 28)",
    "difficulty": "Hard",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Squares of Sorted Array (Variant 28)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      270,
      272,
      266
    ],
    "prerequisites": [
      269
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 1000000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 272,
    "number": 272,
    "title": "3Sum Closest (Variant 28)",
    "difficulty": "Hard",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **3Sum Closest (Variant 28)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      271,
      273,
      267
    ],
    "prerequisites": [
      270
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 1000000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 273,
    "number": 273,
    "title": "Sort Array By Parity (Variant 28)",
    "difficulty": "Hard",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Sort Array By Parity (Variant 28)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      272,
      274,
      268
    ],
    "prerequisites": [
      271
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 1000000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 274,
    "number": 274,
    "title": "4Sum II (Variant 28)",
    "difficulty": "Hard",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **4Sum II (Variant 28)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      273,
      275,
      269
    ],
    "prerequisites": [
      272
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 1000000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 275,
    "number": 275,
    "title": "Boats to Save People (Variant 28)",
    "difficulty": "Hard",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Boats to Save People (Variant 28)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      274,
      276,
      270
    ],
    "prerequisites": [
      273
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 1000000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 276,
    "number": 276,
    "title": "Assign Cookies (Variant 28)",
    "difficulty": "Hard",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Assign Cookies (Variant 28)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      275,
      277,
      271
    ],
    "prerequisites": [
      274
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 1000000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 277,
    "number": 277,
    "title": "Container Placement (Variant 28)",
    "difficulty": "Hard",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Container Placement (Variant 28)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      276,
      278,
      272
    ],
    "prerequisites": [
      275
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 1000000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 278,
    "number": 278,
    "title": "Valid Palindrome II (Variant 28)",
    "difficulty": "Hard",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Valid Palindrome II (Variant 28)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      277,
      279,
      273
    ],
    "prerequisites": [
      276
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 1000000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 279,
    "number": 279,
    "title": "Trapping Rain Water II (Variant 28)",
    "difficulty": "Hard",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Trapping Rain Water II (Variant 28)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      278,
      280,
      274
    ],
    "prerequisites": [
      277
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 1000000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 280,
    "number": 280,
    "title": "Interval Intersection (Variant 28)",
    "difficulty": "Hard",
    "topic": "Two Pointers",
    "subtopic": "Two Pointers Core Mechanics",
    "phase": "Phase 3 \u2014 Two Pointers",
    "pattern": "Two Pointers",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Interval Intersection (Variant 28)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Two Pointers. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Two Pointers technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Two Pointers)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Two Pointers)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Two Pointers)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Two Pointers)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      279,
      281,
      275
    ],
    "prerequisites": [
      278
    ],
    "tags": [
      "Two Pointers",
      "Two Pointers",
      "Phase 3",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Two Pointers because constraints are N <= 1000000, making O(N^2) brute force infeasible. Two Pointers leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Two Pointers optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 281,
    "number": 281,
    "title": "Max Consecutive Ones III (Variant 29)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Max Consecutive Ones III (Variant 29)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      280,
      282,
      276
    ],
    "prerequisites": [
      279
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 282,
    "number": 282,
    "title": "Subarrays with K Distinct (Variant 29)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarrays with K Distinct (Variant 29)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      281,
      283,
      277
    ],
    "prerequisites": [
      280
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 283,
    "number": 283,
    "title": "Permutation in String (Variant 29)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Permutation in String (Variant 29)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      282,
      284,
      278
    ],
    "prerequisites": [
      281
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 284,
    "number": 284,
    "title": "Longest Repeating Character Replacement (Variant 29)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Longest Repeating Character Replacement (Variant 29)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      283,
      285,
      279
    ],
    "prerequisites": [
      282
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 285,
    "number": 285,
    "title": "Fruit Into Baskets (Variant 29)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Fruit Into Baskets (Variant 29)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      284,
      286,
      280
    ],
    "prerequisites": [
      283
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 286,
    "number": 286,
    "title": "Minimum Size Subarray Sum (Variant 29)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Minimum Size Subarray Sum (Variant 29)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      285,
      287,
      281
    ],
    "prerequisites": [
      284
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 287,
    "number": 287,
    "title": "Count Subarrays With Fixed Bounds (Variant 29)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Count Subarrays With Fixed Bounds (Variant 29)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      286,
      288,
      282
    ],
    "prerequisites": [
      285
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 288,
    "number": 288,
    "title": "Grumpy Bookstore Owner (Variant 29)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Grumpy Bookstore Owner (Variant 29)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      287,
      289,
      283
    ],
    "prerequisites": [
      286
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 289,
    "number": 289,
    "title": "Subarray Product Less Than K (Variant 29)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarray Product Less Than K (Variant 29)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      288,
      290,
      284
    ],
    "prerequisites": [
      287
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 290,
    "number": 290,
    "title": "Find All Anagrams (Variant 29)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Find All Anagrams (Variant 29)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      289,
      291,
      285
    ],
    "prerequisites": [
      288
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 291,
    "number": 291,
    "title": "Max Consecutive Ones III (Variant 30)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Max Consecutive Ones III (Variant 30)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      290,
      292,
      286
    ],
    "prerequisites": [
      289
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 292,
    "number": 292,
    "title": "Subarrays with K Distinct (Variant 30)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subarrays with K Distinct (Variant 30)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      291,
      293,
      287
    ],
    "prerequisites": [
      290
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 293,
    "number": 293,
    "title": "Permutation in String (Variant 30)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Permutation in String (Variant 30)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      292,
      294,
      288
    ],
    "prerequisites": [
      291
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 294,
    "number": 294,
    "title": "Longest Repeating Character Replacement (Variant 30)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Longest Repeating Character Replacement (Variant 30)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      293,
      295,
      289
    ],
    "prerequisites": [
      292
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 295,
    "number": 295,
    "title": "Fruit Into Baskets (Variant 30)",
    "difficulty": "Easy",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Fruit Into Baskets (Variant 30)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      294,
      296,
      290
    ],
    "prerequisites": [
      293
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 296,
    "number": 296,
    "title": "Minimum Size Subarray Sum (Variant 30)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Size Subarray Sum (Variant 30)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      295,
      297,
      291
    ],
    "prerequisites": [
      294
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 297,
    "number": 297,
    "title": "Count Subarrays With Fixed Bounds (Variant 30)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Count Subarrays With Fixed Bounds (Variant 30)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      296,
      298,
      292
    ],
    "prerequisites": [
      295
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 298,
    "number": 298,
    "title": "Grumpy Bookstore Owner (Variant 30)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Grumpy Bookstore Owner (Variant 30)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      297,
      299,
      293
    ],
    "prerequisites": [
      296
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 299,
    "number": 299,
    "title": "Subarray Product Less Than K (Variant 30)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Product Less Than K (Variant 30)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      298,
      300,
      294
    ],
    "prerequisites": [
      297
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 300,
    "number": 300,
    "title": "Find All Anagrams (Variant 30)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find All Anagrams (Variant 30)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      299,
      301,
      295
    ],
    "prerequisites": [
      298
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 301,
    "number": 301,
    "title": "Max Consecutive Ones III (Variant 31)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Max Consecutive Ones III (Variant 31)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      300,
      302,
      296
    ],
    "prerequisites": [
      299
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 302,
    "number": 302,
    "title": "Subarrays with K Distinct (Variant 31)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarrays with K Distinct (Variant 31)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      301,
      303,
      297
    ],
    "prerequisites": [
      300
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 303,
    "number": 303,
    "title": "Permutation in String (Variant 31)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutation in String (Variant 31)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      302,
      304,
      298
    ],
    "prerequisites": [
      301
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 304,
    "number": 304,
    "title": "Longest Repeating Character Replacement (Variant 31)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Longest Repeating Character Replacement (Variant 31)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      303,
      305,
      299
    ],
    "prerequisites": [
      302
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 305,
    "number": 305,
    "title": "Fruit Into Baskets (Variant 31)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Fruit Into Baskets (Variant 31)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      304,
      306,
      300
    ],
    "prerequisites": [
      303
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 306,
    "number": 306,
    "title": "Minimum Size Subarray Sum (Variant 31)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Size Subarray Sum (Variant 31)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      305,
      307,
      301
    ],
    "prerequisites": [
      304
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 307,
    "number": 307,
    "title": "Count Subarrays With Fixed Bounds (Variant 31)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Count Subarrays With Fixed Bounds (Variant 31)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      306,
      308,
      302
    ],
    "prerequisites": [
      305
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 308,
    "number": 308,
    "title": "Grumpy Bookstore Owner (Variant 31)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Grumpy Bookstore Owner (Variant 31)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      307,
      309,
      303
    ],
    "prerequisites": [
      306
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 309,
    "number": 309,
    "title": "Subarray Product Less Than K (Variant 31)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Product Less Than K (Variant 31)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      308,
      310,
      304
    ],
    "prerequisites": [
      307
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 310,
    "number": 310,
    "title": "Find All Anagrams (Variant 31)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find All Anagrams (Variant 31)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      309,
      311,
      305
    ],
    "prerequisites": [
      308
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 311,
    "number": 311,
    "title": "Max Consecutive Ones III (Variant 32)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Max Consecutive Ones III (Variant 32)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      310,
      312,
      306
    ],
    "prerequisites": [
      309
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 312,
    "number": 312,
    "title": "Subarrays with K Distinct (Variant 32)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarrays with K Distinct (Variant 32)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      311,
      313,
      307
    ],
    "prerequisites": [
      310
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 313,
    "number": 313,
    "title": "Permutation in String (Variant 32)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutation in String (Variant 32)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      312,
      314,
      308
    ],
    "prerequisites": [
      311
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 314,
    "number": 314,
    "title": "Longest Repeating Character Replacement (Variant 32)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Longest Repeating Character Replacement (Variant 32)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      313,
      315,
      309
    ],
    "prerequisites": [
      312
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 315,
    "number": 315,
    "title": "Fruit Into Baskets (Variant 32)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Fruit Into Baskets (Variant 32)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      314,
      316,
      310
    ],
    "prerequisites": [
      313
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 316,
    "number": 316,
    "title": "Minimum Size Subarray Sum (Variant 32)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Size Subarray Sum (Variant 32)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      315,
      317,
      311
    ],
    "prerequisites": [
      314
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 317,
    "number": 317,
    "title": "Count Subarrays With Fixed Bounds (Variant 32)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Count Subarrays With Fixed Bounds (Variant 32)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      316,
      318,
      312
    ],
    "prerequisites": [
      315
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 318,
    "number": 318,
    "title": "Grumpy Bookstore Owner (Variant 32)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Grumpy Bookstore Owner (Variant 32)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      317,
      319,
      313
    ],
    "prerequisites": [
      316
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 319,
    "number": 319,
    "title": "Subarray Product Less Than K (Variant 32)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Product Less Than K (Variant 32)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      318,
      320,
      314
    ],
    "prerequisites": [
      317
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 320,
    "number": 320,
    "title": "Find All Anagrams (Variant 32)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find All Anagrams (Variant 32)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      319,
      321,
      315
    ],
    "prerequisites": [
      318
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 321,
    "number": 321,
    "title": "Max Consecutive Ones III (Variant 33)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Max Consecutive Ones III (Variant 33)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      320,
      322,
      316
    ],
    "prerequisites": [
      319
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 322,
    "number": 322,
    "title": "Subarrays with K Distinct (Variant 33)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarrays with K Distinct (Variant 33)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      321,
      323,
      317
    ],
    "prerequisites": [
      320
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 323,
    "number": 323,
    "title": "Permutation in String (Variant 33)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutation in String (Variant 33)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      322,
      324,
      318
    ],
    "prerequisites": [
      321
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 324,
    "number": 324,
    "title": "Longest Repeating Character Replacement (Variant 33)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Longest Repeating Character Replacement (Variant 33)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      323,
      325,
      319
    ],
    "prerequisites": [
      322
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 325,
    "number": 325,
    "title": "Fruit Into Baskets (Variant 33)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Fruit Into Baskets (Variant 33)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      324,
      326,
      320
    ],
    "prerequisites": [
      323
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 326,
    "number": 326,
    "title": "Minimum Size Subarray Sum (Variant 33)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Size Subarray Sum (Variant 33)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      325,
      327,
      321
    ],
    "prerequisites": [
      324
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 327,
    "number": 327,
    "title": "Count Subarrays With Fixed Bounds (Variant 33)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Count Subarrays With Fixed Bounds (Variant 33)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      326,
      328,
      322
    ],
    "prerequisites": [
      325
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 328,
    "number": 328,
    "title": "Grumpy Bookstore Owner (Variant 33)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Grumpy Bookstore Owner (Variant 33)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      327,
      329,
      323
    ],
    "prerequisites": [
      326
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 329,
    "number": 329,
    "title": "Subarray Product Less Than K (Variant 33)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subarray Product Less Than K (Variant 33)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      328,
      330,
      324
    ],
    "prerequisites": [
      327
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 330,
    "number": 330,
    "title": "Find All Anagrams (Variant 33)",
    "difficulty": "Medium",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find All Anagrams (Variant 33)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      329,
      331,
      325
    ],
    "prerequisites": [
      328
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 100000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 331,
    "number": 331,
    "title": "Max Consecutive Ones III (Variant 34)",
    "difficulty": "Hard",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Max Consecutive Ones III (Variant 34)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      330,
      332,
      326
    ],
    "prerequisites": [
      329
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 1000000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 332,
    "number": 332,
    "title": "Subarrays with K Distinct (Variant 34)",
    "difficulty": "Hard",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Subarrays with K Distinct (Variant 34)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      331,
      333,
      327
    ],
    "prerequisites": [
      330
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 1000000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 333,
    "number": 333,
    "title": "Permutation in String (Variant 34)",
    "difficulty": "Hard",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Permutation in String (Variant 34)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      332,
      334,
      328
    ],
    "prerequisites": [
      331
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 1000000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 334,
    "number": 334,
    "title": "Longest Repeating Character Replacement (Variant 34)",
    "difficulty": "Hard",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Longest Repeating Character Replacement (Variant 34)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      333,
      335,
      329
    ],
    "prerequisites": [
      332
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 1000000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 335,
    "number": 335,
    "title": "Fruit Into Baskets (Variant 34)",
    "difficulty": "Hard",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Fruit Into Baskets (Variant 34)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      334,
      336,
      330
    ],
    "prerequisites": [
      333
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 1000000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 336,
    "number": 336,
    "title": "Minimum Size Subarray Sum (Variant 34)",
    "difficulty": "Hard",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Minimum Size Subarray Sum (Variant 34)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      335,
      337,
      331
    ],
    "prerequisites": [
      334
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 1000000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 337,
    "number": 337,
    "title": "Count Subarrays With Fixed Bounds (Variant 34)",
    "difficulty": "Hard",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Count Subarrays With Fixed Bounds (Variant 34)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      336,
      338,
      332
    ],
    "prerequisites": [
      335
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 1000000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 338,
    "number": 338,
    "title": "Grumpy Bookstore Owner (Variant 34)",
    "difficulty": "Hard",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Grumpy Bookstore Owner (Variant 34)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      337,
      339,
      333
    ],
    "prerequisites": [
      336
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 1000000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 339,
    "number": 339,
    "title": "Subarray Product Less Than K (Variant 34)",
    "difficulty": "Hard",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Subarray Product Less Than K (Variant 34)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      338,
      340,
      334
    ],
    "prerequisites": [
      337
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 1000000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 340,
    "number": 340,
    "title": "Find All Anagrams (Variant 34)",
    "difficulty": "Hard",
    "topic": "Sliding Window",
    "subtopic": "Sliding Window Core Mechanics",
    "phase": "Phase 4 \u2014 Sliding Window",
    "pattern": "Sliding Window",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Find All Anagrams (Variant 34)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Sliding Window. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Sliding Window technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Sliding Window)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Sliding Window)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Sliding Window)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Sliding Window)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      339,
      341,
      335
    ],
    "prerequisites": [
      338
    ],
    "tags": [
      "Sliding Window",
      "Sliding Window",
      "Phase 4",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Sliding Window because constraints are N <= 1000000, making O(N^2) brute force infeasible. Sliding Window leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Sliding Window optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 341,
    "number": 341,
    "title": "Binary Search (Variant 35)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Binary Search (Variant 35)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      340,
      342,
      336
    ],
    "prerequisites": [
      339
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 342,
    "number": 342,
    "title": "First and Last Position (Variant 35)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **First and Last Position (Variant 35)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      341,
      343,
      337
    ],
    "prerequisites": [
      340
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 343,
    "number": 343,
    "title": "Search Insert Position (Variant 35)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Search Insert Position (Variant 35)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      342,
      344,
      338
    ],
    "prerequisites": [
      341
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 344,
    "number": 344,
    "title": "Find Peak Element (Variant 35)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Find Peak Element (Variant 35)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      343,
      345,
      339
    ],
    "prerequisites": [
      342
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 345,
    "number": 345,
    "title": "Search in Rotated Sorted Array (Variant 35)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Search in Rotated Sorted Array (Variant 35)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      344,
      346,
      340
    ],
    "prerequisites": [
      343
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 346,
    "number": 346,
    "title": "Kth Smallest Pair Distance (Variant 35)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Kth Smallest Pair Distance (Variant 35)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      345,
      347,
      341
    ],
    "prerequisites": [
      344
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 347,
    "number": 347,
    "title": "Capacity To Ship Packages (Variant 35)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Capacity To Ship Packages (Variant 35)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      346,
      348,
      342
    ],
    "prerequisites": [
      345
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 348,
    "number": 348,
    "title": "Koko Eating Bananas (Variant 35)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Koko Eating Bananas (Variant 35)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      347,
      349,
      343
    ],
    "prerequisites": [
      346
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 349,
    "number": 349,
    "title": "Split Array Largest Sum (Variant 35)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Split Array Largest Sum (Variant 35)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      348,
      350,
      344
    ],
    "prerequisites": [
      347
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 350,
    "number": 350,
    "title": "Find Minimum in Rotated Sorted Array (Variant 35)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Find Minimum in Rotated Sorted Array (Variant 35)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      349,
      351,
      345
    ],
    "prerequisites": [
      348
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 351,
    "number": 351,
    "title": "Binary Search (Variant 36)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Binary Search (Variant 36)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      350,
      352,
      346
    ],
    "prerequisites": [
      349
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 352,
    "number": 352,
    "title": "First and Last Position (Variant 36)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **First and Last Position (Variant 36)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      351,
      353,
      347
    ],
    "prerequisites": [
      350
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 353,
    "number": 353,
    "title": "Search Insert Position (Variant 36)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Search Insert Position (Variant 36)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      352,
      354,
      348
    ],
    "prerequisites": [
      351
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 354,
    "number": 354,
    "title": "Find Peak Element (Variant 36)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Find Peak Element (Variant 36)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      353,
      355,
      349
    ],
    "prerequisites": [
      352
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 355,
    "number": 355,
    "title": "Search in Rotated Sorted Array (Variant 36)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Search in Rotated Sorted Array (Variant 36)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      354,
      356,
      350
    ],
    "prerequisites": [
      353
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 356,
    "number": 356,
    "title": "Kth Smallest Pair Distance (Variant 36)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Kth Smallest Pair Distance (Variant 36)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      355,
      357,
      351
    ],
    "prerequisites": [
      354
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 357,
    "number": 357,
    "title": "Capacity To Ship Packages (Variant 36)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Capacity To Ship Packages (Variant 36)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      356,
      358,
      352
    ],
    "prerequisites": [
      355
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 358,
    "number": 358,
    "title": "Koko Eating Bananas (Variant 36)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Koko Eating Bananas (Variant 36)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      357,
      359,
      353
    ],
    "prerequisites": [
      356
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 359,
    "number": 359,
    "title": "Split Array Largest Sum (Variant 36)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Split Array Largest Sum (Variant 36)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      358,
      360,
      354
    ],
    "prerequisites": [
      357
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 360,
    "number": 360,
    "title": "Find Minimum in Rotated Sorted Array (Variant 36)",
    "difficulty": "Easy",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Find Minimum in Rotated Sorted Array (Variant 36)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      359,
      361,
      355
    ],
    "prerequisites": [
      358
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 361,
    "number": 361,
    "title": "Binary Search (Variant 37)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Binary Search (Variant 37)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      360,
      362,
      356
    ],
    "prerequisites": [
      359
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 362,
    "number": 362,
    "title": "First and Last Position (Variant 37)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **First and Last Position (Variant 37)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      361,
      363,
      357
    ],
    "prerequisites": [
      360
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 363,
    "number": 363,
    "title": "Search Insert Position (Variant 37)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Search Insert Position (Variant 37)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      362,
      364,
      358
    ],
    "prerequisites": [
      361
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 364,
    "number": 364,
    "title": "Find Peak Element (Variant 37)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find Peak Element (Variant 37)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      363,
      365,
      359
    ],
    "prerequisites": [
      362
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 365,
    "number": 365,
    "title": "Search in Rotated Sorted Array (Variant 37)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Search in Rotated Sorted Array (Variant 37)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      364,
      366,
      360
    ],
    "prerequisites": [
      363
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 366,
    "number": 366,
    "title": "Kth Smallest Pair Distance (Variant 37)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Kth Smallest Pair Distance (Variant 37)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      365,
      367,
      361
    ],
    "prerequisites": [
      364
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 367,
    "number": 367,
    "title": "Capacity To Ship Packages (Variant 37)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Capacity To Ship Packages (Variant 37)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      366,
      368,
      362
    ],
    "prerequisites": [
      365
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 368,
    "number": 368,
    "title": "Koko Eating Bananas (Variant 37)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Koko Eating Bananas (Variant 37)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      367,
      369,
      363
    ],
    "prerequisites": [
      366
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 369,
    "number": 369,
    "title": "Split Array Largest Sum (Variant 37)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Split Array Largest Sum (Variant 37)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      368,
      370,
      364
    ],
    "prerequisites": [
      367
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 370,
    "number": 370,
    "title": "Find Minimum in Rotated Sorted Array (Variant 37)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find Minimum in Rotated Sorted Array (Variant 37)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      369,
      371,
      365
    ],
    "prerequisites": [
      368
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 371,
    "number": 371,
    "title": "Binary Search (Variant 38)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Binary Search (Variant 38)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      370,
      372,
      366
    ],
    "prerequisites": [
      369
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 372,
    "number": 372,
    "title": "First and Last Position (Variant 38)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **First and Last Position (Variant 38)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      371,
      373,
      367
    ],
    "prerequisites": [
      370
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 373,
    "number": 373,
    "title": "Search Insert Position (Variant 38)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Search Insert Position (Variant 38)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      372,
      374,
      368
    ],
    "prerequisites": [
      371
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 374,
    "number": 374,
    "title": "Find Peak Element (Variant 38)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find Peak Element (Variant 38)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      373,
      375,
      369
    ],
    "prerequisites": [
      372
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 375,
    "number": 375,
    "title": "Search in Rotated Sorted Array (Variant 38)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Search in Rotated Sorted Array (Variant 38)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      374,
      376,
      370
    ],
    "prerequisites": [
      373
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 376,
    "number": 376,
    "title": "Kth Smallest Pair Distance (Variant 38)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Kth Smallest Pair Distance (Variant 38)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      375,
      377,
      371
    ],
    "prerequisites": [
      374
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 377,
    "number": 377,
    "title": "Capacity To Ship Packages (Variant 38)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Capacity To Ship Packages (Variant 38)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      376,
      378,
      372
    ],
    "prerequisites": [
      375
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 378,
    "number": 378,
    "title": "Koko Eating Bananas (Variant 38)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Koko Eating Bananas (Variant 38)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      377,
      379,
      373
    ],
    "prerequisites": [
      376
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 379,
    "number": 379,
    "title": "Split Array Largest Sum (Variant 38)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Split Array Largest Sum (Variant 38)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      378,
      380,
      374
    ],
    "prerequisites": [
      377
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 380,
    "number": 380,
    "title": "Find Minimum in Rotated Sorted Array (Variant 38)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find Minimum in Rotated Sorted Array (Variant 38)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      379,
      381,
      375
    ],
    "prerequisites": [
      378
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 381,
    "number": 381,
    "title": "Binary Search (Variant 39)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Binary Search (Variant 39)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      380,
      382,
      376
    ],
    "prerequisites": [
      379
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 382,
    "number": 382,
    "title": "First and Last Position (Variant 39)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **First and Last Position (Variant 39)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      381,
      383,
      377
    ],
    "prerequisites": [
      380
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 383,
    "number": 383,
    "title": "Search Insert Position (Variant 39)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Search Insert Position (Variant 39)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      382,
      384,
      378
    ],
    "prerequisites": [
      381
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 384,
    "number": 384,
    "title": "Find Peak Element (Variant 39)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find Peak Element (Variant 39)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      383,
      385,
      379
    ],
    "prerequisites": [
      382
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 385,
    "number": 385,
    "title": "Search in Rotated Sorted Array (Variant 39)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Search in Rotated Sorted Array (Variant 39)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      384,
      386,
      380
    ],
    "prerequisites": [
      383
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 386,
    "number": 386,
    "title": "Kth Smallest Pair Distance (Variant 39)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Kth Smallest Pair Distance (Variant 39)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      385,
      387,
      381
    ],
    "prerequisites": [
      384
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 387,
    "number": 387,
    "title": "Capacity To Ship Packages (Variant 39)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Capacity To Ship Packages (Variant 39)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      386,
      388,
      382
    ],
    "prerequisites": [
      385
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 388,
    "number": 388,
    "title": "Koko Eating Bananas (Variant 39)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Koko Eating Bananas (Variant 39)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      387,
      389,
      383
    ],
    "prerequisites": [
      386
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 389,
    "number": 389,
    "title": "Split Array Largest Sum (Variant 39)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Split Array Largest Sum (Variant 39)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      388,
      390,
      384
    ],
    "prerequisites": [
      387
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 390,
    "number": 390,
    "title": "Find Minimum in Rotated Sorted Array (Variant 39)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find Minimum in Rotated Sorted Array (Variant 39)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      389,
      391,
      385
    ],
    "prerequisites": [
      388
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 391,
    "number": 391,
    "title": "Binary Search (Variant 40)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Binary Search (Variant 40)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      390,
      392,
      386
    ],
    "prerequisites": [
      389
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 392,
    "number": 392,
    "title": "First and Last Position (Variant 40)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **First and Last Position (Variant 40)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      391,
      393,
      387
    ],
    "prerequisites": [
      390
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 393,
    "number": 393,
    "title": "Search Insert Position (Variant 40)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Search Insert Position (Variant 40)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      392,
      394,
      388
    ],
    "prerequisites": [
      391
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 394,
    "number": 394,
    "title": "Find Peak Element (Variant 40)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find Peak Element (Variant 40)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      393,
      395,
      389
    ],
    "prerequisites": [
      392
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 395,
    "number": 395,
    "title": "Search in Rotated Sorted Array (Variant 40)",
    "difficulty": "Medium",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Search in Rotated Sorted Array (Variant 40)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      394,
      396,
      390
    ],
    "prerequisites": [
      393
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 100000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 396,
    "number": 396,
    "title": "Kth Smallest Pair Distance (Variant 40)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Kth Smallest Pair Distance (Variant 40)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      395,
      397,
      391
    ],
    "prerequisites": [
      394
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 397,
    "number": 397,
    "title": "Capacity To Ship Packages (Variant 40)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Capacity To Ship Packages (Variant 40)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      396,
      398,
      392
    ],
    "prerequisites": [
      395
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 398,
    "number": 398,
    "title": "Koko Eating Bananas (Variant 40)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Koko Eating Bananas (Variant 40)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      397,
      399,
      393
    ],
    "prerequisites": [
      396
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 399,
    "number": 399,
    "title": "Split Array Largest Sum (Variant 40)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Split Array Largest Sum (Variant 40)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      398,
      400,
      394
    ],
    "prerequisites": [
      397
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 400,
    "number": 400,
    "title": "Find Minimum in Rotated Sorted Array (Variant 40)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Find Minimum in Rotated Sorted Array (Variant 40)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      399,
      401,
      395
    ],
    "prerequisites": [
      398
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 401,
    "number": 401,
    "title": "Binary Search (Variant 41)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Binary Search (Variant 41)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      400,
      402,
      396
    ],
    "prerequisites": [
      399
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 402,
    "number": 402,
    "title": "First and Last Position (Variant 41)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **First and Last Position (Variant 41)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      401,
      403,
      397
    ],
    "prerequisites": [
      400
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 403,
    "number": 403,
    "title": "Search Insert Position (Variant 41)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Search Insert Position (Variant 41)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      402,
      404,
      398
    ],
    "prerequisites": [
      401
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 404,
    "number": 404,
    "title": "Find Peak Element (Variant 41)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Find Peak Element (Variant 41)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      403,
      405,
      399
    ],
    "prerequisites": [
      402
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 405,
    "number": 405,
    "title": "Search in Rotated Sorted Array (Variant 41)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Search in Rotated Sorted Array (Variant 41)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      404,
      406,
      400
    ],
    "prerequisites": [
      403
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 406,
    "number": 406,
    "title": "Kth Smallest Pair Distance (Variant 41)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Kth Smallest Pair Distance (Variant 41)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      405,
      407,
      401
    ],
    "prerequisites": [
      404
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 407,
    "number": 407,
    "title": "Capacity To Ship Packages (Variant 41)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Capacity To Ship Packages (Variant 41)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      406,
      408,
      402
    ],
    "prerequisites": [
      405
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 408,
    "number": 408,
    "title": "Koko Eating Bananas (Variant 41)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Koko Eating Bananas (Variant 41)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      407,
      409,
      403
    ],
    "prerequisites": [
      406
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 409,
    "number": 409,
    "title": "Split Array Largest Sum (Variant 41)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Split Array Largest Sum (Variant 41)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      408,
      410,
      404
    ],
    "prerequisites": [
      407
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 410,
    "number": 410,
    "title": "Find Minimum in Rotated Sorted Array (Variant 41)",
    "difficulty": "Hard",
    "topic": "Searching",
    "subtopic": "Binary Search Core Mechanics",
    "phase": "Phase 5 \u2014 Searching & Binary Search",
    "pattern": "Binary Search",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Find Minimum in Rotated Sorted Array (Variant 41)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Binary Search. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Binary Search technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Binary Search)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Binary Search)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Binary Search)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Binary Search)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      409,
      411,
      405
    ],
    "prerequisites": [
      408
    ],
    "tags": [
      "Searching",
      "Binary Search",
      "Phase 5",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Binary Search because constraints are N <= 1000000, making O(N^2) brute force infeasible. Binary Search leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Binary Search optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 411,
    "number": 411,
    "title": "Sort List (Variant 42)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort List (Variant 42)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      410,
      412,
      406
    ],
    "prerequisites": [
      409
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 412,
    "number": 412,
    "title": "Kth Largest Element (Variant 42)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Kth Largest Element (Variant 42)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      411,
      413,
      407
    ],
    "prerequisites": [
      410
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 413,
    "number": 413,
    "title": "Relative Sort Array (Variant 42)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Relative Sort Array (Variant 42)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      412,
      414,
      408
    ],
    "prerequisites": [
      411
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 414,
    "number": 414,
    "title": "Sort Colors II (Variant 42)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort Colors II (Variant 42)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      413,
      415,
      409
    ],
    "prerequisites": [
      412
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 415,
    "number": 415,
    "title": "Largest Number (Variant 42)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Largest Number (Variant 42)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      414,
      416,
      410
    ],
    "prerequisites": [
      413
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 416,
    "number": 416,
    "title": "Wiggle Sort (Variant 42)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Wiggle Sort (Variant 42)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      415,
      417,
      411
    ],
    "prerequisites": [
      414
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 417,
    "number": 417,
    "title": "Minimum Absolute Difference (Variant 42)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Minimum Absolute Difference (Variant 42)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      416,
      418,
      412
    ],
    "prerequisites": [
      415
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 418,
    "number": 418,
    "title": "H-Index (Variant 42)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **H-Index (Variant 42)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      417,
      419,
      413
    ],
    "prerequisites": [
      416
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 419,
    "number": 419,
    "title": "Custom Salary Sorting (Variant 42)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Custom Salary Sorting (Variant 42)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      418,
      420,
      414
    ],
    "prerequisites": [
      417
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 420,
    "number": 420,
    "title": "Count of Smaller Numbers After Self (Variant 42)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Count of Smaller Numbers After Self (Variant 42)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      419,
      421,
      415
    ],
    "prerequisites": [
      418
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 421,
    "number": 421,
    "title": "Sort List (Variant 43)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort List (Variant 43)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      420,
      422,
      416
    ],
    "prerequisites": [
      419
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 422,
    "number": 422,
    "title": "Kth Largest Element (Variant 43)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Kth Largest Element (Variant 43)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      421,
      423,
      417
    ],
    "prerequisites": [
      420
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 423,
    "number": 423,
    "title": "Relative Sort Array (Variant 43)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Relative Sort Array (Variant 43)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      422,
      424,
      418
    ],
    "prerequisites": [
      421
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 424,
    "number": 424,
    "title": "Sort Colors II (Variant 43)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort Colors II (Variant 43)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      423,
      425,
      419
    ],
    "prerequisites": [
      422
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 425,
    "number": 425,
    "title": "Largest Number (Variant 43)",
    "difficulty": "Easy",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Largest Number (Variant 43)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      424,
      426,
      420
    ],
    "prerequisites": [
      423
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 426,
    "number": 426,
    "title": "Wiggle Sort (Variant 43)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Wiggle Sort (Variant 43)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      425,
      427,
      421
    ],
    "prerequisites": [
      424
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 427,
    "number": 427,
    "title": "Minimum Absolute Difference (Variant 43)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Absolute Difference (Variant 43)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      426,
      428,
      422
    ],
    "prerequisites": [
      425
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 428,
    "number": 428,
    "title": "H-Index (Variant 43)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **H-Index (Variant 43)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      427,
      429,
      423
    ],
    "prerequisites": [
      426
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 429,
    "number": 429,
    "title": "Custom Salary Sorting (Variant 43)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Custom Salary Sorting (Variant 43)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      428,
      430,
      424
    ],
    "prerequisites": [
      427
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 430,
    "number": 430,
    "title": "Count of Smaller Numbers After Self (Variant 43)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Count of Smaller Numbers After Self (Variant 43)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      429,
      431,
      425
    ],
    "prerequisites": [
      428
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 431,
    "number": 431,
    "title": "Sort List (Variant 44)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort List (Variant 44)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      430,
      432,
      426
    ],
    "prerequisites": [
      429
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 432,
    "number": 432,
    "title": "Kth Largest Element (Variant 44)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Kth Largest Element (Variant 44)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      431,
      433,
      427
    ],
    "prerequisites": [
      430
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 433,
    "number": 433,
    "title": "Relative Sort Array (Variant 44)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Relative Sort Array (Variant 44)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      432,
      434,
      428
    ],
    "prerequisites": [
      431
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 434,
    "number": 434,
    "title": "Sort Colors II (Variant 44)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Colors II (Variant 44)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      433,
      435,
      429
    ],
    "prerequisites": [
      432
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 435,
    "number": 435,
    "title": "Largest Number (Variant 44)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Largest Number (Variant 44)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      434,
      436,
      430
    ],
    "prerequisites": [
      433
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 436,
    "number": 436,
    "title": "Wiggle Sort (Variant 44)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Wiggle Sort (Variant 44)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      435,
      437,
      431
    ],
    "prerequisites": [
      434
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 437,
    "number": 437,
    "title": "Minimum Absolute Difference (Variant 44)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Absolute Difference (Variant 44)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      436,
      438,
      432
    ],
    "prerequisites": [
      435
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 438,
    "number": 438,
    "title": "H-Index (Variant 44)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **H-Index (Variant 44)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      437,
      439,
      433
    ],
    "prerequisites": [
      436
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 439,
    "number": 439,
    "title": "Custom Salary Sorting (Variant 44)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Custom Salary Sorting (Variant 44)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      438,
      440,
      434
    ],
    "prerequisites": [
      437
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 440,
    "number": 440,
    "title": "Count of Smaller Numbers After Self (Variant 44)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Count of Smaller Numbers After Self (Variant 44)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      439,
      441,
      435
    ],
    "prerequisites": [
      438
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 441,
    "number": 441,
    "title": "Sort List (Variant 45)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort List (Variant 45)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      440,
      442,
      436
    ],
    "prerequisites": [
      439
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 442,
    "number": 442,
    "title": "Kth Largest Element (Variant 45)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Kth Largest Element (Variant 45)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      441,
      443,
      437
    ],
    "prerequisites": [
      440
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 443,
    "number": 443,
    "title": "Relative Sort Array (Variant 45)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Relative Sort Array (Variant 45)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      442,
      444,
      438
    ],
    "prerequisites": [
      441
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 444,
    "number": 444,
    "title": "Sort Colors II (Variant 45)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort Colors II (Variant 45)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      443,
      445,
      439
    ],
    "prerequisites": [
      442
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 445,
    "number": 445,
    "title": "Largest Number (Variant 45)",
    "difficulty": "Medium",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Largest Number (Variant 45)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      444,
      446,
      440
    ],
    "prerequisites": [
      443
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 100000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 446,
    "number": 446,
    "title": "Wiggle Sort (Variant 45)",
    "difficulty": "Hard",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Wiggle Sort (Variant 45)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      445,
      447,
      441
    ],
    "prerequisites": [
      444
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 1000000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 447,
    "number": 447,
    "title": "Minimum Absolute Difference (Variant 45)",
    "difficulty": "Hard",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Minimum Absolute Difference (Variant 45)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      446,
      448,
      442
    ],
    "prerequisites": [
      445
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 1000000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 448,
    "number": 448,
    "title": "H-Index (Variant 45)",
    "difficulty": "Hard",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **H-Index (Variant 45)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      447,
      449,
      443
    ],
    "prerequisites": [
      446
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 1000000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 449,
    "number": 449,
    "title": "Custom Salary Sorting (Variant 45)",
    "difficulty": "Hard",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Custom Salary Sorting (Variant 45)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      448,
      450,
      444
    ],
    "prerequisites": [
      447
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 1000000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 450,
    "number": 450,
    "title": "Count of Smaller Numbers After Self (Variant 45)",
    "difficulty": "Hard",
    "topic": "Sorting",
    "subtopic": "Merge Sort / QuickSelect / Sorting Core Mechanics",
    "phase": "Phase 6 \u2014 Sorting & Custom Comparators",
    "pattern": "Merge Sort / QuickSelect / Sorting",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Count of Smaller Numbers After Self (Variant 45)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Merge Sort / QuickSelect / Sorting. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Merge Sort / QuickSelect / Sorting technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Merge Sort / QuickSelect / Sorting)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Merge Sort / QuickSelect / Sorting)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Merge Sort / QuickSelect / Sorting)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Merge Sort / QuickSelect / Sorting)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      449,
      451,
      445
    ],
    "prerequisites": [
      448
    ],
    "tags": [
      "Sorting",
      "Merge Sort / QuickSelect / Sorting",
      "Phase 6",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Merge Sort / QuickSelect / Sorting because constraints are N <= 1000000, making O(N^2) brute force infeasible. Merge Sort / QuickSelect / Sorting leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Merge Sort / QuickSelect / Sorting optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 451,
    "number": 451,
    "title": "Delete Node in Linked List (Variant 46)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Delete Node in Linked List (Variant 46)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      450,
      452,
      446
    ],
    "prerequisites": [
      449
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 452,
    "number": 452,
    "title": "Swap Nodes in Pairs (Variant 46)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Swap Nodes in Pairs (Variant 46)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      451,
      453,
      447
    ],
    "prerequisites": [
      450
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 453,
    "number": 453,
    "title": "Rotate List (Variant 46)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Rotate List (Variant 46)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      452,
      454,
      448
    ],
    "prerequisites": [
      451
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 454,
    "number": 454,
    "title": "Partition List (Variant 46)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Partition List (Variant 46)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      453,
      455,
      449
    ],
    "prerequisites": [
      452
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 455,
    "number": 455,
    "title": "Reverse Linked List II (Variant 46)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Reverse Linked List II (Variant 46)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      454,
      456,
      450
    ],
    "prerequisites": [
      453
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 456,
    "number": 456,
    "title": "Flatten Multilevel Doubly LinkedList (Variant 46)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Flatten Multilevel Doubly LinkedList (Variant 46)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      455,
      457,
      451
    ],
    "prerequisites": [
      454
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 457,
    "number": 457,
    "title": "Insertion Sort List (Variant 46)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Insertion Sort List (Variant 46)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      456,
      458,
      452
    ],
    "prerequisites": [
      455
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 458,
    "number": 458,
    "title": "Sort List with Pointers (Variant 46)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort List with Pointers (Variant 46)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      457,
      459,
      453
    ],
    "prerequisites": [
      456
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 459,
    "number": 459,
    "title": "Linked List Cycle II (Variant 46)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Linked List Cycle II (Variant 46)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      458,
      460,
      454
    ],
    "prerequisites": [
      457
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 460,
    "number": 460,
    "title": "Remove Duplicates II (Variant 46)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Remove Duplicates II (Variant 46)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      459,
      461,
      455
    ],
    "prerequisites": [
      458
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 461,
    "number": 461,
    "title": "Delete Node in Linked List (Variant 47)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Delete Node in Linked List (Variant 47)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      460,
      462,
      456
    ],
    "prerequisites": [
      459
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 462,
    "number": 462,
    "title": "Swap Nodes in Pairs (Variant 47)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Swap Nodes in Pairs (Variant 47)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      461,
      463,
      457
    ],
    "prerequisites": [
      460
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 463,
    "number": 463,
    "title": "Rotate List (Variant 47)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Rotate List (Variant 47)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      462,
      464,
      458
    ],
    "prerequisites": [
      461
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 464,
    "number": 464,
    "title": "Partition List (Variant 47)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Partition List (Variant 47)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      463,
      465,
      459
    ],
    "prerequisites": [
      462
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 465,
    "number": 465,
    "title": "Reverse Linked List II (Variant 47)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Reverse Linked List II (Variant 47)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      464,
      466,
      460
    ],
    "prerequisites": [
      463
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 466,
    "number": 466,
    "title": "Flatten Multilevel Doubly LinkedList (Variant 47)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Flatten Multilevel Doubly LinkedList (Variant 47)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      465,
      467,
      461
    ],
    "prerequisites": [
      464
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 467,
    "number": 467,
    "title": "Insertion Sort List (Variant 47)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Insertion Sort List (Variant 47)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      466,
      468,
      462
    ],
    "prerequisites": [
      465
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 468,
    "number": 468,
    "title": "Sort List with Pointers (Variant 47)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sort List with Pointers (Variant 47)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      467,
      469,
      463
    ],
    "prerequisites": [
      466
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 469,
    "number": 469,
    "title": "Linked List Cycle II (Variant 47)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Linked List Cycle II (Variant 47)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      468,
      470,
      464
    ],
    "prerequisites": [
      467
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 470,
    "number": 470,
    "title": "Remove Duplicates II (Variant 47)",
    "difficulty": "Easy",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Remove Duplicates II (Variant 47)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      469,
      471,
      465
    ],
    "prerequisites": [
      468
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 471,
    "number": 471,
    "title": "Delete Node in Linked List (Variant 48)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Delete Node in Linked List (Variant 48)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      470,
      472,
      466
    ],
    "prerequisites": [
      469
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 472,
    "number": 472,
    "title": "Swap Nodes in Pairs (Variant 48)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Swap Nodes in Pairs (Variant 48)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      471,
      473,
      467
    ],
    "prerequisites": [
      470
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 473,
    "number": 473,
    "title": "Rotate List (Variant 48)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Rotate List (Variant 48)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      472,
      474,
      468
    ],
    "prerequisites": [
      471
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 474,
    "number": 474,
    "title": "Partition List (Variant 48)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Partition List (Variant 48)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      473,
      475,
      469
    ],
    "prerequisites": [
      472
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 475,
    "number": 475,
    "title": "Reverse Linked List II (Variant 48)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Reverse Linked List II (Variant 48)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      474,
      476,
      470
    ],
    "prerequisites": [
      473
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 476,
    "number": 476,
    "title": "Flatten Multilevel Doubly LinkedList (Variant 48)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Flatten Multilevel Doubly LinkedList (Variant 48)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      475,
      477,
      471
    ],
    "prerequisites": [
      474
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 477,
    "number": 477,
    "title": "Insertion Sort List (Variant 48)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Insertion Sort List (Variant 48)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      476,
      478,
      472
    ],
    "prerequisites": [
      475
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 478,
    "number": 478,
    "title": "Sort List with Pointers (Variant 48)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort List with Pointers (Variant 48)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      477,
      479,
      473
    ],
    "prerequisites": [
      476
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 479,
    "number": 479,
    "title": "Linked List Cycle II (Variant 48)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Linked List Cycle II (Variant 48)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      478,
      480,
      474
    ],
    "prerequisites": [
      477
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 480,
    "number": 480,
    "title": "Remove Duplicates II (Variant 48)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Remove Duplicates II (Variant 48)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      479,
      481,
      475
    ],
    "prerequisites": [
      478
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 481,
    "number": 481,
    "title": "Delete Node in Linked List (Variant 49)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Delete Node in Linked List (Variant 49)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      480,
      482,
      476
    ],
    "prerequisites": [
      479
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 482,
    "number": 482,
    "title": "Swap Nodes in Pairs (Variant 49)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Swap Nodes in Pairs (Variant 49)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      481,
      483,
      477
    ],
    "prerequisites": [
      480
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 483,
    "number": 483,
    "title": "Rotate List (Variant 49)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Rotate List (Variant 49)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      482,
      484,
      478
    ],
    "prerequisites": [
      481
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 484,
    "number": 484,
    "title": "Partition List (Variant 49)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Partition List (Variant 49)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      483,
      485,
      479
    ],
    "prerequisites": [
      482
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 485,
    "number": 485,
    "title": "Reverse Linked List II (Variant 49)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Reverse Linked List II (Variant 49)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      484,
      486,
      480
    ],
    "prerequisites": [
      483
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 486,
    "number": 486,
    "title": "Flatten Multilevel Doubly LinkedList (Variant 49)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Flatten Multilevel Doubly LinkedList (Variant 49)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      485,
      487,
      481
    ],
    "prerequisites": [
      484
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 487,
    "number": 487,
    "title": "Insertion Sort List (Variant 49)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Insertion Sort List (Variant 49)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      486,
      488,
      482
    ],
    "prerequisites": [
      485
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 488,
    "number": 488,
    "title": "Sort List with Pointers (Variant 49)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort List with Pointers (Variant 49)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      487,
      489,
      483
    ],
    "prerequisites": [
      486
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 489,
    "number": 489,
    "title": "Linked List Cycle II (Variant 49)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Linked List Cycle II (Variant 49)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      488,
      490,
      484
    ],
    "prerequisites": [
      487
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 490,
    "number": 490,
    "title": "Remove Duplicates II (Variant 49)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Remove Duplicates II (Variant 49)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      489,
      491,
      485
    ],
    "prerequisites": [
      488
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 491,
    "number": 491,
    "title": "Delete Node in Linked List (Variant 50)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Delete Node in Linked List (Variant 50)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      490,
      492,
      486
    ],
    "prerequisites": [
      489
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 492,
    "number": 492,
    "title": "Swap Nodes in Pairs (Variant 50)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Swap Nodes in Pairs (Variant 50)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      491,
      493,
      487
    ],
    "prerequisites": [
      490
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 493,
    "number": 493,
    "title": "Rotate List (Variant 50)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Rotate List (Variant 50)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      492,
      494,
      488
    ],
    "prerequisites": [
      491
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 494,
    "number": 494,
    "title": "Partition List (Variant 50)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Partition List (Variant 50)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      493,
      495,
      489
    ],
    "prerequisites": [
      492
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 495,
    "number": 495,
    "title": "Reverse Linked List II (Variant 50)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Reverse Linked List II (Variant 50)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      494,
      496,
      490
    ],
    "prerequisites": [
      493
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 496,
    "number": 496,
    "title": "Flatten Multilevel Doubly LinkedList (Variant 50)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Flatten Multilevel Doubly LinkedList (Variant 50)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      495,
      497,
      491
    ],
    "prerequisites": [
      494
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 497,
    "number": 497,
    "title": "Insertion Sort List (Variant 50)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Insertion Sort List (Variant 50)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      496,
      498,
      492
    ],
    "prerequisites": [
      495
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 498,
    "number": 498,
    "title": "Sort List with Pointers (Variant 50)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sort List with Pointers (Variant 50)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      497,
      499,
      493
    ],
    "prerequisites": [
      496
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 499,
    "number": 499,
    "title": "Linked List Cycle II (Variant 50)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Linked List Cycle II (Variant 50)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      498,
      500,
      494
    ],
    "prerequisites": [
      497
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 500,
    "number": 500,
    "title": "Remove Duplicates II (Variant 50)",
    "difficulty": "Medium",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Remove Duplicates II (Variant 50)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      499,
      501,
      495
    ],
    "prerequisites": [
      498
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 100000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 501,
    "number": 501,
    "title": "Delete Node in Linked List (Variant 51)",
    "difficulty": "Hard",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Delete Node in Linked List (Variant 51)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      500,
      502,
      496
    ],
    "prerequisites": [
      499
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 1000000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 502,
    "number": 502,
    "title": "Swap Nodes in Pairs (Variant 51)",
    "difficulty": "Hard",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Swap Nodes in Pairs (Variant 51)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      501,
      503,
      497
    ],
    "prerequisites": [
      500
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 1000000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 503,
    "number": 503,
    "title": "Rotate List (Variant 51)",
    "difficulty": "Hard",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Rotate List (Variant 51)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      502,
      504,
      498
    ],
    "prerequisites": [
      501
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 1000000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 504,
    "number": 504,
    "title": "Partition List (Variant 51)",
    "difficulty": "Hard",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Partition List (Variant 51)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      503,
      505,
      499
    ],
    "prerequisites": [
      502
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 1000000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 505,
    "number": 505,
    "title": "Reverse Linked List II (Variant 51)",
    "difficulty": "Hard",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Reverse Linked List II (Variant 51)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      504,
      506,
      500
    ],
    "prerequisites": [
      503
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 1000000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 506,
    "number": 506,
    "title": "Flatten Multilevel Doubly LinkedList (Variant 51)",
    "difficulty": "Hard",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Flatten Multilevel Doubly LinkedList (Variant 51)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      505,
      507,
      501
    ],
    "prerequisites": [
      504
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 1000000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 507,
    "number": 507,
    "title": "Insertion Sort List (Variant 51)",
    "difficulty": "Hard",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Insertion Sort List (Variant 51)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      506,
      508,
      502
    ],
    "prerequisites": [
      505
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 1000000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 508,
    "number": 508,
    "title": "Sort List with Pointers (Variant 51)",
    "difficulty": "Hard",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Sort List with Pointers (Variant 51)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      507,
      509,
      503
    ],
    "prerequisites": [
      506
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 1000000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 509,
    "number": 509,
    "title": "Linked List Cycle II (Variant 51)",
    "difficulty": "Hard",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Linked List Cycle II (Variant 51)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      508,
      510,
      504
    ],
    "prerequisites": [
      507
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 1000000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 510,
    "number": 510,
    "title": "Remove Duplicates II (Variant 51)",
    "difficulty": "Hard",
    "topic": "Linked Lists",
    "subtopic": "Fast & Slow Pointer / Reversal Core Mechanics",
    "phase": "Phase 7 \u2014 Linked Lists",
    "pattern": "Fast & Slow Pointer / Reversal",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Remove Duplicates II (Variant 51)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Fast & Slow Pointer / Reversal. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Fast & Slow Pointer / Reversal technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Fast & Slow Pointer / Reversal)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Fast & Slow Pointer / Reversal)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Fast & Slow Pointer / Reversal)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Fast & Slow Pointer / Reversal)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      509,
      511,
      505
    ],
    "prerequisites": [
      508
    ],
    "tags": [
      "Linked Lists",
      "Fast & Slow Pointer / Reversal",
      "Phase 7",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Fast & Slow Pointer / Reversal because constraints are N <= 1000000, making O(N^2) brute force infeasible. Fast & Slow Pointer / Reversal leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Fast & Slow Pointer / Reversal optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 511,
    "number": 511,
    "title": "Next Greater Element II (Variant 52)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Next Greater Element II (Variant 52)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      510,
      512,
      506
    ],
    "prerequisites": [
      509
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 512,
    "number": 512,
    "title": "Daily Temperatures II (Variant 52)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Daily Temperatures II (Variant 52)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      511,
      513,
      507
    ],
    "prerequisites": [
      510
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 513,
    "number": 513,
    "title": "Online Stock Span (Variant 52)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Online Stock Span (Variant 52)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      512,
      514,
      508
    ],
    "prerequisites": [
      511
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 514,
    "number": 514,
    "title": "Decode String (Variant 52)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Decode String (Variant 52)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      513,
      515,
      509
    ],
    "prerequisites": [
      512
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 515,
    "number": 515,
    "title": "Remove All Adjacent Duplicates (Variant 52)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Remove All Adjacent Duplicates (Variant 52)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      514,
      516,
      510
    ],
    "prerequisites": [
      513
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 516,
    "number": 516,
    "title": "Validate Stack Sequences (Variant 52)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Validate Stack Sequences (Variant 52)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      515,
      517,
      511
    ],
    "prerequisites": [
      514
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 517,
    "number": 517,
    "title": "Basic Calculator (Variant 52)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Basic Calculator (Variant 52)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      516,
      518,
      512
    ],
    "prerequisites": [
      515
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 518,
    "number": 518,
    "title": "Maximal Rectangle (Variant 52)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Maximal Rectangle (Variant 52)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      517,
      519,
      513
    ],
    "prerequisites": [
      516
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 519,
    "number": 519,
    "title": "Task Scheduler (Variant 52)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Task Scheduler (Variant 52)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      518,
      520,
      514
    ],
    "prerequisites": [
      517
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 520,
    "number": 520,
    "title": "Design Circular Queue (Variant 52)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Design Circular Queue (Variant 52)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      519,
      521,
      515
    ],
    "prerequisites": [
      518
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 521,
    "number": 521,
    "title": "Next Greater Element II (Variant 53)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Next Greater Element II (Variant 53)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      520,
      522,
      516
    ],
    "prerequisites": [
      519
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 522,
    "number": 522,
    "title": "Daily Temperatures II (Variant 53)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Daily Temperatures II (Variant 53)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      521,
      523,
      517
    ],
    "prerequisites": [
      520
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 523,
    "number": 523,
    "title": "Online Stock Span (Variant 53)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Online Stock Span (Variant 53)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      522,
      524,
      518
    ],
    "prerequisites": [
      521
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 524,
    "number": 524,
    "title": "Decode String (Variant 53)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Decode String (Variant 53)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      523,
      525,
      519
    ],
    "prerequisites": [
      522
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 525,
    "number": 525,
    "title": "Remove All Adjacent Duplicates (Variant 53)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Remove All Adjacent Duplicates (Variant 53)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      524,
      526,
      520
    ],
    "prerequisites": [
      523
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 526,
    "number": 526,
    "title": "Validate Stack Sequences (Variant 53)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Validate Stack Sequences (Variant 53)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      525,
      527,
      521
    ],
    "prerequisites": [
      524
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 527,
    "number": 527,
    "title": "Basic Calculator (Variant 53)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Basic Calculator (Variant 53)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      526,
      528,
      522
    ],
    "prerequisites": [
      525
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 528,
    "number": 528,
    "title": "Maximal Rectangle (Variant 53)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Maximal Rectangle (Variant 53)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      527,
      529,
      523
    ],
    "prerequisites": [
      526
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 529,
    "number": 529,
    "title": "Task Scheduler (Variant 53)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Task Scheduler (Variant 53)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      528,
      530,
      524
    ],
    "prerequisites": [
      527
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 530,
    "number": 530,
    "title": "Design Circular Queue (Variant 53)",
    "difficulty": "Easy",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Design Circular Queue (Variant 53)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      529,
      531,
      525
    ],
    "prerequisites": [
      528
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 531,
    "number": 531,
    "title": "Next Greater Element II (Variant 54)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Next Greater Element II (Variant 54)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      530,
      532,
      526
    ],
    "prerequisites": [
      529
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 532,
    "number": 532,
    "title": "Daily Temperatures II (Variant 54)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Daily Temperatures II (Variant 54)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      531,
      533,
      527
    ],
    "prerequisites": [
      530
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 533,
    "number": 533,
    "title": "Online Stock Span (Variant 54)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Online Stock Span (Variant 54)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      532,
      534,
      528
    ],
    "prerequisites": [
      531
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 534,
    "number": 534,
    "title": "Decode String (Variant 54)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Decode String (Variant 54)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      533,
      535,
      529
    ],
    "prerequisites": [
      532
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 535,
    "number": 535,
    "title": "Remove All Adjacent Duplicates (Variant 54)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Remove All Adjacent Duplicates (Variant 54)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      534,
      536,
      530
    ],
    "prerequisites": [
      533
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 536,
    "number": 536,
    "title": "Validate Stack Sequences (Variant 54)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Validate Stack Sequences (Variant 54)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      535,
      537,
      531
    ],
    "prerequisites": [
      534
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 537,
    "number": 537,
    "title": "Basic Calculator (Variant 54)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Basic Calculator (Variant 54)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      536,
      538,
      532
    ],
    "prerequisites": [
      535
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 538,
    "number": 538,
    "title": "Maximal Rectangle (Variant 54)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Maximal Rectangle (Variant 54)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      537,
      539,
      533
    ],
    "prerequisites": [
      536
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 539,
    "number": 539,
    "title": "Task Scheduler (Variant 54)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Task Scheduler (Variant 54)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      538,
      540,
      534
    ],
    "prerequisites": [
      537
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 540,
    "number": 540,
    "title": "Design Circular Queue (Variant 54)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Design Circular Queue (Variant 54)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      539,
      541,
      535
    ],
    "prerequisites": [
      538
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 541,
    "number": 541,
    "title": "Next Greater Element II (Variant 55)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Next Greater Element II (Variant 55)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      540,
      542,
      536
    ],
    "prerequisites": [
      539
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 542,
    "number": 542,
    "title": "Daily Temperatures II (Variant 55)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Daily Temperatures II (Variant 55)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      541,
      543,
      537
    ],
    "prerequisites": [
      540
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 543,
    "number": 543,
    "title": "Online Stock Span (Variant 55)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Online Stock Span (Variant 55)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      542,
      544,
      538
    ],
    "prerequisites": [
      541
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 544,
    "number": 544,
    "title": "Decode String (Variant 55)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Decode String (Variant 55)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      543,
      545,
      539
    ],
    "prerequisites": [
      542
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 545,
    "number": 545,
    "title": "Remove All Adjacent Duplicates (Variant 55)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Remove All Adjacent Duplicates (Variant 55)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      544,
      546,
      540
    ],
    "prerequisites": [
      543
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 546,
    "number": 546,
    "title": "Validate Stack Sequences (Variant 55)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Validate Stack Sequences (Variant 55)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      545,
      547,
      541
    ],
    "prerequisites": [
      544
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 547,
    "number": 547,
    "title": "Basic Calculator (Variant 55)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Basic Calculator (Variant 55)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      546,
      548,
      542
    ],
    "prerequisites": [
      545
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 548,
    "number": 548,
    "title": "Maximal Rectangle (Variant 55)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Maximal Rectangle (Variant 55)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      547,
      549,
      543
    ],
    "prerequisites": [
      546
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 549,
    "number": 549,
    "title": "Task Scheduler (Variant 55)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Task Scheduler (Variant 55)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      548,
      550,
      544
    ],
    "prerequisites": [
      547
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 550,
    "number": 550,
    "title": "Design Circular Queue (Variant 55)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Design Circular Queue (Variant 55)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      549,
      551,
      545
    ],
    "prerequisites": [
      548
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 551,
    "number": 551,
    "title": "Next Greater Element II (Variant 56)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Next Greater Element II (Variant 56)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      550,
      552,
      546
    ],
    "prerequisites": [
      549
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 552,
    "number": 552,
    "title": "Daily Temperatures II (Variant 56)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Daily Temperatures II (Variant 56)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      551,
      553,
      547
    ],
    "prerequisites": [
      550
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 553,
    "number": 553,
    "title": "Online Stock Span (Variant 56)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Online Stock Span (Variant 56)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      552,
      554,
      548
    ],
    "prerequisites": [
      551
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 554,
    "number": 554,
    "title": "Decode String (Variant 56)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Decode String (Variant 56)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      553,
      555,
      549
    ],
    "prerequisites": [
      552
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 555,
    "number": 555,
    "title": "Remove All Adjacent Duplicates (Variant 56)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Remove All Adjacent Duplicates (Variant 56)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      554,
      556,
      550
    ],
    "prerequisites": [
      553
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 556,
    "number": 556,
    "title": "Validate Stack Sequences (Variant 56)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Validate Stack Sequences (Variant 56)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      555,
      557,
      551
    ],
    "prerequisites": [
      554
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 557,
    "number": 557,
    "title": "Basic Calculator (Variant 56)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Basic Calculator (Variant 56)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      556,
      558,
      552
    ],
    "prerequisites": [
      555
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 558,
    "number": 558,
    "title": "Maximal Rectangle (Variant 56)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Maximal Rectangle (Variant 56)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      557,
      559,
      553
    ],
    "prerequisites": [
      556
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 559,
    "number": 559,
    "title": "Task Scheduler (Variant 56)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Task Scheduler (Variant 56)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      558,
      560,
      554
    ],
    "prerequisites": [
      557
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 560,
    "number": 560,
    "title": "Design Circular Queue (Variant 56)",
    "difficulty": "Medium",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Design Circular Queue (Variant 56)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      559,
      561,
      555
    ],
    "prerequisites": [
      558
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 100000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 561,
    "number": 561,
    "title": "Next Greater Element II (Variant 57)",
    "difficulty": "Hard",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Next Greater Element II (Variant 57)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      560,
      562,
      556
    ],
    "prerequisites": [
      559
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 1000000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 562,
    "number": 562,
    "title": "Daily Temperatures II (Variant 57)",
    "difficulty": "Hard",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Daily Temperatures II (Variant 57)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      561,
      563,
      557
    ],
    "prerequisites": [
      560
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 1000000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 563,
    "number": 563,
    "title": "Online Stock Span (Variant 57)",
    "difficulty": "Hard",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Online Stock Span (Variant 57)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      562,
      564,
      558
    ],
    "prerequisites": [
      561
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 1000000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 564,
    "number": 564,
    "title": "Decode String (Variant 57)",
    "difficulty": "Hard",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Decode String (Variant 57)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      563,
      565,
      559
    ],
    "prerequisites": [
      562
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 1000000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 565,
    "number": 565,
    "title": "Remove All Adjacent Duplicates (Variant 57)",
    "difficulty": "Hard",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Remove All Adjacent Duplicates (Variant 57)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      564,
      566,
      560
    ],
    "prerequisites": [
      563
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 1000000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 566,
    "number": 566,
    "title": "Validate Stack Sequences (Variant 57)",
    "difficulty": "Hard",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Validate Stack Sequences (Variant 57)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      565,
      567,
      561
    ],
    "prerequisites": [
      564
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 1000000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 567,
    "number": 567,
    "title": "Basic Calculator (Variant 57)",
    "difficulty": "Hard",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Basic Calculator (Variant 57)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      566,
      568,
      562
    ],
    "prerequisites": [
      565
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 1000000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 568,
    "number": 568,
    "title": "Maximal Rectangle (Variant 57)",
    "difficulty": "Hard",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Maximal Rectangle (Variant 57)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      567,
      569,
      563
    ],
    "prerequisites": [
      566
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 1000000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 569,
    "number": 569,
    "title": "Task Scheduler (Variant 57)",
    "difficulty": "Hard",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Task Scheduler (Variant 57)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      568,
      570,
      564
    ],
    "prerequisites": [
      567
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 1000000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 570,
    "number": 570,
    "title": "Design Circular Queue (Variant 57)",
    "difficulty": "Hard",
    "topic": "Stack & Queue",
    "subtopic": "Monotonic Stack / Deque Core Mechanics",
    "phase": "Phase 8 \u2014 Stack & Queue",
    "pattern": "Monotonic Stack / Deque",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Design Circular Queue (Variant 57)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Monotonic Stack / Deque. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Monotonic Stack / Deque technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Monotonic Stack / Deque)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Monotonic Stack / Deque)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Monotonic Stack / Deque)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Monotonic Stack / Deque)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      569,
      571,
      565
    ],
    "prerequisites": [
      568
    ],
    "tags": [
      "Stack & Queue",
      "Monotonic Stack / Deque",
      "Phase 8",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Monotonic Stack / Deque because constraints are N <= 1000000, making O(N^2) brute force infeasible. Monotonic Stack / Deque leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Monotonic Stack / Deque optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 571,
    "number": 571,
    "title": "Subsets (Variant 58)",
    "difficulty": "Easy",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subsets (Variant 58)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      570,
      572,
      566
    ],
    "prerequisites": [
      569
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 572,
    "number": 572,
    "title": "Subsets II (Variant 58)",
    "difficulty": "Easy",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Subsets II (Variant 58)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      571,
      573,
      567
    ],
    "prerequisites": [
      570
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 573,
    "number": 573,
    "title": "Permutations (Variant 58)",
    "difficulty": "Easy",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Permutations (Variant 58)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      572,
      574,
      568
    ],
    "prerequisites": [
      571
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 574,
    "number": 574,
    "title": "Permutations II (Variant 58)",
    "difficulty": "Easy",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Permutations II (Variant 58)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      573,
      575,
      569
    ],
    "prerequisites": [
      572
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 575,
    "number": 575,
    "title": "Combinations (Variant 58)",
    "difficulty": "Easy",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Combinations (Variant 58)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      574,
      576,
      570
    ],
    "prerequisites": [
      573
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 576,
    "number": 576,
    "title": "Combination Sum (Variant 58)",
    "difficulty": "Easy",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Combination Sum (Variant 58)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      575,
      577,
      571
    ],
    "prerequisites": [
      574
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 577,
    "number": 577,
    "title": "Combination Sum II (Variant 58)",
    "difficulty": "Easy",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Combination Sum II (Variant 58)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      576,
      578,
      572
    ],
    "prerequisites": [
      575
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 578,
    "number": 578,
    "title": "Word Search (Variant 58)",
    "difficulty": "Easy",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Word Search (Variant 58)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      577,
      579,
      573
    ],
    "prerequisites": [
      576
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 579,
    "number": 579,
    "title": "N-Queens (Variant 58)",
    "difficulty": "Easy",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **N-Queens (Variant 58)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      578,
      580,
      574
    ],
    "prerequisites": [
      577
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 580,
    "number": 580,
    "title": "Sudoku Solver (Variant 58)",
    "difficulty": "Easy",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Sudoku Solver (Variant 58)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      579,
      581,
      575
    ],
    "prerequisites": [
      578
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 581,
    "number": 581,
    "title": "Subsets (Variant 59)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subsets (Variant 59)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      580,
      582,
      576
    ],
    "prerequisites": [
      579
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 582,
    "number": 582,
    "title": "Subsets II (Variant 59)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subsets II (Variant 59)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      581,
      583,
      577
    ],
    "prerequisites": [
      580
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 583,
    "number": 583,
    "title": "Permutations (Variant 59)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutations (Variant 59)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      582,
      584,
      578
    ],
    "prerequisites": [
      581
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 584,
    "number": 584,
    "title": "Permutations II (Variant 59)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutations II (Variant 59)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      583,
      585,
      579
    ],
    "prerequisites": [
      582
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 585,
    "number": 585,
    "title": "Combinations (Variant 59)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Combinations (Variant 59)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      584,
      586,
      580
    ],
    "prerequisites": [
      583
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 586,
    "number": 586,
    "title": "Combination Sum (Variant 59)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Combination Sum (Variant 59)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      585,
      587,
      581
    ],
    "prerequisites": [
      584
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 587,
    "number": 587,
    "title": "Combination Sum II (Variant 59)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Combination Sum II (Variant 59)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      586,
      588,
      582
    ],
    "prerequisites": [
      585
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 588,
    "number": 588,
    "title": "Word Search (Variant 59)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Word Search (Variant 59)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      587,
      589,
      583
    ],
    "prerequisites": [
      586
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 589,
    "number": 589,
    "title": "N-Queens (Variant 59)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **N-Queens (Variant 59)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      588,
      590,
      584
    ],
    "prerequisites": [
      587
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 590,
    "number": 590,
    "title": "Sudoku Solver (Variant 59)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sudoku Solver (Variant 59)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      589,
      591,
      585
    ],
    "prerequisites": [
      588
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 591,
    "number": 591,
    "title": "Subsets (Variant 60)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subsets (Variant 60)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      590,
      592,
      586
    ],
    "prerequisites": [
      589
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 592,
    "number": 592,
    "title": "Subsets II (Variant 60)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subsets II (Variant 60)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      591,
      593,
      587
    ],
    "prerequisites": [
      590
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 593,
    "number": 593,
    "title": "Permutations (Variant 60)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutations (Variant 60)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      592,
      594,
      588
    ],
    "prerequisites": [
      591
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 594,
    "number": 594,
    "title": "Permutations II (Variant 60)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutations II (Variant 60)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      593,
      595,
      589
    ],
    "prerequisites": [
      592
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 595,
    "number": 595,
    "title": "Combinations (Variant 60)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Combinations (Variant 60)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      594,
      596,
      590
    ],
    "prerequisites": [
      593
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 596,
    "number": 596,
    "title": "Combination Sum (Variant 60)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Combination Sum (Variant 60)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      595,
      597,
      591
    ],
    "prerequisites": [
      594
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 597,
    "number": 597,
    "title": "Combination Sum II (Variant 60)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Combination Sum II (Variant 60)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      596,
      598,
      592
    ],
    "prerequisites": [
      595
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 598,
    "number": 598,
    "title": "Word Search (Variant 60)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Word Search (Variant 60)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      597,
      599,
      593
    ],
    "prerequisites": [
      596
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 599,
    "number": 599,
    "title": "N-Queens (Variant 60)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **N-Queens (Variant 60)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      598,
      600,
      594
    ],
    "prerequisites": [
      597
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 600,
    "number": 600,
    "title": "Sudoku Solver (Variant 60)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sudoku Solver (Variant 60)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      599,
      601,
      595
    ],
    "prerequisites": [
      598
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 601,
    "number": 601,
    "title": "Subsets (Variant 61)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subsets (Variant 61)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      600,
      602,
      596
    ],
    "prerequisites": [
      599
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 602,
    "number": 602,
    "title": "Subsets II (Variant 61)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subsets II (Variant 61)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      601,
      603,
      597
    ],
    "prerequisites": [
      600
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 603,
    "number": 603,
    "title": "Permutations (Variant 61)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutations (Variant 61)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      602,
      604,
      598
    ],
    "prerequisites": [
      601
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 604,
    "number": 604,
    "title": "Permutations II (Variant 61)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutations II (Variant 61)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      603,
      605,
      599
    ],
    "prerequisites": [
      602
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 605,
    "number": 605,
    "title": "Combinations (Variant 61)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Combinations (Variant 61)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      604,
      606,
      600
    ],
    "prerequisites": [
      603
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 606,
    "number": 606,
    "title": "Combination Sum (Variant 61)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Combination Sum (Variant 61)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      605,
      607,
      601
    ],
    "prerequisites": [
      604
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 607,
    "number": 607,
    "title": "Combination Sum II (Variant 61)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Combination Sum II (Variant 61)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      606,
      608,
      602
    ],
    "prerequisites": [
      605
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 608,
    "number": 608,
    "title": "Word Search (Variant 61)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Word Search (Variant 61)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      607,
      609,
      603
    ],
    "prerequisites": [
      606
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 609,
    "number": 609,
    "title": "N-Queens (Variant 61)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **N-Queens (Variant 61)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      608,
      610,
      604
    ],
    "prerequisites": [
      607
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 610,
    "number": 610,
    "title": "Sudoku Solver (Variant 61)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sudoku Solver (Variant 61)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      609,
      611,
      605
    ],
    "prerequisites": [
      608
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 611,
    "number": 611,
    "title": "Subsets (Variant 62)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subsets (Variant 62)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      610,
      612,
      606
    ],
    "prerequisites": [
      609
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 612,
    "number": 612,
    "title": "Subsets II (Variant 62)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Subsets II (Variant 62)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      611,
      613,
      607
    ],
    "prerequisites": [
      610
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 613,
    "number": 613,
    "title": "Permutations (Variant 62)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutations (Variant 62)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      612,
      614,
      608
    ],
    "prerequisites": [
      611
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 614,
    "number": 614,
    "title": "Permutations II (Variant 62)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Permutations II (Variant 62)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      613,
      615,
      609
    ],
    "prerequisites": [
      612
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 615,
    "number": 615,
    "title": "Combinations (Variant 62)",
    "difficulty": "Medium",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Combinations (Variant 62)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      614,
      616,
      610
    ],
    "prerequisites": [
      613
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 100000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 616,
    "number": 616,
    "title": "Combination Sum (Variant 62)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Combination Sum (Variant 62)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      615,
      617,
      611
    ],
    "prerequisites": [
      614
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 617,
    "number": 617,
    "title": "Combination Sum II (Variant 62)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Combination Sum II (Variant 62)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      616,
      618,
      612
    ],
    "prerequisites": [
      615
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 618,
    "number": 618,
    "title": "Word Search (Variant 62)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Word Search (Variant 62)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      617,
      619,
      613
    ],
    "prerequisites": [
      616
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 619,
    "number": 619,
    "title": "N-Queens (Variant 62)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **N-Queens (Variant 62)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      618,
      620,
      614
    ],
    "prerequisites": [
      617
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 620,
    "number": 620,
    "title": "Sudoku Solver (Variant 62)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Sudoku Solver (Variant 62)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      619,
      621,
      615
    ],
    "prerequisites": [
      618
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 621,
    "number": 621,
    "title": "Subsets (Variant 63)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Subsets (Variant 63)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      620,
      622,
      616
    ],
    "prerequisites": [
      619
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 622,
    "number": 622,
    "title": "Subsets II (Variant 63)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Subsets II (Variant 63)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      621,
      623,
      617
    ],
    "prerequisites": [
      620
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 623,
    "number": 623,
    "title": "Permutations (Variant 63)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Permutations (Variant 63)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      622,
      624,
      618
    ],
    "prerequisites": [
      621
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 624,
    "number": 624,
    "title": "Permutations II (Variant 63)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Permutations II (Variant 63)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      623,
      625,
      619
    ],
    "prerequisites": [
      622
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 625,
    "number": 625,
    "title": "Combinations (Variant 63)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Combinations (Variant 63)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      624,
      626,
      620
    ],
    "prerequisites": [
      623
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 626,
    "number": 626,
    "title": "Combination Sum (Variant 63)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Combination Sum (Variant 63)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      625,
      627,
      621
    ],
    "prerequisites": [
      624
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 627,
    "number": 627,
    "title": "Combination Sum II (Variant 63)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Combination Sum II (Variant 63)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      626,
      628,
      622
    ],
    "prerequisites": [
      625
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 628,
    "number": 628,
    "title": "Word Search (Variant 63)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Word Search (Variant 63)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      627,
      629,
      623
    ],
    "prerequisites": [
      626
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 629,
    "number": 629,
    "title": "N-Queens (Variant 63)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **N-Queens (Variant 63)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      628,
      630,
      624
    ],
    "prerequisites": [
      627
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 630,
    "number": 630,
    "title": "Sudoku Solver (Variant 63)",
    "difficulty": "Hard",
    "topic": "Recursion & Backtracking",
    "subtopic": "Backtracking Core Mechanics",
    "phase": "Phase 9 \u2014 Recursion & Backtracking",
    "pattern": "Backtracking",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Sudoku Solver (Variant 63)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Backtracking. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Backtracking technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Backtracking)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Backtracking)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Backtracking)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Backtracking)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      629,
      631,
      625
    ],
    "prerequisites": [
      628
    ],
    "tags": [
      "Recursion & Backtracking",
      "Backtracking",
      "Phase 9",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Backtracking because constraints are N <= 1000000, making O(N^2) brute force infeasible. Backtracking leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Backtracking optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 631,
    "number": 631,
    "title": "Diameter of Binary Tree (Variant 64)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Diameter of Binary Tree (Variant 64)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      630,
      632,
      626
    ],
    "prerequisites": [
      629
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 632,
    "number": 632,
    "title": "Balanced Binary Tree (Variant 64)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Balanced Binary Tree (Variant 64)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      631,
      633,
      627
    ],
    "prerequisites": [
      630
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 633,
    "number": 633,
    "title": "Path Sum (Variant 64)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Path Sum (Variant 64)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      632,
      634,
      628
    ],
    "prerequisites": [
      631
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 634,
    "number": 634,
    "title": "Path Sum II (Variant 64)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Path Sum II (Variant 64)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      633,
      635,
      629
    ],
    "prerequisites": [
      632
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 635,
    "number": 635,
    "title": "Path Sum III (Variant 64)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Path Sum III (Variant 64)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      634,
      636,
      630
    ],
    "prerequisites": [
      633
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 636,
    "number": 636,
    "title": "Construct Tree Preorder Inorder (Variant 64)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Construct Tree Preorder Inorder (Variant 64)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      635,
      637,
      631
    ],
    "prerequisites": [
      634
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 637,
    "number": 637,
    "title": "Populate Next Right Pointers (Variant 64)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Populate Next Right Pointers (Variant 64)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      636,
      638,
      632
    ],
    "prerequisites": [
      635
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 638,
    "number": 638,
    "title": "Flatten Tree to LinkedList (Variant 64)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Flatten Tree to LinkedList (Variant 64)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      637,
      639,
      633
    ],
    "prerequisites": [
      636
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 639,
    "number": 639,
    "title": "All Nodes Distance K (Variant 64)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **All Nodes Distance K (Variant 64)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      638,
      640,
      634
    ],
    "prerequisites": [
      637
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 640,
    "number": 640,
    "title": "Binary Tree Zigzag Traversal (Variant 64)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Binary Tree Zigzag Traversal (Variant 64)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      639,
      641,
      635
    ],
    "prerequisites": [
      638
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 641,
    "number": 641,
    "title": "Diameter of Binary Tree (Variant 65)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Diameter of Binary Tree (Variant 65)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      640,
      642,
      636
    ],
    "prerequisites": [
      639
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 642,
    "number": 642,
    "title": "Balanced Binary Tree (Variant 65)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Balanced Binary Tree (Variant 65)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      641,
      643,
      637
    ],
    "prerequisites": [
      640
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 643,
    "number": 643,
    "title": "Path Sum (Variant 65)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Path Sum (Variant 65)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      642,
      644,
      638
    ],
    "prerequisites": [
      641
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 644,
    "number": 644,
    "title": "Path Sum II (Variant 65)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Path Sum II (Variant 65)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      643,
      645,
      639
    ],
    "prerequisites": [
      642
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 645,
    "number": 645,
    "title": "Path Sum III (Variant 65)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Path Sum III (Variant 65)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      644,
      646,
      640
    ],
    "prerequisites": [
      643
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 646,
    "number": 646,
    "title": "Construct Tree Preorder Inorder (Variant 65)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Construct Tree Preorder Inorder (Variant 65)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      645,
      647,
      641
    ],
    "prerequisites": [
      644
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 647,
    "number": 647,
    "title": "Populate Next Right Pointers (Variant 65)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Populate Next Right Pointers (Variant 65)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      646,
      648,
      642
    ],
    "prerequisites": [
      645
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 648,
    "number": 648,
    "title": "Flatten Tree to LinkedList (Variant 65)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Flatten Tree to LinkedList (Variant 65)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      647,
      649,
      643
    ],
    "prerequisites": [
      646
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 649,
    "number": 649,
    "title": "All Nodes Distance K (Variant 65)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **All Nodes Distance K (Variant 65)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      648,
      650,
      644
    ],
    "prerequisites": [
      647
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 650,
    "number": 650,
    "title": "Binary Tree Zigzag Traversal (Variant 65)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Binary Tree Zigzag Traversal (Variant 65)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      649,
      651,
      645
    ],
    "prerequisites": [
      648
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 651,
    "number": 651,
    "title": "Diameter of Binary Tree (Variant 66)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Diameter of Binary Tree (Variant 66)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      650,
      652,
      646
    ],
    "prerequisites": [
      649
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 652,
    "number": 652,
    "title": "Balanced Binary Tree (Variant 66)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Balanced Binary Tree (Variant 66)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      651,
      653,
      647
    ],
    "prerequisites": [
      650
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 653,
    "number": 653,
    "title": "Path Sum (Variant 66)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Path Sum (Variant 66)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      652,
      654,
      648
    ],
    "prerequisites": [
      651
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 654,
    "number": 654,
    "title": "Path Sum II (Variant 66)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Path Sum II (Variant 66)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      653,
      655,
      649
    ],
    "prerequisites": [
      652
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 655,
    "number": 655,
    "title": "Path Sum III (Variant 66)",
    "difficulty": "Easy",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Path Sum III (Variant 66)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      654,
      656,
      650
    ],
    "prerequisites": [
      653
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 656,
    "number": 656,
    "title": "Construct Tree Preorder Inorder (Variant 66)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Construct Tree Preorder Inorder (Variant 66)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      655,
      657,
      651
    ],
    "prerequisites": [
      654
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 657,
    "number": 657,
    "title": "Populate Next Right Pointers (Variant 66)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Populate Next Right Pointers (Variant 66)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      656,
      658,
      652
    ],
    "prerequisites": [
      655
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 658,
    "number": 658,
    "title": "Flatten Tree to LinkedList (Variant 66)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Flatten Tree to LinkedList (Variant 66)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      657,
      659,
      653
    ],
    "prerequisites": [
      656
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 659,
    "number": 659,
    "title": "All Nodes Distance K (Variant 66)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **All Nodes Distance K (Variant 66)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      658,
      660,
      654
    ],
    "prerequisites": [
      657
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 660,
    "number": 660,
    "title": "Binary Tree Zigzag Traversal (Variant 66)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Binary Tree Zigzag Traversal (Variant 66)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      659,
      661,
      655
    ],
    "prerequisites": [
      658
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 661,
    "number": 661,
    "title": "Diameter of Binary Tree (Variant 67)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Diameter of Binary Tree (Variant 67)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      660,
      662,
      656
    ],
    "prerequisites": [
      659
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 662,
    "number": 662,
    "title": "Balanced Binary Tree (Variant 67)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Balanced Binary Tree (Variant 67)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      661,
      663,
      657
    ],
    "prerequisites": [
      660
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 663,
    "number": 663,
    "title": "Path Sum (Variant 67)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum (Variant 67)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      662,
      664,
      658
    ],
    "prerequisites": [
      661
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 664,
    "number": 664,
    "title": "Path Sum II (Variant 67)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum II (Variant 67)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      663,
      665,
      659
    ],
    "prerequisites": [
      662
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 665,
    "number": 665,
    "title": "Path Sum III (Variant 67)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum III (Variant 67)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      664,
      666,
      660
    ],
    "prerequisites": [
      663
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 666,
    "number": 666,
    "title": "Construct Tree Preorder Inorder (Variant 67)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Construct Tree Preorder Inorder (Variant 67)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      665,
      667,
      661
    ],
    "prerequisites": [
      664
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 667,
    "number": 667,
    "title": "Populate Next Right Pointers (Variant 67)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Populate Next Right Pointers (Variant 67)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      666,
      668,
      662
    ],
    "prerequisites": [
      665
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 668,
    "number": 668,
    "title": "Flatten Tree to LinkedList (Variant 67)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Flatten Tree to LinkedList (Variant 67)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      667,
      669,
      663
    ],
    "prerequisites": [
      666
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 669,
    "number": 669,
    "title": "All Nodes Distance K (Variant 67)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **All Nodes Distance K (Variant 67)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      668,
      670,
      664
    ],
    "prerequisites": [
      667
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 670,
    "number": 670,
    "title": "Binary Tree Zigzag Traversal (Variant 67)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Binary Tree Zigzag Traversal (Variant 67)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      669,
      671,
      665
    ],
    "prerequisites": [
      668
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 671,
    "number": 671,
    "title": "Diameter of Binary Tree (Variant 68)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Diameter of Binary Tree (Variant 68)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      670,
      672,
      666
    ],
    "prerequisites": [
      669
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 672,
    "number": 672,
    "title": "Balanced Binary Tree (Variant 68)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Balanced Binary Tree (Variant 68)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      671,
      673,
      667
    ],
    "prerequisites": [
      670
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 673,
    "number": 673,
    "title": "Path Sum (Variant 68)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum (Variant 68)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      672,
      674,
      668
    ],
    "prerequisites": [
      671
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 674,
    "number": 674,
    "title": "Path Sum II (Variant 68)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum II (Variant 68)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      673,
      675,
      669
    ],
    "prerequisites": [
      672
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 675,
    "number": 675,
    "title": "Path Sum III (Variant 68)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum III (Variant 68)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      674,
      676,
      670
    ],
    "prerequisites": [
      673
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 676,
    "number": 676,
    "title": "Construct Tree Preorder Inorder (Variant 68)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Construct Tree Preorder Inorder (Variant 68)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      675,
      677,
      671
    ],
    "prerequisites": [
      674
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 677,
    "number": 677,
    "title": "Populate Next Right Pointers (Variant 68)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Populate Next Right Pointers (Variant 68)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      676,
      678,
      672
    ],
    "prerequisites": [
      675
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 678,
    "number": 678,
    "title": "Flatten Tree to LinkedList (Variant 68)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Flatten Tree to LinkedList (Variant 68)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      677,
      679,
      673
    ],
    "prerequisites": [
      676
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 679,
    "number": 679,
    "title": "All Nodes Distance K (Variant 68)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **All Nodes Distance K (Variant 68)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      678,
      680,
      674
    ],
    "prerequisites": [
      677
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 680,
    "number": 680,
    "title": "Binary Tree Zigzag Traversal (Variant 68)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Binary Tree Zigzag Traversal (Variant 68)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      679,
      681,
      675
    ],
    "prerequisites": [
      678
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 681,
    "number": 681,
    "title": "Diameter of Binary Tree (Variant 69)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Diameter of Binary Tree (Variant 69)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      680,
      682,
      676
    ],
    "prerequisites": [
      679
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 682,
    "number": 682,
    "title": "Balanced Binary Tree (Variant 69)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Balanced Binary Tree (Variant 69)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      681,
      683,
      677
    ],
    "prerequisites": [
      680
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 683,
    "number": 683,
    "title": "Path Sum (Variant 69)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum (Variant 69)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      682,
      684,
      678
    ],
    "prerequisites": [
      681
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 684,
    "number": 684,
    "title": "Path Sum II (Variant 69)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum II (Variant 69)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      683,
      685,
      679
    ],
    "prerequisites": [
      682
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 685,
    "number": 685,
    "title": "Path Sum III (Variant 69)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum III (Variant 69)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      684,
      686,
      680
    ],
    "prerequisites": [
      683
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 686,
    "number": 686,
    "title": "Construct Tree Preorder Inorder (Variant 69)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Construct Tree Preorder Inorder (Variant 69)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      685,
      687,
      681
    ],
    "prerequisites": [
      684
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 687,
    "number": 687,
    "title": "Populate Next Right Pointers (Variant 69)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Populate Next Right Pointers (Variant 69)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      686,
      688,
      682
    ],
    "prerequisites": [
      685
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 688,
    "number": 688,
    "title": "Flatten Tree to LinkedList (Variant 69)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Flatten Tree to LinkedList (Variant 69)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      687,
      689,
      683
    ],
    "prerequisites": [
      686
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 689,
    "number": 689,
    "title": "All Nodes Distance K (Variant 69)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **All Nodes Distance K (Variant 69)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      688,
      690,
      684
    ],
    "prerequisites": [
      687
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 690,
    "number": 690,
    "title": "Binary Tree Zigzag Traversal (Variant 69)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Binary Tree Zigzag Traversal (Variant 69)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      689,
      691,
      685
    ],
    "prerequisites": [
      688
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 691,
    "number": 691,
    "title": "Diameter of Binary Tree (Variant 70)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Diameter of Binary Tree (Variant 70)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      690,
      692,
      686
    ],
    "prerequisites": [
      689
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 692,
    "number": 692,
    "title": "Balanced Binary Tree (Variant 70)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Balanced Binary Tree (Variant 70)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      691,
      693,
      687
    ],
    "prerequisites": [
      690
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 693,
    "number": 693,
    "title": "Path Sum (Variant 70)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum (Variant 70)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      692,
      694,
      688
    ],
    "prerequisites": [
      691
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 694,
    "number": 694,
    "title": "Path Sum II (Variant 70)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum II (Variant 70)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      693,
      695,
      689
    ],
    "prerequisites": [
      692
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 695,
    "number": 695,
    "title": "Path Sum III (Variant 70)",
    "difficulty": "Medium",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Path Sum III (Variant 70)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      694,
      696,
      690
    ],
    "prerequisites": [
      693
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 100000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 696,
    "number": 696,
    "title": "Construct Tree Preorder Inorder (Variant 70)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Construct Tree Preorder Inorder (Variant 70)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      695,
      697,
      691
    ],
    "prerequisites": [
      694
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 697,
    "number": 697,
    "title": "Populate Next Right Pointers (Variant 70)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Populate Next Right Pointers (Variant 70)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      696,
      698,
      692
    ],
    "prerequisites": [
      695
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 698,
    "number": 698,
    "title": "Flatten Tree to LinkedList (Variant 70)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Flatten Tree to LinkedList (Variant 70)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      697,
      699,
      693
    ],
    "prerequisites": [
      696
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 699,
    "number": 699,
    "title": "All Nodes Distance K (Variant 70)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **All Nodes Distance K (Variant 70)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      698,
      700,
      694
    ],
    "prerequisites": [
      697
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 700,
    "number": 700,
    "title": "Binary Tree Zigzag Traversal (Variant 70)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Binary Tree Zigzag Traversal (Variant 70)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      699,
      701,
      695
    ],
    "prerequisites": [
      698
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 701,
    "number": 701,
    "title": "Diameter of Binary Tree (Variant 71)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Diameter of Binary Tree (Variant 71)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      700,
      702,
      696
    ],
    "prerequisites": [
      699
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 702,
    "number": 702,
    "title": "Balanced Binary Tree (Variant 71)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Balanced Binary Tree (Variant 71)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      701,
      703,
      697
    ],
    "prerequisites": [
      700
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 703,
    "number": 703,
    "title": "Path Sum (Variant 71)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Path Sum (Variant 71)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      702,
      704,
      698
    ],
    "prerequisites": [
      701
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 704,
    "number": 704,
    "title": "Path Sum II (Variant 71)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Path Sum II (Variant 71)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      703,
      705,
      699
    ],
    "prerequisites": [
      702
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 705,
    "number": 705,
    "title": "Path Sum III (Variant 71)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Path Sum III (Variant 71)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      704,
      706,
      700
    ],
    "prerequisites": [
      703
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 706,
    "number": 706,
    "title": "Construct Tree Preorder Inorder (Variant 71)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Construct Tree Preorder Inorder (Variant 71)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      705,
      707,
      701
    ],
    "prerequisites": [
      704
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 707,
    "number": 707,
    "title": "Populate Next Right Pointers (Variant 71)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Populate Next Right Pointers (Variant 71)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      706,
      708,
      702
    ],
    "prerequisites": [
      705
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 708,
    "number": 708,
    "title": "Flatten Tree to LinkedList (Variant 71)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Flatten Tree to LinkedList (Variant 71)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      707,
      709,
      703
    ],
    "prerequisites": [
      706
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 709,
    "number": 709,
    "title": "All Nodes Distance K (Variant 71)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **All Nodes Distance K (Variant 71)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      708,
      710,
      704
    ],
    "prerequisites": [
      707
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 710,
    "number": 710,
    "title": "Binary Tree Zigzag Traversal (Variant 71)",
    "difficulty": "Hard",
    "topic": "Trees",
    "subtopic": "DFS / BFS / Tree Traversals Core Mechanics",
    "phase": "Phase 10 \u2014 Trees & BST",
    "pattern": "DFS / BFS / Tree Traversals",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Binary Tree Zigzag Traversal (Variant 71)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of DFS / BFS / Tree Traversals. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the DFS / BFS / Tree Traversals technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (DFS / BFS / Tree Traversals)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (DFS / BFS / Tree Traversals)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (DFS / BFS / Tree Traversals)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (DFS / BFS / Tree Traversals)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      709,
      711,
      705
    ],
    "prerequisites": [
      708
    ],
    "tags": [
      "Trees",
      "DFS / BFS / Tree Traversals",
      "Phase 10",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects DFS / BFS / Tree Traversals because constraints are N <= 1000000, making O(N^2) brute force infeasible. DFS / BFS / Tree Traversals leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose DFS / BFS / Tree Traversals optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 711,
    "number": 711,
    "title": "Kth Largest Element in Stream (Variant 72)",
    "difficulty": "Easy",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Kth Largest Element in Stream (Variant 72)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      710,
      712,
      706
    ],
    "prerequisites": [
      709
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 712,
    "number": 712,
    "title": "Top K Frequent Elements (Variant 72)",
    "difficulty": "Easy",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Top K Frequent Elements (Variant 72)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      711,
      713,
      707
    ],
    "prerequisites": [
      710
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 713,
    "number": 713,
    "title": "K Closest Points to Origin (Variant 72)",
    "difficulty": "Easy",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **K Closest Points to Origin (Variant 72)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      712,
      714,
      708
    ],
    "prerequisites": [
      711
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 714,
    "number": 714,
    "title": "Reorganize String (Variant 72)",
    "difficulty": "Easy",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Reorganize String (Variant 72)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      713,
      715,
      709
    ],
    "prerequisites": [
      712
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 715,
    "number": 715,
    "title": "Find Median from Data Stream (Variant 72)",
    "difficulty": "Easy",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Find Median from Data Stream (Variant 72)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      714,
      716,
      710
    ],
    "prerequisites": [
      713
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 716,
    "number": 716,
    "title": "Smallest Range Covering Elements (Variant 72)",
    "difficulty": "Easy",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Smallest Range Covering Elements (Variant 72)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      715,
      717,
      711
    ],
    "prerequisites": [
      714
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 717,
    "number": 717,
    "title": "Minimum Cost to Hire K Workers (Variant 72)",
    "difficulty": "Easy",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Minimum Cost to Hire K Workers (Variant 72)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      716,
      718,
      712
    ],
    "prerequisites": [
      715
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 718,
    "number": 718,
    "title": "Distant Barcodes (Variant 72)",
    "difficulty": "Easy",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Distant Barcodes (Variant 72)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      717,
      719,
      713
    ],
    "prerequisites": [
      716
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 719,
    "number": 719,
    "title": "Single-Threaded CPU (Variant 72)",
    "difficulty": "Easy",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Single-Threaded CPU (Variant 72)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      718,
      720,
      714
    ],
    "prerequisites": [
      717
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 720,
    "number": 720,
    "title": "IPO (Variant 72)",
    "difficulty": "Easy",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **IPO (Variant 72)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      719,
      721,
      715
    ],
    "prerequisites": [
      718
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 721,
    "number": 721,
    "title": "Kth Largest Element in Stream (Variant 73)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Kth Largest Element in Stream (Variant 73)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      720,
      722,
      716
    ],
    "prerequisites": [
      719
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 722,
    "number": 722,
    "title": "Top K Frequent Elements (Variant 73)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Top K Frequent Elements (Variant 73)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      721,
      723,
      717
    ],
    "prerequisites": [
      720
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 723,
    "number": 723,
    "title": "K Closest Points to Origin (Variant 73)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **K Closest Points to Origin (Variant 73)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      722,
      724,
      718
    ],
    "prerequisites": [
      721
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 724,
    "number": 724,
    "title": "Reorganize String (Variant 73)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Reorganize String (Variant 73)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      723,
      725,
      719
    ],
    "prerequisites": [
      722
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 725,
    "number": 725,
    "title": "Find Median from Data Stream (Variant 73)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find Median from Data Stream (Variant 73)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      724,
      726,
      720
    ],
    "prerequisites": [
      723
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 726,
    "number": 726,
    "title": "Smallest Range Covering Elements (Variant 73)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Smallest Range Covering Elements (Variant 73)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      725,
      727,
      721
    ],
    "prerequisites": [
      724
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 727,
    "number": 727,
    "title": "Minimum Cost to Hire K Workers (Variant 73)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Cost to Hire K Workers (Variant 73)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      726,
      728,
      722
    ],
    "prerequisites": [
      725
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 728,
    "number": 728,
    "title": "Distant Barcodes (Variant 73)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Distant Barcodes (Variant 73)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      727,
      729,
      723
    ],
    "prerequisites": [
      726
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 729,
    "number": 729,
    "title": "Single-Threaded CPU (Variant 73)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Single-Threaded CPU (Variant 73)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      728,
      730,
      724
    ],
    "prerequisites": [
      727
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 730,
    "number": 730,
    "title": "IPO (Variant 73)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **IPO (Variant 73)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      729,
      731,
      725
    ],
    "prerequisites": [
      728
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 731,
    "number": 731,
    "title": "Kth Largest Element in Stream (Variant 74)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Kth Largest Element in Stream (Variant 74)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      730,
      732,
      726
    ],
    "prerequisites": [
      729
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 732,
    "number": 732,
    "title": "Top K Frequent Elements (Variant 74)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Top K Frequent Elements (Variant 74)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      731,
      733,
      727
    ],
    "prerequisites": [
      730
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 733,
    "number": 733,
    "title": "K Closest Points to Origin (Variant 74)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **K Closest Points to Origin (Variant 74)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      732,
      734,
      728
    ],
    "prerequisites": [
      731
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 734,
    "number": 734,
    "title": "Reorganize String (Variant 74)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Reorganize String (Variant 74)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      733,
      735,
      729
    ],
    "prerequisites": [
      732
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 735,
    "number": 735,
    "title": "Find Median from Data Stream (Variant 74)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Find Median from Data Stream (Variant 74)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      734,
      736,
      730
    ],
    "prerequisites": [
      733
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 736,
    "number": 736,
    "title": "Smallest Range Covering Elements (Variant 74)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Smallest Range Covering Elements (Variant 74)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      735,
      737,
      731
    ],
    "prerequisites": [
      734
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 737,
    "number": 737,
    "title": "Minimum Cost to Hire K Workers (Variant 74)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Cost to Hire K Workers (Variant 74)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      736,
      738,
      732
    ],
    "prerequisites": [
      735
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 738,
    "number": 738,
    "title": "Distant Barcodes (Variant 74)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Distant Barcodes (Variant 74)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      737,
      739,
      733
    ],
    "prerequisites": [
      736
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 739,
    "number": 739,
    "title": "Single-Threaded CPU (Variant 74)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Single-Threaded CPU (Variant 74)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      738,
      740,
      734
    ],
    "prerequisites": [
      737
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 740,
    "number": 740,
    "title": "IPO (Variant 74)",
    "difficulty": "Medium",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **IPO (Variant 74)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      739,
      741,
      735
    ],
    "prerequisites": [
      738
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 100000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 741,
    "number": 741,
    "title": "Kth Largest Element in Stream (Variant 75)",
    "difficulty": "Hard",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Kth Largest Element in Stream (Variant 75)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      740,
      742,
      736
    ],
    "prerequisites": [
      739
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 1000000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 742,
    "number": 742,
    "title": "Top K Frequent Elements (Variant 75)",
    "difficulty": "Hard",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Top K Frequent Elements (Variant 75)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      741,
      743,
      737
    ],
    "prerequisites": [
      740
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 1000000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 743,
    "number": 743,
    "title": "K Closest Points to Origin (Variant 75)",
    "difficulty": "Hard",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **K Closest Points to Origin (Variant 75)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      742,
      744,
      738
    ],
    "prerequisites": [
      741
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 1000000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 744,
    "number": 744,
    "title": "Reorganize String (Variant 75)",
    "difficulty": "Hard",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Reorganize String (Variant 75)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      743,
      745,
      739
    ],
    "prerequisites": [
      742
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 1000000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 745,
    "number": 745,
    "title": "Find Median from Data Stream (Variant 75)",
    "difficulty": "Hard",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Find Median from Data Stream (Variant 75)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      744,
      746,
      740
    ],
    "prerequisites": [
      743
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 1000000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 746,
    "number": 746,
    "title": "Smallest Range Covering Elements (Variant 75)",
    "difficulty": "Hard",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Smallest Range Covering Elements (Variant 75)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      745,
      747,
      741
    ],
    "prerequisites": [
      744
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 1000000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 747,
    "number": 747,
    "title": "Minimum Cost to Hire K Workers (Variant 75)",
    "difficulty": "Hard",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Minimum Cost to Hire K Workers (Variant 75)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      746,
      748,
      742
    ],
    "prerequisites": [
      745
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 1000000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 748,
    "number": 748,
    "title": "Distant Barcodes (Variant 75)",
    "difficulty": "Hard",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Distant Barcodes (Variant 75)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      747,
      749,
      743
    ],
    "prerequisites": [
      746
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 1000000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 749,
    "number": 749,
    "title": "Single-Threaded CPU (Variant 75)",
    "difficulty": "Hard",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Single-Threaded CPU (Variant 75)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      748,
      750,
      744
    ],
    "prerequisites": [
      747
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 1000000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 750,
    "number": 750,
    "title": "IPO (Variant 75)",
    "difficulty": "Hard",
    "topic": "Heap / Priority Queue",
    "subtopic": "Top K Elements / Two Heaps Core Mechanics",
    "phase": "Phase 11 \u2014 Heap / Priority Queue",
    "pattern": "Top K Elements / Two Heaps",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **IPO (Variant 75)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Top K Elements / Two Heaps. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Top K Elements / Two Heaps technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Top K Elements / Two Heaps)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Top K Elements / Two Heaps)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Top K Elements / Two Heaps)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Top K Elements / Two Heaps)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      749,
      751,
      745
    ],
    "prerequisites": [
      748
    ],
    "tags": [
      "Heap / Priority Queue",
      "Top K Elements / Two Heaps",
      "Phase 11",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Top K Elements / Two Heaps because constraints are N <= 1000000, making O(N^2) brute force infeasible. Top K Elements / Two Heaps leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Top K Elements / Two Heaps optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 751,
    "number": 751,
    "title": "Assign Cookies (Variant 76)",
    "difficulty": "Easy",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Assign Cookies (Variant 76)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      750,
      752,
      746
    ],
    "prerequisites": [
      749
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 752,
    "number": 752,
    "title": "Lemonade Change (Variant 76)",
    "difficulty": "Easy",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Lemonade Change (Variant 76)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      751,
      753,
      747
    ],
    "prerequisites": [
      750
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 753,
    "number": 753,
    "title": "Jump Game II (Variant 76)",
    "difficulty": "Easy",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Jump Game II (Variant 76)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      752,
      754,
      748
    ],
    "prerequisites": [
      751
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 754,
    "number": 754,
    "title": "Non-overlapping Intervals (Variant 76)",
    "difficulty": "Easy",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Non-overlapping Intervals (Variant 76)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      753,
      755,
      749
    ],
    "prerequisites": [
      752
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 755,
    "number": 755,
    "title": "Minimum Number of Arrows (Variant 76)",
    "difficulty": "Easy",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Minimum Number of Arrows (Variant 76)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      754,
      756,
      750
    ],
    "prerequisites": [
      753
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 756,
    "number": 756,
    "title": "Candy (Variant 76)",
    "difficulty": "Easy",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Candy (Variant 76)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      755,
      757,
      751
    ],
    "prerequisites": [
      754
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 757,
    "number": 757,
    "title": "Gas Station (Variant 76)",
    "difficulty": "Easy",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Gas Station (Variant 76)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      756,
      758,
      752
    ],
    "prerequisites": [
      755
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 758,
    "number": 758,
    "title": "Partition Labels (Variant 76)",
    "difficulty": "Easy",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Partition Labels (Variant 76)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      757,
      759,
      753
    ],
    "prerequisites": [
      756
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 759,
    "number": 759,
    "title": "Queue Reconstruction by Height (Variant 76)",
    "difficulty": "Easy",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Queue Reconstruction by Height (Variant 76)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      758,
      760,
      754
    ],
    "prerequisites": [
      757
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 760,
    "number": 760,
    "title": "Task Scheduler Greedy (Variant 76)",
    "difficulty": "Easy",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Task Scheduler Greedy (Variant 76)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      759,
      761,
      755
    ],
    "prerequisites": [
      758
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 761,
    "number": 761,
    "title": "Assign Cookies (Variant 77)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Assign Cookies (Variant 77)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      760,
      762,
      756
    ],
    "prerequisites": [
      759
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 762,
    "number": 762,
    "title": "Lemonade Change (Variant 77)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Lemonade Change (Variant 77)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      761,
      763,
      757
    ],
    "prerequisites": [
      760
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 763,
    "number": 763,
    "title": "Jump Game II (Variant 77)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Jump Game II (Variant 77)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      762,
      764,
      758
    ],
    "prerequisites": [
      761
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 764,
    "number": 764,
    "title": "Non-overlapping Intervals (Variant 77)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Non-overlapping Intervals (Variant 77)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      763,
      765,
      759
    ],
    "prerequisites": [
      762
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 765,
    "number": 765,
    "title": "Minimum Number of Arrows (Variant 77)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Number of Arrows (Variant 77)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      764,
      766,
      760
    ],
    "prerequisites": [
      763
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 766,
    "number": 766,
    "title": "Candy (Variant 77)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Candy (Variant 77)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      765,
      767,
      761
    ],
    "prerequisites": [
      764
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 767,
    "number": 767,
    "title": "Gas Station (Variant 77)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Gas Station (Variant 77)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      766,
      768,
      762
    ],
    "prerequisites": [
      765
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 768,
    "number": 768,
    "title": "Partition Labels (Variant 77)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Partition Labels (Variant 77)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      767,
      769,
      763
    ],
    "prerequisites": [
      766
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 769,
    "number": 769,
    "title": "Queue Reconstruction by Height (Variant 77)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Queue Reconstruction by Height (Variant 77)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      768,
      770,
      764
    ],
    "prerequisites": [
      767
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 770,
    "number": 770,
    "title": "Task Scheduler Greedy (Variant 77)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Task Scheduler Greedy (Variant 77)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      769,
      771,
      765
    ],
    "prerequisites": [
      768
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 771,
    "number": 771,
    "title": "Assign Cookies (Variant 78)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Assign Cookies (Variant 78)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      770,
      772,
      766
    ],
    "prerequisites": [
      769
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 772,
    "number": 772,
    "title": "Lemonade Change (Variant 78)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Lemonade Change (Variant 78)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      771,
      773,
      767
    ],
    "prerequisites": [
      770
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 773,
    "number": 773,
    "title": "Jump Game II (Variant 78)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Jump Game II (Variant 78)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      772,
      774,
      768
    ],
    "prerequisites": [
      771
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 774,
    "number": 774,
    "title": "Non-overlapping Intervals (Variant 78)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Non-overlapping Intervals (Variant 78)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      773,
      775,
      769
    ],
    "prerequisites": [
      772
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 775,
    "number": 775,
    "title": "Minimum Number of Arrows (Variant 78)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Number of Arrows (Variant 78)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      774,
      776,
      770
    ],
    "prerequisites": [
      773
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 776,
    "number": 776,
    "title": "Candy (Variant 78)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Candy (Variant 78)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      775,
      777,
      771
    ],
    "prerequisites": [
      774
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 777,
    "number": 777,
    "title": "Gas Station (Variant 78)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Gas Station (Variant 78)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      776,
      778,
      772
    ],
    "prerequisites": [
      775
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 778,
    "number": 778,
    "title": "Partition Labels (Variant 78)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Partition Labels (Variant 78)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      777,
      779,
      773
    ],
    "prerequisites": [
      776
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 779,
    "number": 779,
    "title": "Queue Reconstruction by Height (Variant 78)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Queue Reconstruction by Height (Variant 78)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      778,
      780,
      774
    ],
    "prerequisites": [
      777
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 780,
    "number": 780,
    "title": "Task Scheduler Greedy (Variant 78)",
    "difficulty": "Medium",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Task Scheduler Greedy (Variant 78)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      779,
      781,
      775
    ],
    "prerequisites": [
      778
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 100000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 781,
    "number": 781,
    "title": "Assign Cookies (Variant 79)",
    "difficulty": "Hard",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Assign Cookies (Variant 79)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      780,
      782,
      776
    ],
    "prerequisites": [
      779
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 1000000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 782,
    "number": 782,
    "title": "Lemonade Change (Variant 79)",
    "difficulty": "Hard",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Lemonade Change (Variant 79)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      781,
      783,
      777
    ],
    "prerequisites": [
      780
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 1000000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 783,
    "number": 783,
    "title": "Jump Game II (Variant 79)",
    "difficulty": "Hard",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Jump Game II (Variant 79)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      782,
      784,
      778
    ],
    "prerequisites": [
      781
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 1000000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 784,
    "number": 784,
    "title": "Non-overlapping Intervals (Variant 79)",
    "difficulty": "Hard",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Non-overlapping Intervals (Variant 79)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      783,
      785,
      779
    ],
    "prerequisites": [
      782
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 1000000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 785,
    "number": 785,
    "title": "Minimum Number of Arrows (Variant 79)",
    "difficulty": "Hard",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Minimum Number of Arrows (Variant 79)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      784,
      786,
      780
    ],
    "prerequisites": [
      783
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 1000000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 786,
    "number": 786,
    "title": "Candy (Variant 79)",
    "difficulty": "Hard",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Candy (Variant 79)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      785,
      787,
      781
    ],
    "prerequisites": [
      784
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 1000000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 787,
    "number": 787,
    "title": "Gas Station (Variant 79)",
    "difficulty": "Hard",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Gas Station (Variant 79)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      786,
      788,
      782
    ],
    "prerequisites": [
      785
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 1000000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 788,
    "number": 788,
    "title": "Partition Labels (Variant 79)",
    "difficulty": "Hard",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Partition Labels (Variant 79)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      787,
      789,
      783
    ],
    "prerequisites": [
      786
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 1000000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 789,
    "number": 789,
    "title": "Queue Reconstruction by Height (Variant 79)",
    "difficulty": "Hard",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Queue Reconstruction by Height (Variant 79)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      788,
      790,
      784
    ],
    "prerequisites": [
      787
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 1000000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 790,
    "number": 790,
    "title": "Task Scheduler Greedy (Variant 79)",
    "difficulty": "Hard",
    "topic": "Greedy",
    "subtopic": "Greedy Choice Property Core Mechanics",
    "phase": "Phase 12 \u2014 Greedy Algorithms",
    "pattern": "Greedy Choice Property",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Task Scheduler Greedy (Variant 79)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Greedy Choice Property. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Greedy Choice Property technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Greedy Choice Property)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Greedy Choice Property)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Greedy Choice Property)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Greedy Choice Property)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      789,
      791,
      785
    ],
    "prerequisites": [
      788
    ],
    "tags": [
      "Greedy",
      "Greedy Choice Property",
      "Phase 12",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Greedy Choice Property because constraints are N <= 1000000, making O(N^2) brute force infeasible. Greedy Choice Property leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Greedy Choice Property optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 791,
    "number": 791,
    "title": "Is Graph Bipartite (Variant 80)",
    "difficulty": "Easy",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Is Graph Bipartite (Variant 80)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      790,
      792,
      786
    ],
    "prerequisites": [
      789
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 792,
    "number": 792,
    "title": "Course Schedule II (Variant 80)",
    "difficulty": "Easy",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Course Schedule II (Variant 80)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      791,
      793,
      787
    ],
    "prerequisites": [
      790
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 793,
    "number": 793,
    "title": "Surrounded Regions (Variant 80)",
    "difficulty": "Easy",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Surrounded Regions (Variant 80)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      792,
      794,
      788
    ],
    "prerequisites": [
      791
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 794,
    "number": 794,
    "title": "Cheapest Flights Within K Stops (Variant 80)",
    "difficulty": "Easy",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Cheapest Flights Within K Stops (Variant 80)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      793,
      795,
      789
    ],
    "prerequisites": [
      792
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 795,
    "number": 795,
    "title": "Network Delay Time (Variant 80)",
    "difficulty": "Easy",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Network Delay Time (Variant 80)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      794,
      796,
      790
    ],
    "prerequisites": [
      793
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 796,
    "number": 796,
    "title": "As Far from Land as Possible (Variant 80)",
    "difficulty": "Easy",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **As Far from Land as Possible (Variant 80)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      795,
      797,
      791
    ],
    "prerequisites": [
      794
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 797,
    "number": 797,
    "title": "Minimum Height Trees (Variant 80)",
    "difficulty": "Easy",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Minimum Height Trees (Variant 80)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      796,
      798,
      792
    ],
    "prerequisites": [
      795
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 798,
    "number": 798,
    "title": "Critical Connections in Network (Variant 80)",
    "difficulty": "Easy",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Critical Connections in Network (Variant 80)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      797,
      799,
      793
    ],
    "prerequisites": [
      796
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 799,
    "number": 799,
    "title": "Swim in Rising Water (Variant 80)",
    "difficulty": "Easy",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Swim in Rising Water (Variant 80)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      798,
      800,
      794
    ],
    "prerequisites": [
      797
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 800,
    "number": 800,
    "title": "Evaluate Division (Variant 80)",
    "difficulty": "Easy",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Evaluate Division (Variant 80)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      799,
      801,
      795
    ],
    "prerequisites": [
      798
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 801,
    "number": 801,
    "title": "Is Graph Bipartite (Variant 81)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Is Graph Bipartite (Variant 81)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      800,
      802,
      796
    ],
    "prerequisites": [
      799
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 802,
    "number": 802,
    "title": "Course Schedule II (Variant 81)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Course Schedule II (Variant 81)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      801,
      803,
      797
    ],
    "prerequisites": [
      800
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 803,
    "number": 803,
    "title": "Surrounded Regions (Variant 81)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Surrounded Regions (Variant 81)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      802,
      804,
      798
    ],
    "prerequisites": [
      801
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 804,
    "number": 804,
    "title": "Cheapest Flights Within K Stops (Variant 81)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Cheapest Flights Within K Stops (Variant 81)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      803,
      805,
      799
    ],
    "prerequisites": [
      802
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 805,
    "number": 805,
    "title": "Network Delay Time (Variant 81)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Network Delay Time (Variant 81)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      804,
      806,
      800
    ],
    "prerequisites": [
      803
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 806,
    "number": 806,
    "title": "As Far from Land as Possible (Variant 81)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **As Far from Land as Possible (Variant 81)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      805,
      807,
      801
    ],
    "prerequisites": [
      804
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 807,
    "number": 807,
    "title": "Minimum Height Trees (Variant 81)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Height Trees (Variant 81)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      806,
      808,
      802
    ],
    "prerequisites": [
      805
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 808,
    "number": 808,
    "title": "Critical Connections in Network (Variant 81)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Critical Connections in Network (Variant 81)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      807,
      809,
      803
    ],
    "prerequisites": [
      806
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 809,
    "number": 809,
    "title": "Swim in Rising Water (Variant 81)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Swim in Rising Water (Variant 81)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      808,
      810,
      804
    ],
    "prerequisites": [
      807
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 810,
    "number": 810,
    "title": "Evaluate Division (Variant 81)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Evaluate Division (Variant 81)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      809,
      811,
      805
    ],
    "prerequisites": [
      808
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 811,
    "number": 811,
    "title": "Is Graph Bipartite (Variant 82)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Is Graph Bipartite (Variant 82)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      810,
      812,
      806
    ],
    "prerequisites": [
      809
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 812,
    "number": 812,
    "title": "Course Schedule II (Variant 82)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Course Schedule II (Variant 82)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      811,
      813,
      807
    ],
    "prerequisites": [
      810
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 813,
    "number": 813,
    "title": "Surrounded Regions (Variant 82)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Surrounded Regions (Variant 82)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      812,
      814,
      808
    ],
    "prerequisites": [
      811
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 814,
    "number": 814,
    "title": "Cheapest Flights Within K Stops (Variant 82)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Cheapest Flights Within K Stops (Variant 82)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      813,
      815,
      809
    ],
    "prerequisites": [
      812
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 815,
    "number": 815,
    "title": "Network Delay Time (Variant 82)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Network Delay Time (Variant 82)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      814,
      816,
      810
    ],
    "prerequisites": [
      813
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 816,
    "number": 816,
    "title": "As Far from Land as Possible (Variant 82)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **As Far from Land as Possible (Variant 82)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      815,
      817,
      811
    ],
    "prerequisites": [
      814
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 817,
    "number": 817,
    "title": "Minimum Height Trees (Variant 82)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Height Trees (Variant 82)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      816,
      818,
      812
    ],
    "prerequisites": [
      815
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 818,
    "number": 818,
    "title": "Critical Connections in Network (Variant 82)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Critical Connections in Network (Variant 82)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      817,
      819,
      813
    ],
    "prerequisites": [
      816
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 819,
    "number": 819,
    "title": "Swim in Rising Water (Variant 82)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Swim in Rising Water (Variant 82)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      818,
      820,
      814
    ],
    "prerequisites": [
      817
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 820,
    "number": 820,
    "title": "Evaluate Division (Variant 82)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Evaluate Division (Variant 82)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      819,
      821,
      815
    ],
    "prerequisites": [
      818
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 821,
    "number": 821,
    "title": "Is Graph Bipartite (Variant 83)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Is Graph Bipartite (Variant 83)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      820,
      822,
      816
    ],
    "prerequisites": [
      819
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 822,
    "number": 822,
    "title": "Course Schedule II (Variant 83)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Course Schedule II (Variant 83)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      821,
      823,
      817
    ],
    "prerequisites": [
      820
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 823,
    "number": 823,
    "title": "Surrounded Regions (Variant 83)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Surrounded Regions (Variant 83)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      822,
      824,
      818
    ],
    "prerequisites": [
      821
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 824,
    "number": 824,
    "title": "Cheapest Flights Within K Stops (Variant 83)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Cheapest Flights Within K Stops (Variant 83)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      823,
      825,
      819
    ],
    "prerequisites": [
      822
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 825,
    "number": 825,
    "title": "Network Delay Time (Variant 83)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Network Delay Time (Variant 83)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      824,
      826,
      820
    ],
    "prerequisites": [
      823
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 826,
    "number": 826,
    "title": "As Far from Land as Possible (Variant 83)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **As Far from Land as Possible (Variant 83)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      825,
      827,
      821
    ],
    "prerequisites": [
      824
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 827,
    "number": 827,
    "title": "Minimum Height Trees (Variant 83)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Height Trees (Variant 83)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      826,
      828,
      822
    ],
    "prerequisites": [
      825
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 828,
    "number": 828,
    "title": "Critical Connections in Network (Variant 83)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Critical Connections in Network (Variant 83)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      827,
      829,
      823
    ],
    "prerequisites": [
      826
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 829,
    "number": 829,
    "title": "Swim in Rising Water (Variant 83)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Swim in Rising Water (Variant 83)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      828,
      830,
      824
    ],
    "prerequisites": [
      827
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 830,
    "number": 830,
    "title": "Evaluate Division (Variant 83)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Evaluate Division (Variant 83)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      829,
      831,
      825
    ],
    "prerequisites": [
      828
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 831,
    "number": 831,
    "title": "Is Graph Bipartite (Variant 84)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Is Graph Bipartite (Variant 84)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      830,
      832,
      826
    ],
    "prerequisites": [
      829
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 832,
    "number": 832,
    "title": "Course Schedule II (Variant 84)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Course Schedule II (Variant 84)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      831,
      833,
      827
    ],
    "prerequisites": [
      830
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 833,
    "number": 833,
    "title": "Surrounded Regions (Variant 84)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Surrounded Regions (Variant 84)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      832,
      834,
      828
    ],
    "prerequisites": [
      831
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 834,
    "number": 834,
    "title": "Cheapest Flights Within K Stops (Variant 84)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Cheapest Flights Within K Stops (Variant 84)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      833,
      835,
      829
    ],
    "prerequisites": [
      832
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 835,
    "number": 835,
    "title": "Network Delay Time (Variant 84)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Network Delay Time (Variant 84)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      834,
      836,
      830
    ],
    "prerequisites": [
      833
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 836,
    "number": 836,
    "title": "As Far from Land as Possible (Variant 84)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **As Far from Land as Possible (Variant 84)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      835,
      837,
      831
    ],
    "prerequisites": [
      834
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 837,
    "number": 837,
    "title": "Minimum Height Trees (Variant 84)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Height Trees (Variant 84)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      836,
      838,
      832
    ],
    "prerequisites": [
      835
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 838,
    "number": 838,
    "title": "Critical Connections in Network (Variant 84)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Critical Connections in Network (Variant 84)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      837,
      839,
      833
    ],
    "prerequisites": [
      836
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 839,
    "number": 839,
    "title": "Swim in Rising Water (Variant 84)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Swim in Rising Water (Variant 84)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      838,
      840,
      834
    ],
    "prerequisites": [
      837
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 840,
    "number": 840,
    "title": "Evaluate Division (Variant 84)",
    "difficulty": "Medium",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Evaluate Division (Variant 84)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      839,
      841,
      835
    ],
    "prerequisites": [
      838
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 841,
    "number": 841,
    "title": "Is Graph Bipartite (Variant 85)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Is Graph Bipartite (Variant 85)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      840,
      842,
      836
    ],
    "prerequisites": [
      839
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 842,
    "number": 842,
    "title": "Course Schedule II (Variant 85)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Course Schedule II (Variant 85)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      841,
      843,
      837
    ],
    "prerequisites": [
      840
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 843,
    "number": 843,
    "title": "Surrounded Regions (Variant 85)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Surrounded Regions (Variant 85)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      842,
      844,
      838
    ],
    "prerequisites": [
      841
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 844,
    "number": 844,
    "title": "Cheapest Flights Within K Stops (Variant 85)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Cheapest Flights Within K Stops (Variant 85)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      843,
      845,
      839
    ],
    "prerequisites": [
      842
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 845,
    "number": 845,
    "title": "Network Delay Time (Variant 85)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Network Delay Time (Variant 85)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      844,
      846,
      840
    ],
    "prerequisites": [
      843
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 846,
    "number": 846,
    "title": "As Far from Land as Possible (Variant 85)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **As Far from Land as Possible (Variant 85)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      845,
      847,
      841
    ],
    "prerequisites": [
      844
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 847,
    "number": 847,
    "title": "Minimum Height Trees (Variant 85)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Minimum Height Trees (Variant 85)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      846,
      848,
      842
    ],
    "prerequisites": [
      845
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 848,
    "number": 848,
    "title": "Critical Connections in Network (Variant 85)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Critical Connections in Network (Variant 85)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      847,
      849,
      843
    ],
    "prerequisites": [
      846
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 849,
    "number": 849,
    "title": "Swim in Rising Water (Variant 85)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Swim in Rising Water (Variant 85)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      848,
      850,
      844
    ],
    "prerequisites": [
      847
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 850,
    "number": 850,
    "title": "Evaluate Division (Variant 85)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Evaluate Division (Variant 85)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      849,
      851,
      845
    ],
    "prerequisites": [
      848
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 851,
    "number": 851,
    "title": "Is Graph Bipartite (Variant 86)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Is Graph Bipartite (Variant 86)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      850,
      852,
      846
    ],
    "prerequisites": [
      849
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 852,
    "number": 852,
    "title": "Course Schedule II (Variant 86)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Course Schedule II (Variant 86)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      851,
      853,
      847
    ],
    "prerequisites": [
      850
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 853,
    "number": 853,
    "title": "Surrounded Regions (Variant 86)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Surrounded Regions (Variant 86)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      852,
      854,
      848
    ],
    "prerequisites": [
      851
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 854,
    "number": 854,
    "title": "Cheapest Flights Within K Stops (Variant 86)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Cheapest Flights Within K Stops (Variant 86)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      853,
      855,
      849
    ],
    "prerequisites": [
      852
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 855,
    "number": 855,
    "title": "Network Delay Time (Variant 86)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Network Delay Time (Variant 86)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      854,
      856,
      850
    ],
    "prerequisites": [
      853
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 856,
    "number": 856,
    "title": "As Far from Land as Possible (Variant 86)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **As Far from Land as Possible (Variant 86)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      855,
      857,
      851
    ],
    "prerequisites": [
      854
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 857,
    "number": 857,
    "title": "Minimum Height Trees (Variant 86)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Minimum Height Trees (Variant 86)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      856,
      858,
      852
    ],
    "prerequisites": [
      855
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 858,
    "number": 858,
    "title": "Critical Connections in Network (Variant 86)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Critical Connections in Network (Variant 86)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      857,
      859,
      853
    ],
    "prerequisites": [
      856
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 859,
    "number": 859,
    "title": "Swim in Rising Water (Variant 86)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Swim in Rising Water (Variant 86)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      858,
      860,
      854
    ],
    "prerequisites": [
      857
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 860,
    "number": 860,
    "title": "Evaluate Division (Variant 86)",
    "difficulty": "Hard",
    "topic": "Graphs",
    "subtopic": "BFS / DFS / Dijkstra / DSU Core Mechanics",
    "phase": "Phase 13 \u2014 Graph Algorithms",
    "pattern": "BFS / DFS / Dijkstra / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Evaluate Division (Variant 86)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of BFS / DFS / Dijkstra / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the BFS / DFS / Dijkstra / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (BFS / DFS / Dijkstra / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (BFS / DFS / Dijkstra / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (BFS / DFS / Dijkstra / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (BFS / DFS / Dijkstra / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      859,
      861,
      855
    ],
    "prerequisites": [
      858
    ],
    "tags": [
      "Graphs",
      "BFS / DFS / Dijkstra / DSU",
      "Phase 13",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects BFS / DFS / Dijkstra / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. BFS / DFS / Dijkstra / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose BFS / DFS / Dijkstra / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 861,
    "number": 861,
    "title": "Implement Trie (Variant 87)",
    "difficulty": "Easy",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Implement Trie (Variant 87)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      860,
      862,
      856
    ],
    "prerequisites": [
      859
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 862,
    "number": 862,
    "title": "Design Add and Search Words (Variant 87)",
    "difficulty": "Easy",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Design Add and Search Words (Variant 87)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      861,
      863,
      857
    ],
    "prerequisites": [
      860
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 863,
    "number": 863,
    "title": "Word Search II (Variant 87)",
    "difficulty": "Easy",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Word Search II (Variant 87)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      862,
      864,
      858
    ],
    "prerequisites": [
      861
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 864,
    "number": 864,
    "title": "Replace Words (Variant 87)",
    "difficulty": "Easy",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Replace Words (Variant 87)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      863,
      865,
      859
    ],
    "prerequisites": [
      862
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 865,
    "number": 865,
    "title": "Map Sum Pairs (Variant 87)",
    "difficulty": "Easy",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Map Sum Pairs (Variant 87)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      864,
      866,
      860
    ],
    "prerequisites": [
      863
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 866,
    "number": 866,
    "title": "Maximum XOR of Two Numbers (Variant 87)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Maximum XOR of Two Numbers (Variant 87)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      865,
      867,
      861
    ],
    "prerequisites": [
      864
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 867,
    "number": 867,
    "title": "Concatenated Words (Variant 87)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Concatenated Words (Variant 87)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      866,
      868,
      862
    ],
    "prerequisites": [
      865
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 868,
    "number": 868,
    "title": "Stream of Characters (Variant 87)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Stream of Characters (Variant 87)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      867,
      869,
      863
    ],
    "prerequisites": [
      866
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 869,
    "number": 869,
    "title": "Palindrome Pairs (Variant 87)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Palindrome Pairs (Variant 87)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      868,
      870,
      864
    ],
    "prerequisites": [
      867
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 870,
    "number": 870,
    "title": "Multi-Search Dictionary (Variant 87)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Multi-Search Dictionary (Variant 87)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      869,
      871,
      865
    ],
    "prerequisites": [
      868
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 871,
    "number": 871,
    "title": "Implement Trie (Variant 88)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Implement Trie (Variant 88)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      870,
      872,
      866
    ],
    "prerequisites": [
      869
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 872,
    "number": 872,
    "title": "Design Add and Search Words (Variant 88)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Design Add and Search Words (Variant 88)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      871,
      873,
      867
    ],
    "prerequisites": [
      870
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 873,
    "number": 873,
    "title": "Word Search II (Variant 88)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Word Search II (Variant 88)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      872,
      874,
      868
    ],
    "prerequisites": [
      871
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 874,
    "number": 874,
    "title": "Replace Words (Variant 88)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Replace Words (Variant 88)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      873,
      875,
      869
    ],
    "prerequisites": [
      872
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 875,
    "number": 875,
    "title": "Map Sum Pairs (Variant 88)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Map Sum Pairs (Variant 88)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      874,
      876,
      870
    ],
    "prerequisites": [
      873
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 876,
    "number": 876,
    "title": "Maximum XOR of Two Numbers (Variant 88)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Maximum XOR of Two Numbers (Variant 88)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      875,
      877,
      871
    ],
    "prerequisites": [
      874
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 877,
    "number": 877,
    "title": "Concatenated Words (Variant 88)",
    "difficulty": "Medium",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Concatenated Words (Variant 88)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      876,
      878,
      872
    ],
    "prerequisites": [
      875
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 100000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 878,
    "number": 878,
    "title": "Stream of Characters (Variant 88)",
    "difficulty": "Hard",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Stream of Characters (Variant 88)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      877,
      879,
      873
    ],
    "prerequisites": [
      876
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 879,
    "number": 879,
    "title": "Palindrome Pairs (Variant 88)",
    "difficulty": "Hard",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Palindrome Pairs (Variant 88)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      878,
      880,
      874
    ],
    "prerequisites": [
      877
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 880,
    "number": 880,
    "title": "Multi-Search Dictionary (Variant 88)",
    "difficulty": "Hard",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Multi-Search Dictionary (Variant 88)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      879,
      881,
      875
    ],
    "prerequisites": [
      878
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 881,
    "number": 881,
    "title": "Implement Trie (Variant 89)",
    "difficulty": "Hard",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Implement Trie (Variant 89)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      880,
      882,
      876
    ],
    "prerequisites": [
      879
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 882,
    "number": 882,
    "title": "Design Add and Search Words (Variant 89)",
    "difficulty": "Hard",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Design Add and Search Words (Variant 89)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      881,
      883,
      877
    ],
    "prerequisites": [
      880
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 883,
    "number": 883,
    "title": "Word Search II (Variant 89)",
    "difficulty": "Hard",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Word Search II (Variant 89)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      882,
      884,
      878
    ],
    "prerequisites": [
      881
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 884,
    "number": 884,
    "title": "Replace Words (Variant 89)",
    "difficulty": "Hard",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Replace Words (Variant 89)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      883,
      885,
      879
    ],
    "prerequisites": [
      882
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 885,
    "number": 885,
    "title": "Map Sum Pairs (Variant 89)",
    "difficulty": "Hard",
    "topic": "Trie",
    "subtopic": "Prefix Tree Core Mechanics",
    "phase": "Phase 14 \u2014 Trie",
    "pattern": "Prefix Tree",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Map Sum Pairs (Variant 89)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Prefix Tree. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Prefix Tree technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Prefix Tree)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Prefix Tree)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Prefix Tree)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Prefix Tree)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      884,
      886,
      880
    ],
    "prerequisites": [
      883
    ],
    "tags": [
      "Trie",
      "Prefix Tree",
      "Phase 14",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Prefix Tree because constraints are N <= 1000000, making O(N^2) brute force infeasible. Prefix Tree leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Prefix Tree optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 886,
    "number": 886,
    "title": "Unique Paths II (Variant 89)",
    "difficulty": "Easy",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Unique Paths II (Variant 89)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      885,
      887,
      881
    ],
    "prerequisites": [
      884
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 887,
    "number": 887,
    "title": "Minimum Path Sum (Variant 89)",
    "difficulty": "Easy",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Minimum Path Sum (Variant 89)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      886,
      888,
      882
    ],
    "prerequisites": [
      885
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 888,
    "number": 888,
    "title": "Longest Common Subsequence (Variant 89)",
    "difficulty": "Easy",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Longest Common Subsequence (Variant 89)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      887,
      889,
      883
    ],
    "prerequisites": [
      886
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 889,
    "number": 889,
    "title": "Palindromic Substrings (Variant 89)",
    "difficulty": "Easy",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Palindromic Substrings (Variant 89)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      888,
      890,
      884
    ],
    "prerequisites": [
      887
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 890,
    "number": 890,
    "title": "Interleaving String (Variant 89)",
    "difficulty": "Easy",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Interleaving String (Variant 89)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      889,
      891,
      885
    ],
    "prerequisites": [
      888
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 891,
    "number": 891,
    "title": "House Robber II (Variant 90)",
    "difficulty": "Easy",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **House Robber II (Variant 90)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      890,
      892,
      886
    ],
    "prerequisites": [
      889
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 892,
    "number": 892,
    "title": "House Robber III (Variant 90)",
    "difficulty": "Easy",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **House Robber III (Variant 90)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      891,
      893,
      887
    ],
    "prerequisites": [
      890
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 893,
    "number": 893,
    "title": "Target Sum (Variant 90)",
    "difficulty": "Easy",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Target Sum (Variant 90)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      892,
      894,
      888
    ],
    "prerequisites": [
      891
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 894,
    "number": 894,
    "title": "Coin Change II (Variant 90)",
    "difficulty": "Easy",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Coin Change II (Variant 90)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      893,
      895,
      889
    ],
    "prerequisites": [
      892
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 895,
    "number": 895,
    "title": "Unique Paths (Variant 90)",
    "difficulty": "Easy",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Unique Paths (Variant 90)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      894,
      896,
      890
    ],
    "prerequisites": [
      893
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 896,
    "number": 896,
    "title": "Unique Paths II (Variant 90)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Unique Paths II (Variant 90)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      895,
      897,
      891
    ],
    "prerequisites": [
      894
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 897,
    "number": 897,
    "title": "Minimum Path Sum (Variant 90)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Path Sum (Variant 90)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      896,
      898,
      892
    ],
    "prerequisites": [
      895
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 898,
    "number": 898,
    "title": "Longest Common Subsequence (Variant 90)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Longest Common Subsequence (Variant 90)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      897,
      899,
      893
    ],
    "prerequisites": [
      896
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 899,
    "number": 899,
    "title": "Palindromic Substrings (Variant 90)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Palindromic Substrings (Variant 90)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      898,
      900,
      894
    ],
    "prerequisites": [
      897
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 900,
    "number": 900,
    "title": "Interleaving String (Variant 90)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Interleaving String (Variant 90)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      899,
      901,
      895
    ],
    "prerequisites": [
      898
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 901,
    "number": 901,
    "title": "House Robber II (Variant 91)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **House Robber II (Variant 91)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      900,
      902,
      896
    ],
    "prerequisites": [
      899
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 902,
    "number": 902,
    "title": "House Robber III (Variant 91)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **House Robber III (Variant 91)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      901,
      903,
      897
    ],
    "prerequisites": [
      900
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 903,
    "number": 903,
    "title": "Target Sum (Variant 91)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Target Sum (Variant 91)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      902,
      904,
      898
    ],
    "prerequisites": [
      901
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 904,
    "number": 904,
    "title": "Coin Change II (Variant 91)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Coin Change II (Variant 91)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      903,
      905,
      899
    ],
    "prerequisites": [
      902
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 905,
    "number": 905,
    "title": "Unique Paths (Variant 91)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Unique Paths (Variant 91)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      904,
      906,
      900
    ],
    "prerequisites": [
      903
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 906,
    "number": 906,
    "title": "Unique Paths II (Variant 91)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Unique Paths II (Variant 91)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      905,
      907,
      901
    ],
    "prerequisites": [
      904
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 907,
    "number": 907,
    "title": "Minimum Path Sum (Variant 91)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Path Sum (Variant 91)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      906,
      908,
      902
    ],
    "prerequisites": [
      905
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 908,
    "number": 908,
    "title": "Longest Common Subsequence (Variant 91)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Longest Common Subsequence (Variant 91)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      907,
      909,
      903
    ],
    "prerequisites": [
      906
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 909,
    "number": 909,
    "title": "Palindromic Substrings (Variant 91)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Palindromic Substrings (Variant 91)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      908,
      910,
      904
    ],
    "prerequisites": [
      907
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 910,
    "number": 910,
    "title": "Interleaving String (Variant 91)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Interleaving String (Variant 91)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      909,
      911,
      905
    ],
    "prerequisites": [
      908
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 911,
    "number": 911,
    "title": "House Robber II (Variant 92)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **House Robber II (Variant 92)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      910,
      912,
      906
    ],
    "prerequisites": [
      909
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 912,
    "number": 912,
    "title": "House Robber III (Variant 92)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **House Robber III (Variant 92)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      911,
      913,
      907
    ],
    "prerequisites": [
      910
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 913,
    "number": 913,
    "title": "Target Sum (Variant 92)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Target Sum (Variant 92)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      912,
      914,
      908
    ],
    "prerequisites": [
      911
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 914,
    "number": 914,
    "title": "Coin Change II (Variant 92)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Coin Change II (Variant 92)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      913,
      915,
      909
    ],
    "prerequisites": [
      912
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 915,
    "number": 915,
    "title": "Unique Paths (Variant 92)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Unique Paths (Variant 92)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      914,
      916,
      910
    ],
    "prerequisites": [
      913
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 916,
    "number": 916,
    "title": "Unique Paths II (Variant 92)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Unique Paths II (Variant 92)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      915,
      917,
      911
    ],
    "prerequisites": [
      914
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 917,
    "number": 917,
    "title": "Minimum Path Sum (Variant 92)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Path Sum (Variant 92)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      916,
      918,
      912
    ],
    "prerequisites": [
      915
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 918,
    "number": 918,
    "title": "Longest Common Subsequence (Variant 92)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Longest Common Subsequence (Variant 92)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      917,
      919,
      913
    ],
    "prerequisites": [
      916
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 919,
    "number": 919,
    "title": "Palindromic Substrings (Variant 92)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Palindromic Substrings (Variant 92)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      918,
      920,
      914
    ],
    "prerequisites": [
      917
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 920,
    "number": 920,
    "title": "Interleaving String (Variant 92)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Interleaving String (Variant 92)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      919,
      921,
      915
    ],
    "prerequisites": [
      918
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 921,
    "number": 921,
    "title": "House Robber II (Variant 93)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **House Robber II (Variant 93)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      920,
      922,
      916
    ],
    "prerequisites": [
      919
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 922,
    "number": 922,
    "title": "House Robber III (Variant 93)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **House Robber III (Variant 93)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      921,
      923,
      917
    ],
    "prerequisites": [
      920
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 923,
    "number": 923,
    "title": "Target Sum (Variant 93)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Target Sum (Variant 93)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      922,
      924,
      918
    ],
    "prerequisites": [
      921
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 924,
    "number": 924,
    "title": "Coin Change II (Variant 93)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Coin Change II (Variant 93)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      923,
      925,
      919
    ],
    "prerequisites": [
      922
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 925,
    "number": 925,
    "title": "Unique Paths (Variant 93)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Unique Paths (Variant 93)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      924,
      926,
      920
    ],
    "prerequisites": [
      923
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 926,
    "number": 926,
    "title": "Unique Paths II (Variant 93)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Unique Paths II (Variant 93)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      925,
      927,
      921
    ],
    "prerequisites": [
      924
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 927,
    "number": 927,
    "title": "Minimum Path Sum (Variant 93)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Path Sum (Variant 93)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      926,
      928,
      922
    ],
    "prerequisites": [
      925
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 928,
    "number": 928,
    "title": "Longest Common Subsequence (Variant 93)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Longest Common Subsequence (Variant 93)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      927,
      929,
      923
    ],
    "prerequisites": [
      926
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 929,
    "number": 929,
    "title": "Palindromic Substrings (Variant 93)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Palindromic Substrings (Variant 93)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      928,
      930,
      924
    ],
    "prerequisites": [
      927
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 930,
    "number": 930,
    "title": "Interleaving String (Variant 93)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Interleaving String (Variant 93)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      929,
      931,
      925
    ],
    "prerequisites": [
      928
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 931,
    "number": 931,
    "title": "House Robber II (Variant 94)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **House Robber II (Variant 94)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      930,
      932,
      926
    ],
    "prerequisites": [
      929
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 932,
    "number": 932,
    "title": "House Robber III (Variant 94)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **House Robber III (Variant 94)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      931,
      933,
      927
    ],
    "prerequisites": [
      930
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 933,
    "number": 933,
    "title": "Target Sum (Variant 94)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Target Sum (Variant 94)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      932,
      934,
      928
    ],
    "prerequisites": [
      931
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 934,
    "number": 934,
    "title": "Coin Change II (Variant 94)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Coin Change II (Variant 94)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      933,
      935,
      929
    ],
    "prerequisites": [
      932
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 935,
    "number": 935,
    "title": "Unique Paths (Variant 94)",
    "difficulty": "Medium",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Unique Paths (Variant 94)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      934,
      936,
      930
    ],
    "prerequisites": [
      933
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 100000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 936,
    "number": 936,
    "title": "Unique Paths II (Variant 94)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Unique Paths II (Variant 94)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      935,
      937,
      931
    ],
    "prerequisites": [
      934
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 937,
    "number": 937,
    "title": "Minimum Path Sum (Variant 94)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Minimum Path Sum (Variant 94)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      936,
      938,
      932
    ],
    "prerequisites": [
      935
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 938,
    "number": 938,
    "title": "Longest Common Subsequence (Variant 94)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Longest Common Subsequence (Variant 94)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      937,
      939,
      933
    ],
    "prerequisites": [
      936
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 939,
    "number": 939,
    "title": "Palindromic Substrings (Variant 94)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Palindromic Substrings (Variant 94)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      938,
      940,
      934
    ],
    "prerequisites": [
      937
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 940,
    "number": 940,
    "title": "Interleaving String (Variant 94)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Interleaving String (Variant 94)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      939,
      941,
      935
    ],
    "prerequisites": [
      938
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 941,
    "number": 941,
    "title": "House Robber II (Variant 95)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **House Robber II (Variant 95)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      940,
      942,
      936
    ],
    "prerequisites": [
      939
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 942,
    "number": 942,
    "title": "House Robber III (Variant 95)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **House Robber III (Variant 95)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      941,
      943,
      937
    ],
    "prerequisites": [
      940
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 943,
    "number": 943,
    "title": "Target Sum (Variant 95)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Target Sum (Variant 95)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      942,
      944,
      938
    ],
    "prerequisites": [
      941
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 944,
    "number": 944,
    "title": "Coin Change II (Variant 95)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Coin Change II (Variant 95)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      943,
      945,
      939
    ],
    "prerequisites": [
      942
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 945,
    "number": 945,
    "title": "Unique Paths (Variant 95)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Unique Paths (Variant 95)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      944,
      946,
      940
    ],
    "prerequisites": [
      943
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 946,
    "number": 946,
    "title": "Unique Paths II (Variant 95)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Unique Paths II (Variant 95)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      945,
      947,
      941
    ],
    "prerequisites": [
      944
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 947,
    "number": 947,
    "title": "Minimum Path Sum (Variant 95)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Minimum Path Sum (Variant 95)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      946,
      948,
      942
    ],
    "prerequisites": [
      945
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 948,
    "number": 948,
    "title": "Longest Common Subsequence (Variant 95)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Longest Common Subsequence (Variant 95)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      947,
      949,
      943
    ],
    "prerequisites": [
      946
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 949,
    "number": 949,
    "title": "Palindromic Substrings (Variant 95)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Palindromic Substrings (Variant 95)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      948,
      950,
      944
    ],
    "prerequisites": [
      947
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 950,
    "number": 950,
    "title": "Interleaving String (Variant 95)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Interleaving String (Variant 95)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      949,
      951,
      945
    ],
    "prerequisites": [
      948
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 951,
    "number": 951,
    "title": "House Robber II (Variant 96)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **House Robber II (Variant 96)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      950,
      952,
      946
    ],
    "prerequisites": [
      949
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 952,
    "number": 952,
    "title": "House Robber III (Variant 96)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **House Robber III (Variant 96)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      951,
      953,
      947
    ],
    "prerequisites": [
      950
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 953,
    "number": 953,
    "title": "Target Sum (Variant 96)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Target Sum (Variant 96)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      952,
      954,
      948
    ],
    "prerequisites": [
      951
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 954,
    "number": 954,
    "title": "Coin Change II (Variant 96)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Coin Change II (Variant 96)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      953,
      955,
      949
    ],
    "prerequisites": [
      952
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 955,
    "number": 955,
    "title": "Unique Paths (Variant 96)",
    "difficulty": "Hard",
    "topic": "Dynamic Programming",
    "subtopic": "1D / 2D DP / Knapsack / State Machine Core Mechanics",
    "phase": "Phase 15 \u2014 Dynamic Programming",
    "pattern": "1D / 2D DP / Knapsack / State Machine",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Unique Paths (Variant 96)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of 1D / 2D DP / Knapsack / State Machine. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the 1D / 2D DP / Knapsack / State Machine technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (1D / 2D DP / Knapsack / State Machine)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (1D / 2D DP / Knapsack / State Machine)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (1D / 2D DP / Knapsack / State Machine)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (1D / 2D DP / Knapsack / State Machine)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      954,
      956,
      950
    ],
    "prerequisites": [
      953
    ],
    "tags": [
      "Dynamic Programming",
      "1D / 2D DP / Knapsack / State Machine",
      "Phase 15",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects 1D / 2D DP / Knapsack / State Machine because constraints are N <= 1000000, making O(N^2) brute force infeasible. 1D / 2D DP / Knapsack / State Machine leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose 1D / 2D DP / Knapsack / State Machine optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 956,
    "number": 956,
    "title": "The Skyline Problem (Variant 96)",
    "difficulty": "Easy",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **The Skyline Problem (Variant 96)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      955,
      957,
      951
    ],
    "prerequisites": [
      954
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 957,
    "number": 957,
    "title": "Falling Squares (Variant 96)",
    "difficulty": "Easy",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Falling Squares (Variant 96)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      956,
      958,
      952
    ],
    "prerequisites": [
      955
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 958,
    "number": 958,
    "title": "Range Module (Variant 96)",
    "difficulty": "Easy",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Range Module (Variant 96)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      957,
      959,
      953
    ],
    "prerequisites": [
      956
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 959,
    "number": 959,
    "title": "My Calendar Three (Variant 96)",
    "difficulty": "Medium",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **My Calendar Three (Variant 96)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      958,
      960,
      954
    ],
    "prerequisites": [
      957
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 960,
    "number": 960,
    "title": "Dynamic Segment Tree (Variant 96)",
    "difficulty": "Medium",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Dynamic Segment Tree (Variant 96)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      959,
      961,
      955
    ],
    "prerequisites": [
      958
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 961,
    "number": 961,
    "title": "Redundant Connection (Variant 97)",
    "difficulty": "Medium",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Redundant Connection (Variant 97)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      960,
      962,
      956
    ],
    "prerequisites": [
      959
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 962,
    "number": 962,
    "title": "Redundant Connection II (Variant 97)",
    "difficulty": "Medium",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Redundant Connection II (Variant 97)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      961,
      963,
      957
    ],
    "prerequisites": [
      960
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 963,
    "number": 963,
    "title": "Range Sum Query Immutable (Variant 97)",
    "difficulty": "Medium",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Range Sum Query Immutable (Variant 97)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      962,
      964,
      958
    ],
    "prerequisites": [
      961
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 964,
    "number": 964,
    "title": "Range Sum Query Mutable (Variant 97)",
    "difficulty": "Medium",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Range Sum Query Mutable (Variant 97)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      963,
      965,
      959
    ],
    "prerequisites": [
      962
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 965,
    "number": 965,
    "title": "Count of Range Sum (Variant 97)",
    "difficulty": "Medium",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Count of Range Sum (Variant 97)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      964,
      966,
      960
    ],
    "prerequisites": [
      963
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 966,
    "number": 966,
    "title": "The Skyline Problem (Variant 97)",
    "difficulty": "Medium",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **The Skyline Problem (Variant 97)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      965,
      967,
      961
    ],
    "prerequisites": [
      964
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 967,
    "number": 967,
    "title": "Falling Squares (Variant 97)",
    "difficulty": "Medium",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Falling Squares (Variant 97)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      966,
      968,
      962
    ],
    "prerequisites": [
      965
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 968,
    "number": 968,
    "title": "Range Module (Variant 97)",
    "difficulty": "Medium",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Range Module (Variant 97)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      967,
      969,
      963
    ],
    "prerequisites": [
      966
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 100000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 969,
    "number": 969,
    "title": "My Calendar Three (Variant 97)",
    "difficulty": "Hard",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **My Calendar Three (Variant 97)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      968,
      970,
      964
    ],
    "prerequisites": [
      967
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 970,
    "number": 970,
    "title": "Dynamic Segment Tree (Variant 97)",
    "difficulty": "Hard",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Dynamic Segment Tree (Variant 97)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      969,
      971,
      965
    ],
    "prerequisites": [
      968
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 971,
    "number": 971,
    "title": "Redundant Connection (Variant 98)",
    "difficulty": "Hard",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Redundant Connection (Variant 98)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      970,
      972,
      966
    ],
    "prerequisites": [
      969
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 972,
    "number": 972,
    "title": "Redundant Connection II (Variant 98)",
    "difficulty": "Hard",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Redundant Connection II (Variant 98)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      971,
      973,
      967
    ],
    "prerequisites": [
      970
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 973,
    "number": 973,
    "title": "Range Sum Query Immutable (Variant 98)",
    "difficulty": "Hard",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Range Sum Query Immutable (Variant 98)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      972,
      974,
      968
    ],
    "prerequisites": [
      971
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 974,
    "number": 974,
    "title": "Range Sum Query Mutable (Variant 98)",
    "difficulty": "Hard",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Range Sum Query Mutable (Variant 98)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      973,
      975,
      969
    ],
    "prerequisites": [
      972
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 975,
    "number": 975,
    "title": "Count of Range Sum (Variant 98)",
    "difficulty": "Hard",
    "topic": "Advanced Data Structures",
    "subtopic": "Segment Tree / Fenwick Tree / DSU Core Mechanics",
    "phase": "Phase 16 \u2014 Advanced Data Structures",
    "pattern": "Segment Tree / Fenwick Tree / DSU",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Count of Range Sum (Variant 98)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Segment Tree / Fenwick Tree / DSU. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Segment Tree / Fenwick Tree / DSU technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(N)",
      "code": {
        "cpp": "// Optimal C++ Solution (Segment Tree / Fenwick Tree / DSU)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Segment Tree / Fenwick Tree / DSU)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Segment Tree / Fenwick Tree / DSU)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Segment Tree / Fenwick Tree / DSU)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      974,
      976,
      970
    ],
    "prerequisites": [
      973
    ],
    "tags": [
      "Advanced Data Structures",
      "Segment Tree / Fenwick Tree / DSU",
      "Phase 16",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Segment Tree / Fenwick Tree / DSU because constraints are N <= 1000000, making O(N^2) brute force infeasible. Segment Tree / Fenwick Tree / DSU leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Segment Tree / Fenwick Tree / DSU optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 976,
    "number": 976,
    "title": "Number of 1 Bits (Variant 98)",
    "difficulty": "Easy",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Number of 1 Bits (Variant 98)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      975,
      977,
      971
    ],
    "prerequisites": [
      974
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 977,
    "number": 977,
    "title": "Power of Two (Variant 98)",
    "difficulty": "Easy",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Power of Two (Variant 98)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      976,
      978,
      972
    ],
    "prerequisites": [
      975
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 978,
    "number": 978,
    "title": "Power of Three (Variant 98)",
    "difficulty": "Easy",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Power of Three (Variant 98)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      977,
      979,
      973
    ],
    "prerequisites": [
      976
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 979,
    "number": 979,
    "title": "Multiply Strings (Variant 98)",
    "difficulty": "Easy",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Multiply Strings (Variant 98)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      978,
      980,
      974
    ],
    "prerequisites": [
      977
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 980,
    "number": 980,
    "title": "Fraction to Recurring Decimal (Variant 98)",
    "difficulty": "Easy",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Fraction to Recurring Decimal (Variant 98)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      979,
      981,
      975
    ],
    "prerequisites": [
      978
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 981,
    "number": 981,
    "title": "Single Number II (Variant 99)",
    "difficulty": "Easy",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Single Number II (Variant 99)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      980,
      982,
      976
    ],
    "prerequisites": [
      979
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 982,
    "number": 982,
    "title": "Single Number III (Variant 99)",
    "difficulty": "Easy",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 15,
    "statement": "Given an input configuration representative of **Single Number III (Variant 99)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      981,
      983,
      977
    ],
    "prerequisites": [
      980
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Easy"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 983,
    "number": 983,
    "title": "Bitwise AND of Numbers Range (Variant 99)",
    "difficulty": "Medium",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Bitwise AND of Numbers Range (Variant 99)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      982,
      984,
      978
    ],
    "prerequisites": [
      981
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 984,
    "number": 984,
    "title": "Counting Bits (Variant 99)",
    "difficulty": "Medium",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Counting Bits (Variant 99)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      983,
      985,
      979
    ],
    "prerequisites": [
      982
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 985,
    "number": 985,
    "title": "Reverse Bits (Variant 99)",
    "difficulty": "Medium",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Reverse Bits (Variant 99)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      984,
      986,
      980
    ],
    "prerequisites": [
      983
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 986,
    "number": 986,
    "title": "Number of 1 Bits (Variant 99)",
    "difficulty": "Medium",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Number of 1 Bits (Variant 99)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      985,
      987,
      981
    ],
    "prerequisites": [
      984
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 987,
    "number": 987,
    "title": "Power of Two (Variant 99)",
    "difficulty": "Medium",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Power of Two (Variant 99)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      986,
      988,
      982
    ],
    "prerequisites": [
      985
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 988,
    "number": 988,
    "title": "Power of Three (Variant 99)",
    "difficulty": "Medium",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Power of Three (Variant 99)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      987,
      989,
      983
    ],
    "prerequisites": [
      986
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 989,
    "number": 989,
    "title": "Multiply Strings (Variant 99)",
    "difficulty": "Medium",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Multiply Strings (Variant 99)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      988,
      990,
      984
    ],
    "prerequisites": [
      987
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 990,
    "number": 990,
    "title": "Fraction to Recurring Decimal (Variant 99)",
    "difficulty": "Medium",
    "topic": "Math & Bit Manipulation",
    "subtopic": "Bitwise XOR / Fast Exponentiation Core Mechanics",
    "phase": "Phase 17 \u2014 Math & Bit Manipulation",
    "pattern": "Bitwise XOR / Fast Exponentiation",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Fraction to Recurring Decimal (Variant 99)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Bitwise XOR / Fast Exponentiation. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Bitwise XOR / Fast Exponentiation technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Bitwise XOR / Fast Exponentiation)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Bitwise XOR / Fast Exponentiation)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Bitwise XOR / Fast Exponentiation)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Bitwise XOR / Fast Exponentiation)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      989,
      991,
      985
    ],
    "prerequisites": [
      988
    ],
    "tags": [
      "Math & Bit Manipulation",
      "Bitwise XOR / Fast Exponentiation",
      "Phase 17",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Bitwise XOR / Fast Exponentiation because constraints are N <= 100000, making O(N^2) brute force infeasible. Bitwise XOR / Fast Exponentiation leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Bitwise XOR / Fast Exponentiation optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 991,
    "number": 991,
    "title": "Minimum Window Subsequence (Variant 100)",
    "difficulty": "Medium",
    "topic": "Mixed Interview Problems",
    "subtopic": "Multi-Pattern System Design Core Mechanics",
    "phase": "Phase 18 \u2014 Mixed Interview Problems",
    "pattern": "Multi-Pattern System Design",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Minimum Window Subsequence (Variant 100)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Multi-Pattern System Design. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Multi-Pattern System Design technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Multi-Pattern System Design)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Multi-Pattern System Design)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Multi-Pattern System Design)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Multi-Pattern System Design)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      990,
      992,
      986
    ],
    "prerequisites": [
      989
    ],
    "tags": [
      "Mixed Interview Problems",
      "Multi-Pattern System Design",
      "Phase 18",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Multi-Pattern System Design because constraints are N <= 100000, making O(N^2) brute force infeasible. Multi-Pattern System Design leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Multi-Pattern System Design optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 992,
    "number": 992,
    "title": "Stamping The Sequence (Variant 100)",
    "difficulty": "Medium",
    "topic": "Mixed Interview Problems",
    "subtopic": "Multi-Pattern System Design Core Mechanics",
    "phase": "Phase 18 \u2014 Mixed Interview Problems",
    "pattern": "Multi-Pattern System Design",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Stamping The Sequence (Variant 100)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Multi-Pattern System Design. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Multi-Pattern System Design technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Multi-Pattern System Design)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Multi-Pattern System Design)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Multi-Pattern System Design)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Multi-Pattern System Design)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      991,
      993,
      987
    ],
    "prerequisites": [
      990
    ],
    "tags": [
      "Mixed Interview Problems",
      "Multi-Pattern System Design",
      "Phase 18",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Multi-Pattern System Design because constraints are N <= 100000, making O(N^2) brute force infeasible. Multi-Pattern System Design leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Multi-Pattern System Design optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 993,
    "number": 993,
    "title": "Trapping Rain Water Mixed (Variant 100)",
    "difficulty": "Medium",
    "topic": "Mixed Interview Problems",
    "subtopic": "Multi-Pattern System Design Core Mechanics",
    "phase": "Phase 18 \u2014 Mixed Interview Problems",
    "pattern": "Multi-Pattern System Design",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Trapping Rain Water Mixed (Variant 100)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Multi-Pattern System Design. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Multi-Pattern System Design technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Multi-Pattern System Design)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Multi-Pattern System Design)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Multi-Pattern System Design)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Multi-Pattern System Design)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      992,
      994,
      988
    ],
    "prerequisites": [
      991
    ],
    "tags": [
      "Mixed Interview Problems",
      "Multi-Pattern System Design",
      "Phase 18",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Multi-Pattern System Design because constraints are N <= 100000, making O(N^2) brute force infeasible. Multi-Pattern System Design leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Multi-Pattern System Design optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 994,
    "number": 994,
    "title": "Shortest Subarray with Sum at Least K (Variant 100)",
    "difficulty": "Medium",
    "topic": "Mixed Interview Problems",
    "subtopic": "Multi-Pattern System Design Core Mechanics",
    "phase": "Phase 18 \u2014 Mixed Interview Problems",
    "pattern": "Multi-Pattern System Design",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Shortest Subarray with Sum at Least K (Variant 100)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "nums = [2, 7, 11, 15], target = 9",
        "output": "[0, 1]",
        "explanation": "Selecting the elements at indices 0 and 1 yields the target sum of 9."
      },
      {
        "input": "nums = [3, 2, 4], target = 6",
        "output": "[1, 2]",
        "explanation": "Selecting indices 1 and 2 yields 2 + 4 = 6."
      }
    ],
    "hints": [
      "Think about the primary invariant of Multi-Pattern System Design. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Multi-Pattern System Design technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Multi-Pattern System Design)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Multi-Pattern System Design)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Multi-Pattern System Design)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Multi-Pattern System Design)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      993,
      995,
      989
    ],
    "prerequisites": [
      992
    ],
    "tags": [
      "Mixed Interview Problems",
      "Multi-Pattern System Design",
      "Phase 18",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Multi-Pattern System Design because constraints are N <= 100000, making O(N^2) brute force infeasible. Multi-Pattern System Design leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Multi-Pattern System Design optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 995,
    "number": 995,
    "title": "Sliding Window Maximum + Heap (Variant 100)",
    "difficulty": "Medium",
    "topic": "Mixed Interview Problems",
    "subtopic": "Multi-Pattern System Design Core Mechanics",
    "phase": "Phase 18 \u2014 Mixed Interview Problems",
    "pattern": "Multi-Pattern System Design",
    "estimatedTime": 30,
    "statement": "Given an input configuration representative of **Sliding Window Maximum + Heap (Variant 100)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 100000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Multi-Pattern System Design. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(N^2)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Multi-Pattern System Design technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Multi-Pattern System Design)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Multi-Pattern System Design)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Multi-Pattern System Design)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Multi-Pattern System Design)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      994,
      996,
      990
    ],
    "prerequisites": [
      993
    ],
    "tags": [
      "Mixed Interview Problems",
      "Multi-Pattern System Design",
      "Phase 18",
      "Medium"
    ],
    "whyThisPattern": "Interviewer expects Multi-Pattern System Design because constraints are N <= 100000, making O(N^2) brute force infeasible. Multi-Pattern System Design leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Multi-Pattern System Design optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 996,
    "number": 996,
    "title": "Distributed Log System Simulator (Variant 200)",
    "difficulty": "Hard",
    "topic": "FAANG & Product Level",
    "subtopic": "Multi-Pattern System Design Core Mechanics",
    "phase": "Phase 19 \u2014 FAANG & Product Company Level",
    "pattern": "Multi-Pattern System Design",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Distributed Log System Simulator (Variant 200)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Multi-Pattern System Design. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Multi-Pattern System Design technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Multi-Pattern System Design)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Multi-Pattern System Design)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Multi-Pattern System Design)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Multi-Pattern System Design)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      995,
      997,
      991
    ],
    "prerequisites": [
      994
    ],
    "tags": [
      "FAANG & Product Level",
      "Multi-Pattern System Design",
      "Phase 19",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Multi-Pattern System Design because constraints are N <= 1000000, making O(N^2) brute force infeasible. Multi-Pattern System Design leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Multi-Pattern System Design optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 997,
    "number": 997,
    "title": "High Throughput Cache Invalidation (Variant 200)",
    "difficulty": "Hard",
    "topic": "FAANG & Product Level",
    "subtopic": "Multi-Pattern System Design Core Mechanics",
    "phase": "Phase 19 \u2014 FAANG & Product Company Level",
    "pattern": "Multi-Pattern System Design",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **High Throughput Cache Invalidation (Variant 200)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Multi-Pattern System Design. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Multi-Pattern System Design technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Multi-Pattern System Design)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Multi-Pattern System Design)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Multi-Pattern System Design)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Multi-Pattern System Design)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      996,
      998,
      992
    ],
    "prerequisites": [
      995
    ],
    "tags": [
      "FAANG & Product Level",
      "Multi-Pattern System Design",
      "Phase 19",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Multi-Pattern System Design because constraints are N <= 1000000, making O(N^2) brute force infeasible. Multi-Pattern System Design leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Multi-Pattern System Design optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 998,
    "number": 998,
    "title": "Real-Time Top K Trending Stream (Variant 200)",
    "difficulty": "Hard",
    "topic": "FAANG & Product Level",
    "subtopic": "Multi-Pattern System Design Core Mechanics",
    "phase": "Phase 19 \u2014 FAANG & Product Company Level",
    "pattern": "Multi-Pattern System Design",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Real-Time Top K Trending Stream (Variant 200)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Multi-Pattern System Design. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Multi-Pattern System Design technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Multi-Pattern System Design)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Multi-Pattern System Design)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Multi-Pattern System Design)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Multi-Pattern System Design)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      997,
      999,
      993
    ],
    "prerequisites": [
      996
    ],
    "tags": [
      "FAANG & Product Level",
      "Multi-Pattern System Design",
      "Phase 19",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Multi-Pattern System Design because constraints are N <= 1000000, making O(N^2) brute force infeasible. Multi-Pattern System Design leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Multi-Pattern System Design optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 999,
    "number": 999,
    "title": "Garbage Collector Reference Graph Analyzer (Variant 200)",
    "difficulty": "Hard",
    "topic": "FAANG & Product Level",
    "subtopic": "Multi-Pattern System Design Core Mechanics",
    "phase": "Phase 20 \u2014 Interview Simulation",
    "pattern": "Multi-Pattern System Design",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Garbage Collector Reference Graph Analyzer (Variant 200)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Multi-Pattern System Design. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Multi-Pattern System Design technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Multi-Pattern System Design)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Multi-Pattern System Design)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Multi-Pattern System Design)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Multi-Pattern System Design)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      998,
      1000,
      994
    ],
    "prerequisites": [
      997
    ],
    "tags": [
      "FAANG & Product Level",
      "Multi-Pattern System Design",
      "Phase 20",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Multi-Pattern System Design because constraints are N <= 1000000, making O(N^2) brute force infeasible. Multi-Pattern System Design leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Multi-Pattern System Design optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  },
  {
    "id": 1000,
    "number": 1000,
    "title": "Memory Allocator First Fit Optimizer (Variant 200)",
    "difficulty": "Hard",
    "topic": "FAANG & Product Level",
    "subtopic": "Multi-Pattern System Design Core Mechanics",
    "phase": "Phase 20 \u2014 Interview Simulation",
    "pattern": "Multi-Pattern System Design",
    "estimatedTime": 45,
    "statement": "Given an input configuration representative of **Memory Allocator First Fit Optimizer (Variant 200)**, write an optimal algorithm to return the required output according to the problem constraints.",
    "constraints": [
      "1 <= N <= 1000000",
      "-10^9 <= Value <= 10^9",
      "Time Complexity Requirement: O(N) or O(N log N)",
      "Space Complexity Requirement: O(1) or O(N)"
    ],
    "examples": [
      {
        "input": "input = [1, 2, 3, 4]",
        "output": "[2, 4, 6, 8]",
        "explanation": "Each element is multiplied by 2 according to rule."
      },
      {
        "input": "input = [5, 10, 15]",
        "output": "[10, 20, 30]",
        "explanation": "Transformation applied directly."
      }
    ],
    "hints": [
      "Think about the primary invariant of Multi-Pattern System Design. Can you simplify the lookup using extra memory?",
      "Consider sorting or using a two-pointer approach to shrink the search space.",
      "Analyze the bottleneck of brute force. Can a Hash Map or Monotonic Stack reduce O(N^2) to O(N)?"
    ],
    "bruteForce": {
      "intuition": "Iterate through all pairs or combinations using nested loops, checking if the candidate satisfies the condition.",
      "approach": "Nested loop iteration over all possible sub-arrays/combinations.",
      "timeComplexity": "O(2^N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Brute Force C++ Solution\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solve(vector<int>& nums) {\n        int n = nums.size();\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n};",
        "java": "// Brute Force Java Solution\npublic class Solution {\n    public int solve(int[] nums) {\n        int n = nums.length;\n        int ans = 0;\n        for (int i = 0; i < n; i++) {\n            for (int j = i + 1; j < n; j++) {\n                ans = Math.max(ans, nums[i] + nums[j]);\n            }\n        }\n        return ans;\n    }\n}",
        "python": "# Brute Force Python Solution\nclass Solution:\n    def solve(self, nums: list[int]) -> int:\n        n = len(nums)\n        ans = 0\n        for i in range(n):\n            for j in range(i + 1, n):\n                ans = max(ans, nums[i] + nums[j])\n        return ans",
        "javascript": "// Brute Force JavaScript Solution\nfunction solve(nums) {\n    let ans = 0;\n    for (let i = 0; i < nums.length; i++) {\n        for (let j = i + 1; j < nums.length; j++) {\n            ans = Math.max(ans, nums[i] + nums[j]);\n        }\n    }\n    return ans;\n}"
      }
    },
    "optimalSolution": {
      "intuition": "Use the Multi-Pattern System Design technique to maintain optimal state dynamically and process elements in linear time.",
      "approach": "Initialize pointers/frequency map/stack, traverse the input once, updating state and maintaining the invariant.",
      "timeComplexity": "O(N log N)",
      "spaceComplexity": "O(1)",
      "code": {
        "cpp": "// Optimal C++ Solution (Multi-Pattern System Design)\n#include <vector>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int solveOptimal(vector<int>& nums) {\n        unordered_map<int, int> mp;\n        int maxVal = 0;\n        for (int x : nums) {\n            mp[x]++;\n            maxVal = max(maxVal, x);\n        }\n        return maxVal;\n    }\n};",
        "java": "// Optimal Java Solution (Multi-Pattern System Design)\nimport java.util.*;\n\npublic class Solution {\n    public int solveOptimal(int[] nums) {\n        Map<Integer, Integer> map = new HashMap<>();\n        int maxVal = 0;\n        for (int x : nums) {\n            map.put(x, map.getOrDefault(x, 0) + 1);\n            maxVal = Math.max(maxVal, x);\n        }\n        return maxVal;\n    }\n}",
        "python": "# Optimal Python Solution (Multi-Pattern System Design)\nclass Solution:\n    def solveOptimal(self, nums: list[int]) -> int:\n        seen = {}\n        max_val = 0\n        for x in nums:\n            seen[x] = seen.get(x, 0) + 1\n            max_val = max(max_val, x)\n        return max_val",
        "javascript": "// Optimal JavaScript Solution (Multi-Pattern System Design)\nfunction solveOptimal(nums) {\n    const map = new Map();\n    let maxVal = -Infinity;\n    for (const x of nums) {\n        map.set(x, (map.get(x) || 0) + 1);\n        if (x > maxVal) maxVal = x;\n    }\n    return maxVal;\n}"
      }
    },
    "edgeCases": [
      "Empty array or single-element input.",
      "Array containing negative numbers or duplicate elements.",
      "Maximum integer limits causing integer overflow during calculation."
    ],
    "commonMistakes": [
      "Forgetting to clear or reinitialize state variables across iterations.",
      "Off-by-one errors in pointer bounds or window termination conditions.",
      "Not handling negative values when using modulo operations."
    ],
    "relatedProblems": [
      999,
      1000,
      995
    ],
    "prerequisites": [
      998
    ],
    "tags": [
      "FAANG & Product Level",
      "Multi-Pattern System Design",
      "Phase 20",
      "Hard"
    ],
    "whyThisPattern": "Interviewer expects Multi-Pattern System Design because constraints are N <= 1000000, making O(N^2) brute force infeasible. Multi-Pattern System Design leverages monotonic or sorted properties to prune search space.",
    "interviewExplanation": "1. State assumptions and edge cases.\n2. Present O(N^2) brute force approach first.\n3. Identify bottleneck (redundant comparisons).\n4. Propose Multi-Pattern System Design optimal solution reducing complexity to O(N).\n5. Dry run with Example 1.",
    "reasoningChallenge": "Before writing code, can you state the optimal data structure to achieve O(1) average lookup time for this problem?",
    "testCases": [
      {
        "input": "[2, 7, 11, 15]",
        "expected": "[0, 1]"
      },
      {
        "input": "[3, 2, 4]",
        "expected": "[1, 2]"
      },
      {
        "input": "[3, 3]",
        "expected": "[0, 1]"
      }
    ]
  }
];
if (typeof module !== 'undefined') module.exports = PROBLEMS;
